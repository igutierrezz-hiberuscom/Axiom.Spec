# Plan de revisión integral de Axiom

## Objetivo

Entender el runtime actual de `Axiom/` de arriba abajo antes de decidir qué simplificar, retirar, corregir o ampliar. La revisión se realizará en sesiones independientes y este documento será el registro único de áreas, hallazgos y acciones propuestas.

Durante esta fase no se modifica el producto. Las decisiones de cambio se registran aquí y se ejecutan después mediante incrementos o bugs separados, con su propia validación.

**Aclaración histórica (R-10, 2026-08-18):** Las filas históricas de este registro que describen `copilot-vscode` o `visual-studio-2026` como «alias» conservan nomenclatura retirada. `copilot-vscode` sólo es un literal histórico que `configure` puede migrar desde `init.json`; `visual-studio-2026` siguió siendo un target canónico distinto que delega y reutiliza el writer de `github-copilot` para la salida común.

## Preparación de cada sesión

1. Abrir el workspace completo: `Axiom.SDD`, `Axiom.Spec`, `Axiom` y `Axiom.Pruebas`.
2. Iniciar una conversación limpia y seleccionar `Axiom Runtime Auditor` en Copilot, o escribir `Auditor Axiom: R-XX` en los otros targets.
3. Indicar el identificador del área, por ejemplo:

   ```text
   Auditor Axiom: R-04. Explícame esta área desde cero y no cambies nada fuera del plan de revisión.
   ```

4. Resolver dudas durante la conversación. El agente explica primero y solo anota una acción cuando se le pide expresamente.
5. Cerrar la sesión con un resumen breve. El siguiente área empieza en una sesión limpia, no con un contexto acumulado.

## Método de revisión

Para cada área el agente debe:

1. Localizar las fuentes reales: código, configuración, tests y documentación.
2. Explicar en castellano claro qué es cada pieza, por qué existe, cómo funciona, cómo se usa y con qué está conectada.
3. Trazar al menos un flujo concreto de entrada a salida cuando aplique.
4. Distinguir hechos verificados de documentación histórica o afirmaciones no confirmadas.
5. Registrar únicamente las acciones que el usuario solicite conservar.

La prioridad de evidencia es: comportamiento ejecutable y tests, código fuente, configuración activa, documentación operativa y, por último, documentación histórica. Una discrepancia se registra como investigación, no como eliminación automática.

## Áreas y sesiones

| ID | Área | Alcance principal | Pregunta guía |
| --- | --- | --- | --- |
| R-00 | Mapa y reglas de revisión | Raíces del workspace, ownership, entradas de documentación y límites de la auditoría | ¿Qué repositorio es responsable de cada cosa y qué fuentes describen el estado actual? |
| R-01 | Estructura, build y empaquetado | `package.json`, TypeScript project references, workspaces, scripts, `dist/`, configuración de tests | ¿Qué se compila, qué se publica o ejecuta y qué artefactos pueden ser residuales? |
| R-02 | Shell de la CLI y registro de comandos | `apps/cli/src/index.ts`, bindings, bootstrap, helpers compartidos y registro de comandos | ¿Cómo entra una orden al runtime y qué comandos están realmente conectados? |
| R-03 | Lifecycle básico de proyecto | `init`, `join`, `configure`, `sync`, `start` y sus dependencias directas | ¿Qué crea o modifica cada paso y cuál es la secuencia mínima útil? |
| R-04 | Configuración y modelo de capacidades | `axiom.config/`, `config-validation`, `capability-model`, `providers`, `install-profiles` | ¿Qué YAML decide el comportamiento y qué configuración se usa realmente? |
| R-05 | Estado, filesystem y aislamiento | `core`, `persistence`, `filesystem-truth`, `project-resolution`, `isolation`, `installer` | ¿Dónde persiste Axiom el estado, cómo encuentra un proyecto y cómo evita mezclar proyectos? |
| R-06 | Scaffolding y superficies generadas | Comandos de scaffold, `document-bootstrap`, plantillas y archivos emitidos | ¿Qué archivos se generan, cuáles se pueden editar y quién los regenera? |
| R-07 | Targets y adapters | `packages/adapters/*`, support matrix y proyección a OpenCode, Claude Code, Copilot, Antigravity y otros targets | ¿Qué soporte es real por target, qué archivos recibe y qué queda en fallback? |
| R-08 | Agents, skills y componentes | `agents`, `skills`, `components`, catálogos y materializadores | ¿Qué contratos se instalan para los agentes, qué hacen de verdad y qué puede sobrar o estar duplicado? |
| R-09 | Salud, auditoría y telemetría | `doctor`, `audit`, `telemetry`, políticas y comandos asociados | ¿Qué se valida, qué bloquea operaciones y qué datos se registran o emiten? |
| R-10 | Workflow SDD y gobierno de cambios | `workflow`, `orchestrator`, `cavekit-discipline` y comandos de incrementos, bugs, planes, roles, fases y freeze | ¿Cuál es el flujo SDD que se ejecuta realmente y qué rutas son solo contratos o stubs? |
| R-11 | Contexto técnico e integraciones de herramientas | `toolchain`, `tool-routing`, `mcp-server`, `mcp-tools`, `technical-context`, `knowledge*` | ¿Cómo obtiene y entrega contexto Axiom, y qué integraciones son necesarias frente a aspiracionales? |
| R-12 | Memoria, trackers y sincronización externa | `memory`, `tracker`, `tracker-ado`, `external-sync`, `learn` | ¿Qué información mantiene Axiom, dónde queda y qué dependencias externas añade? |
| R-13 | Workspace de usuario, topología y launcher | `user-workspace`, `topology`, `launcher`, `projects`, `repo`, `gateway`, `self-update` | ¿Qué vive fuera del proyecto, cómo se conectan varios repositorios y qué automatismos se activan? |
| R-14 | Control de operador e interfaces | `model-routing`, `versioning`, `upgrade`, `tui`, `app*` y comandos relacionados | ¿Qué controles ya son operativos, qué interfaz los expone y qué partes son solo prototipo? |
| R-15 | Documentación, spec e historial | `docs/`, `axiom.spec/`, `Axiom.Spec/`, referencias legacy y fuentes duplicadas | ¿Qué documentación está vigente, cuál es histórica y dónde hay contradicciones que puedan confundir? |
| R-16 | Pruebas, coherencia y cierre del inventario | Vitest, scripts de readiness y doctor, artefactos `dist/` y síntesis de los hallazgos | ¿Qué está cubierto, qué no se puede verificar y qué acciones deben priorizarse? |

## Orden recomendado

Completar las áreas en orden. Si surge una dependencia, se puede consultar puntualmente otra área, pero la decisión se registra en la sesión propietaria. R-15 y R-16 se reservan para contrastar las conclusiones contra el estado real y priorizar la lista final.

## Registro de sesiones

El agente agrega aquí una entrada por sesión terminada. No debe reemplazar entradas anteriores.
Las entradas de R-15 fechadas el 2026-09-12 son el diagnóstico y la definición
iniciales del lote; sus cifras y estados fueron supersedidos por el cierre
consolidado del 2026-09-13, que es la referencia vigente.

| Fecha | Área | Resumen verificado | Dudas abiertas | Referencias revisadas |
| --- | --- | --- | --- | --- |
| 2026-08-03 | R-00 | Se verificó el ownership del workspace: `Axiom.Spec/` es el repo canónico, `Axiom/` contiene el runtime y `Axiom.SDD/` contiene el workflow; se migraron los ocho ADR a `Axiom.Spec/decisions/`, se alineó la documentación y se conservó `Axiom/axiom.spec/` como baseline product-owned por sus consumidores activos. | Ninguna bloqueante. La similitud nominal queda mitigada y documentada por ADR-0032. | `Axiom.SDD/AGENTS.md`, `Axiom.Spec/README.md`, `Axiom.Spec/decisions/`, `Axiom.Spec/decisions/0032-axiom-spec-boundary-and-runtime-baseline.md`, `Axiom/axiom.yaml`, `Axiom/axiom.config/topology.yaml`, consumidores de `Axiom/axiom.spec/`. |
| 2026-08-04 | R-01 | Se verificó que el build raíz usa TypeScript project references: cada paquete genera su propio `dist/`, la CLI termina en `apps/cli/dist/index.js` y los artefactos generados están ignorados por Git. `@axiom/cli-commands` y `@axiom/tui` usan hoy `rootDir: "../.."`; `cli-commands` además incluye fuentes que viven bajo `apps/cli`, por lo que emite rutas anidadas y declara `dist/packages/...` como `main/types`. TUI solo incluye fuentes bajo su propio `src` y pasa una comprobación con raíz local. El build completo terminó correctamente y el test nativo de instalación pasó 18/18. | La salida anidada de `cli-commands` no se puede corregir solo cambiando `rootDir` a `src`: TypeScript falla porque sus fuentes y dependencias directas están fuera de esa raíz. Hay que decidir cómo conservar el ownership único sin mezclar raíces. TUI parece una normalización independiente y más sencilla. La suite completa de Vitest produjo una salida excesiva y no se obtuvo un resumen final limpio en esta sesión; las pruebas focalizadas y el build sí fueron comprobados. | `Axiom/package.json`, `Axiom/tsconfig.json`, `Axiom/tsconfig.base.json`, `Axiom/vitest.config.ts`, `Axiom/apps/cli/package.json`, `Axiom/apps/cli/tsconfig.json`, `Axiom/apps/cli/README.md`, `Axiom/packages/cli-commands/package.json`, `Axiom/packages/cli-commands/tsconfig.json`, `Axiom/packages/cli-commands/src/index.ts`, `Axiom/packages/tui/package.json`, `Axiom/packages/tui/tsconfig.json`, `Axiom/scripts/install-global.test.mjs`, `Axiom/.gitignore`. |
| 2026-08-04 | R-02 | Se verificó que `Axiom/apps/cli/src/index.ts` crea la CLI, registra la superficie de comandos en un orden explícito, define `doctor` directamente, inicializa la telemetría de forma no bloqueante, abre la TUI cuando no se indica subcomando y finalmente entrega los argumentos al enrutador de comandos. La ayuda compilada expone los comandos registrados. Se confirmó la separación entre el registro (`registerX`) y el trabajo (`runX`) en `init`, `join`, `bindings` y `bootstrap`; las pruebas enfocadas pasaron 24/24. | No se verificó el comportamiento completo de cada subcomando. `phase`, `freeze` y `doctor` contienen parte de su lógica directamente en el registro de la CLI. Un comentario antiguo de `@axiom/cli-commands` dice que no se reexporta `registerX`, pero el código vigente sí lo hace y declara que esa decisión fue superada. | `Axiom/apps/cli/src/index.ts`, `Axiom/apps/cli/src/commands/_shared.ts`, `Axiom/apps/cli/src/commands/init.ts`, `Axiom/apps/cli/src/commands/join.ts`, `Axiom/apps/cli/src/commands/bindings.ts`, `Axiom/apps/cli/src/commands/bootstrap.ts`, `Axiom/packages/cli-commands/src/index.ts`, `Axiom/apps/cli/tsconfig.json`, `Axiom/apps/cli/tests/bindings.test.ts`, `Axiom/apps/cli/tests/bootstrap.test.ts`. |
| 2026-08-04 | R-03 | Se verificó el flujo básico: `init` crea `axiom.yaml`, el estado local y `init.json`; `join` solo registra miembros y es opcional; `configure` compone `install-profile.json`; `sync` regenera outputs solo después de pasar su gate; `start` comprueba un primer ruteo y escribe `last-start.json`. Las pruebas focalizadas pasan: 5 archivos, 50 pruebas. | La sospecha inicial sobre `enterprise` y las señales del sink quedó resuelta el 2026-08-05: el loader construye un `AuditTrailSink` real que cubre `lineage` y `verification`. También se confirmó que la afirmación de `topology.yaml` era documentación obsoleta, no comportamiento actual. | `Axiom/apps/cli/src/commands/init.ts`, `join.ts`, `configure.ts`, `sync.ts`, `start.ts`, `_shared.ts`, `Axiom/packages/orchestrator/src/state-machine.ts`, `Axiom/packages/telemetry/src/loader.ts`, pruebas focalizadas en `Axiom/apps/cli/tests/`, `Axiom/axiom.config/profiles.yaml`, `telemetry-sinks.yaml`, `providers.yaml`, `Axiom/docs/cli/`. |
| 2026-08-04 | R-13/R-14 (consulta cruzada) | Se verificó que `axiom app` ya abre por defecto el launcher web local bajo `/launcher/`, con onboarding de instalación y unión, catálogo de acciones, previews, ejecución confirmada, registro de incrementos/bugs/planes, doctor y paneles ADO/Git. También se verificó que `axiom tui` sigue registrado como superficie independiente y que el paquete `@axiom/tui` mantiene su propio driver, pantallas, flujos y pruebas. | El launcher actual no cubre todavía de forma demostrada todo el recorrido de migración de spec/contexto desde su interfaz; el registro expone `id`, `title` y `status`, pero no la relación completa entre plan, roles, repositorio e implementación. La ejecución genérica de plugins declarativos todavía no está conectada a un endpoint de comandos/scripts; Azure DevOps sí tiene rutas especializadas. La retirada de la TUI depende de completar y probar esa paridad. | `Axiom/apps/cli/src/index.ts`, `Axiom/apps/cli/src/commands/app.ts`, `app-api.ts`, `app-launcher.ts`, `app-onboarding.ts`, `app-plugins.ts`, `app-plugins-azure-devops.ts`, `Axiom/apps/cli/static/launcher/`, `Axiom/packages/launcher/src/`, `Axiom/packages/tui/src/`, `Axiom/packages/tui/tests/`, `Axiom/apps/cli/tests/app-launcher.test.ts`, `Axiom/apps/cli/tests/tui.test.ts`, `Axiom.Spec/specs/05_Interfaces_Operativas.md`, `Axiom.Spec/specs/06_Integraciones_y_Capacidades.md`. |

| 2026-08-04 | R-13/R-14 -> ACC-004..ACC-008 | Se ejecutó el lote solicitado mediante cinco incrementos SDD secuenciados: ACC-004 normalizó `@axiom/cli-commands`; ACC-006 completó onboarding/adopción del launcher; ACC-007 completó el ciclo SDD; ACC-008 integró plugins allowlisted y seguros; ACC-005 retiró la TUI solo después de verificar la paridad. Se integraron los hechos estables en `specs/00..08` y `context/**`, con review independiente, freeze/receipts y archive completados. | R-04 quedó como siguiente área; se mantienen como riesgos residuales la cobertura DOM del launcher y el redactor de plugins basado en patrones. | `Axiom.Spec/specs/increments/INC-20260804-*`, `Axiom.Spec/specs/00..08`, `Axiom.Spec/context/**`, `Axiom/` build/doctor/readiness y suites focalizadas. |
| 2026-08-05 | R-04 | Se verificó la ruta real de configuración: `profiles.yaml` y `DEFAULT_PROFILES` coinciden en functional profiles, overlays, targets, bindings y aliases; `loadCapabilityModel` carga 19 capabilities (16 provider-routed y 3 MCP-only), 6 providers y sus fallbacks desde `axiom.config`; la batería focalizada de validación/composición pasa 4 archivos y 70 tests. Se corrigió la lectura inicial: `config-validation` acepta el kind `structural-code-intel` usado por `cmm`; `installProfile` solo usa `DEFAULT_PROFILES` cuando `profiles.yaml` está ausente y devuelve `invalid-profiles-yaml` si el archivo presente es ilegible o inválido; los tipos separan los cuatro dominios provider-routed de las capabilities MCP-only `axiom.*`; `CC-004` comprueba cobertura canónica y distingue capabilities no declaradas, huérfanas requeridas, opcionales o inactivas. | La petición del usuario abre dos acciones propuestas: eliminar el eje de perfiles funcionales y colapsar los tres overlays en una política fija. Queda por definir la política única de telemetría y retención. `generated-snapshots` está declarado como provider de fallback y modo de resolución, pero no se encontró un generador o cliente ejecutable en `src`; no se autoriza retirarlo automáticamente. | `Axiom/axiom.config/{profiles,capabilities,providers}.yaml`, `Axiom/packages/config-validation/src/{schemas,validator}.ts`, `Axiom/packages/capability-model/src/{constants,loader,types}.ts`, `Axiom/packages/install-profiles/src/{composer,default-profiles,types}.ts`, `Axiom/packages/installer/src/{installer,profiles-loader}.ts`, `Axiom/packages/doctor/src/checks.ts`, `Axiom.Spec/specs/03_Modelo_Operativo_y_Datos.md`. |
| 2026-08-05 | R-02 (cierre) | Se corrigió el comentario obsoleto de `@axiom/cli-commands`: ahora documenta que el barrel reexporta tanto `runX` como los wrappers `registerX`, y que `apps/cli/src/index.ts` usa estos últimos para registrar Commander. R-02 queda cerrado sin cambios de lógica. | Ninguna. | `Axiom/packages/cli-commands/src/index.ts`, `Axiom/apps/cli/src/index.ts`, `Axiom/packages/cli-commands/tsconfig.json`, `Axiom/packages/cli-commands/package.json`. |
| 2026-08-05 | R-03 (corrección documental y decisión sobre enterprise) | Se alinearon los manuales de `init` con el código: `init` no crea `topology.yaml` ni el repo hermano de specification, sí puede escribir `AGENTS.md`, persiste `init.json`, admite `--role`/aliases/`--no-register` y registra el proyecto de forma best-effort. Se documentó `enterprise` con su alcance real: gate local de telemetría, modo gateway, snapshot project-scoped, detección de drift y selección limitada de `axiom-gateway`. La batería de gateway/start/sync/routing pasa 4 archivos y 63 tests; la validación documental también pasa. | `enterprise` funciona como política y estado local, pero no es un daemon ni un broker remoto: `start` no inicia el gateway, `gateway start` solo persiste el snapshot y `routeTool` no abre conexiones. Además, `discoveryProviderProfile: filesystem-first` convive hoy con `discoveryOrder: [gateway, filesystem]`; queda como deuda de configuración. Se conserva provisionalmente; la decisión de retirarlo o ampliarlo queda pendiente de una petición posterior. | `Axiom/docs/cli/init.md`, `Axiom/docs/generated-files.md`, `Axiom/docs/installation.md`, `Axiom/docs/cli/start.md`, `Axiom/docs/usage/README.md`, `Axiom/docs/configuration/{profiles-overlays-targets.md,files/profiles.md,files/telemetry-sinks.md}`, `Axiom/docs/overview.md`, `Axiom/apps/cli/src/commands/{init,start,gateway,sync}.ts`, `Axiom/packages/{tool-routing,telemetry}/src/`, contexto técnico actualizado. |
| 2026-08-05 | R-03/R-04/R-11 (análisis funcional de gateway) | Se verificó que `enterprise` es un preset de gobernanza local: exige `lineage`/`verification` en el gate de `sync`, selecciona el modo `gateway`, y `gateway start/status` guarda y compara un snapshot local de configuración. Se verificó que `axiom-gateway` solo declara `sdd.commandResolution`, no tiene `ProviderClient` registrado y puede terminar en `provider-unavailable`; las capabilities reales del MCP se asignan a `sdd-mcp-server`, `spec-mcp-broker` y al broker separado `axiom-mcp-broker`. La batería de providers, doctor y broker MCP pasa 4 archivos y 28 tests. | Para un MVP usable, `enterprise` y `axiom-gateway` parecen prescindibles; la auditoría no ejecutó su retirada. El audit trail y `axiom-mcp-broker` son piezas separadas y no deben eliminarse automáticamente. Si se retira gateway, `standard` pierde su diferencia operativa principal (`--gateway` opt-in) y habrá que decidir si se conserva como metadata o se colapsa con `local-only`. No se registró una acción de cambio. | `Axiom/axiom.config/{profiles,providers,mcp-manifest,integrations}.yaml`, `Axiom/apps/cli/src/commands/{start,gateway,mcp-serve}.ts`, `Axiom/packages/{capability-model,tool-routing,providers,telemetry,mcp-server,mcp-tools,doctor}/src/`, pruebas `providers`, `doctor/gateway` y `mcp-server/axiom-broker`. |
| 2026-08-05 | R-04 (explicación y acciones solicitadas) | Se explicó que el perfil funcional actual controla capacidades activadas, providers preferidos, alcance de mutación, roles de implementación y bloqueos de degradación; se explicó que el overlay controla riesgo declarado, orden de discovery, expectativa de gateway, flags de `start`, gate de `sync` y selección de sink/retención de telemetría. `local-only` usa filesystem y prohíbe gateway; `standard` usa filesystem por defecto y permite gateway opt-in; `enterprise` prioriza gateway, rechaza `--no-gateway`, exige `lineage`/`verification` en `sync` y mantiene un estado local de gateway, pero no inicia un daemon ni abre una conexión remota. `generated-snapshots` es una entrada declarativa de fallback y un modo que el router sabe reportar; no es un servicio ejecutable de snapshots encontrado en el código fuente. | Se registraron ACC-017, ACC-018 y ACC-019 como propuestas del usuario. La eliminación del perfil funcional debe distinguir permisos de usuario frente a roles de equipo y nombres de fases del workflow. La política única resultante debe decidir si conserva un audit trail común y qué retención usa. No se registró una acción independiente para retirar `generated-snapshots`. | `Axiom/axiom.config/{profiles,providers,telemetry-sinks}.yaml`, `Axiom/apps/cli/src/commands/{start,gateway}.ts`, `Axiom/packages/cli-commands/src/commands/{_shared,sync}.ts`, `Axiom/packages/{install-profiles,tool-routing,telemetry,memory}/src/`, `Axiom/docs/configuration/profiles-overlays-targets.md`, `Axiom/docs/cli/{start,sync,configure}.md`, pruebas focalizadas de gateway, start, sync, routing y providers. |
| 2026-08-05 | R-04 (decisión explícita de simplificación) | El usuario decidió que el producto tendrá una única configuración funcional `builder` y una única política operativa local-only, ambas implícitas y no seleccionables. Se retirarán `product-owner`, sus aliases, `standard`, `enterprise`, `axiom-gateway` y todas sus superficies de gateway; se conservarán el audit trail como trazabilidad permanente y los servidores MCP reales (`axiom-mcp-broker`, `sdd-mcp-server`, `spec-mcp-broker`). Se registraron ACC-017, ACC-018, ACC-019 y ACC-020 como acciones propuestas; no se ejecutaron cambios de código en esta auditoría. | Queda por definir durante la implementación la migración de estados antiguos, la retención local única, el destino de `generated-snapshots` si no queda ningún uso y la posible compatibilidad de targets/documentos que mencionen gateway. | `Axiom.Spec/plans/PLAN-REVISION-INTEGRAL-AXIOM.md`, acciones `ACC-017..ACC-020`, `Axiom/axiom.config/{profiles,providers,telemetry-sinks,mcp-manifest}.yaml`, comandos `init`, `start`, `gateway`, `mcp-serve`, paquetes `capability-model`, `tool-routing`, `providers`, `telemetry`, `mcp-server`, `mcp-tools`, `doctor`. |
| 2026-08-05 | R-04 (acción explícita sobre generated-snapshots) | El usuario pidió retirar `generated-snapshots` de Axiom porque no identifica utilidad ni contenido operativo en la implementación actual. Se registró ACC-021 como propuesta de eliminación; no se ejecutaron cambios de código. | La implementación debe confirmar el alcance completo de la retirada y distinguir este marcador declarativo del backend JSON local de memoria, que sí es un fallback ejecutable e independiente. También debe comprobar si quedan referencias históricas que deban conservarse solo como historia. | `Axiom/axiom.config/providers.yaml`, `Axiom/packages/capability-model/src/{constants,loader,types}.ts`, `Axiom/packages/tool-routing/src/{dispatcher,types}.ts`, `Axiom/packages/providers/src/selectable.ts`, `Axiom/packages/memory/src/resolve-backend.ts`, `Axiom/packages/mcp-tools/src/memory-handlers.ts`, `Axiom/packages/{capability-model,doctor,tool-routing,providers,memory}/tests/`. |
| 2026-08-05 | R-04 (consolidación de acciones) | Se compararon las acciones `ACC-015` a `ACC-021` por resultado y no solo por área. `ACC-021` queda absorbida por `ACC-018`, que pasa a contener también la retirada completa de `generated-snapshots`; `ACC-015` y `ACC-016` no se duplican porque son, respectivamente, una corrección documental ya validada y la investigación que fundamenta la retirada. `ACC-019` y `ACC-020` siguen siendo trabajos posteriores con alcance propio. | No quedan duplicaciones activas entre R-03 y R-04. La ejecución de `ACC-017`, `ACC-018`, `ACC-019` y `ACC-020` sigue pendiente; `ACC-021` no se ejecuta como acción independiente. | `Axiom.Spec/plans/PLAN-REVISION-INTEGRAL-AXIOM.md`, acciones `ACC-015..ACC-021`. |
| 2026-08-06 | R-04 (cierre de ACC-017..ACC-020) | Se implementó y verificó el lote completo: `builder` y `local-only` quedaron implícitos; el registry quedó en cuatro providers locales; gateway, overlays retirados y `generated-snapshots` dejaron de ser contratos activos; el audit trail conserva `P365D`; los tres brokers MCP permanecen independientes; la documentación activa y el contexto técnico fueron reconciliados. `CC-004` queda en warning no bloqueante por 3 capabilities opcionales sin provider (`13/16`). | No queda acción abierta del lote R-04. Se conserva como deuda de tooling la diferencia entre el candidato auxiliar materializado bajo `Axiom/axiom.spec/increments/` y el scope canónico de freeze/receipts bajo `Axiom.Spec/specs/increments/`; no se borra el artefacto product-owned de forma destructiva. R-05 sigue siendo la siguiente área de auditoría; no se inventan acciones para R-05..R-16. | `Axiom.Spec/specs/increments/INC-20260805-225413-6fx4hs/`, `Axiom.Spec/specs/increments/INC-20260806-docs-builder-local-only/`, `Axiom.Spec/specs/00..08`, `Axiom.Spec/context/**`, `Axiom/vitest.config.ts`, build/doctor/readiness y suite final `326/326`, `3313/3313`. |
| 2026-08-06 | R-04 (revisión independiente y remediación final) | `axiom-reviewer` encontró un selector activo de perfil/overlay en el launcher estático, README/manuales con gates retirados, una prueba de perfiles semánticamente obsoleta, hashes de bundles ya corregidos y deriva entre `integrations.yaml` y `mcp-manifest.yaml`. Se corrigieron todas esas superficies; el barrido restante solo conserva referencias dentro de secciones históricas explícitas. | El warning de hygiene de `Axiom/apps/cli/dist/commands/gateway.js` quedó resuelto al eliminar el fuente residual `apps/cli/src/commands/gateway.ts` y limpiar sus salidas generadas. El candidato auxiliar product-owned conserva además un receipt histórico cuyo texto menciona `INC-20260806-unified-builder-local-only` en lugar del ID de su carpeta; no se reescriben receipts inmutables ni se elimina el artefacto de forma destructiva. | Batería launcher/perfiles `73/73`, checks doctor/MCP `77/77`, build limpio, doctor PASS `45/60`, readiness PASS, suite final `326/326` archivos y `3313/3313` tests; review independiente `REVIEW-001..008` resueltos salvo la nota histórica del receipt. |
| 2026-08-06 | R-05 | Se verificó que `resolveProject` encuentra `axiom.yaml`, valida v1/v2 y bloquea estados no resueltos antes de mutar; el helper CLI liga un `FilesystemStore` a la raíz resuelta. El store valida claves, calcula rutas bajo `.axiom-state`, devuelve errores estructurados y separa proyectos por `projectName`; los estados de componentes, model-routing y managed-state usan `config`, mientras workflow, memoria, MCP bindings y audit trail conservan escritores especializados en `local`. Se verificó además el aislamiento por ejecución bajo `.axiom-state/executions/<id>/`, el guard de `executionId`, la persistencia atómica de `execution.json` y el harvest antes de retirar un worktree. Las suites focalizadas de R-05 pasaron `14/14` archivos y `162/162` tests. | Conviven dos convenciones físicas: el `FilesystemStore` usa `.axiom-state/<scope>/<projectName>/`, pero `init.json`, `install-profile.json`, checkpoints y varios comandos escriben directamente en `.axiom-state/<projectName>/`; memoria, workflow, bindings MCP y telemetría usan variantes de `.axiom-state/local/`. El README de `persistence` afirma escritura atómica y scopes que no coinciden plenamente con el código actual. La allowlist P0 usa nombres lógicos (`sdd`, `spec`, `serena`) y el manifest activo declara además los servidores ejecutables (`sdd-mcp-server`, `spec-mcp-broker`, `axiom-mcp-broker`); no se registró una acción para reconciliarlos. | `Axiom/packages/filesystem-truth/src/{discovery,path-canonical}.ts`, `Axiom/packages/project-resolution/src/resolver.ts`, `Axiom/packages/persistence/src/{types,path-guard,isolation,filesystem-store,execution-store,execution-harvest}.ts`, `Axiom/packages/isolation/src/{p0,execution-paths,execution}.ts`, `Axiom/packages/installer/src/{installer,persist}.ts`, consumidores `components`, `model-routing`, `workflow`, `memory`, `telemetry`, `cli-commands`, `Axiom/axiom.config/{mcp-manifest,integrations}.yaml`. |
| 2026-08-06 | R-05 (modos y rutas) | El resolver solo necesita exponer la política efectiva `local-only`; `gateway` y `hybrid` sobreviven como unión pública, normalización y expectativa de test, no como modos activos. Para el estado, la opción coherente es conservar `.axiom-state/<projectKey>/` como único namespace project-scoped, retirar el segmento físico `config`, sacar memoria, workflow y bindings MCP de `local`, y reservar `local` para datos realmente repo/operador-locales. Se registraron `ACC-022` y `ACC-023` como propuestas; no se modificó código. | La implementación deberá fijar cómo se deriva `projectKey` para v1/v2 y cómo migra estados que usaron el nombre humano frente al `projectId` slug. `axiom.config/` declarativo, `~/.axiom/` user-level y `.axiom-state/executions/<id>/` execution-scoped no deben confundirse con el namespace project-scoped. | `Axiom/packages/project-resolution/src/{resolver,index}.ts`, `Axiom/packages/config-validation/src/schemas.ts`, `Axiom/packages/doctor/src/checks.ts`, `Axiom/packages/persistence/src/{types,isolation,filesystem-store}.ts`, `Axiom/packages/isolation/src/{p0,execution-paths}.ts`, `Axiom/packages/{memory,workflow,topology,versioning,installer,components,model-routing}/src/`, `Axiom/apps/cli/src/commands/{init,join,start,workspace-setup,member-install,external-sync,app-plugins}.ts`, `Axiom.Spec/specs/03_Modelo_Operativo_y_Datos.md`, `Axiom.Spec/context/architecture/02-modelo-de-datos-y-configuracion.md`. |
| 2026-08-06 | R-05 (cierre ACC-022/ACC-023) | Se implementaron ambas acciones mediante el autopilot. `ProjectMode` quedó limitado a `local-only` con compatibilidad raw v1/v2; el estado project-bound usa `.axiom-state/<projectKey>/`, `config` quedó como label API, y `local`/`executions` conservan sus fronteras. La migración legacy es determinista, atómica e idempotente; checkpoint restore remapea destinos, toolchain detect/probe/repair acepta aliases y elimina duplicados, y worktree provisioning usa `Execution.projectId`. Ambos incrementos tienen review independiente sin blockers, metadata `archived`, integración `integrated`, freeze final verificable y receipts SHA-256 íntegros. | Ninguna bloqueante. La ejecución paralela de la suite mostró interferencia no determinista en dos tests no relacionados; ambos pasaron aislados y la revisión independiente reportó `327/327` archivos y `3326/3326` tests verdes. | `Axiom.Spec/specs/increments/_archive/INC-20260806-remove-legacy-resolution-modes/`, `Axiom.Spec/specs/increments/_archive/INC-20260806-unify-project-state-paths/`, `Axiom/packages/filesystem-truth/src/state-paths.ts`, `Axiom/packages/versioning/src/checkpoints.ts`, `Axiom/packages/toolchain/src/{detect,probe,repair}.ts`, `Axiom/apps/cli/src/commands/{workspace-worktree-provision,freeze,phase}.ts`, `Axiom/packages/cli-commands/src/commands/_spec-scope.ts`, `Axiom.Spec/specs/00..08`, `Axiom.Spec/context/**`. |

| 2026-08-07 | R-06 | Se verificó el mapa de scaffolding: `workspace setup` compone writers para repos, estado, topología, adapters, superficies de proceso, catálogos y base de spec; `scaffold increment|bug|plan` delega en `scaffoldArtifact`, que resuelve primero plantillas del proyecto y después el bundle, crea un árbol no-clobber y deja la edición humana en los documentos y metadatos. La documentación oficial de GitHub Copilot y VS Code reconoce `.github/copilot-instructions.md` como instrucciones generales y `.github/instructions/*.instructions.md` para reglas por ruta. El runtime distingue `github-copilot` y `copilot-vscode`, pero sus rutas actuales no están reconciliadas. | La ruta recomendada para instrucciones generales es `.github/copilot-instructions.md`; `.vscode/copilot-instructions.md` no aparece como ubicación oficial. `workspace setup` usa `.vscode/copilot-instructions.md`, `configure` usa `.github/copilot-instructions.md` y `sync` usa `.github/copilot-instructions.md` más `.vscode/settings.json`/`extensions.json`; el registry y las superficies `.vscode/*.instructions.md` añaden otras afirmaciones activas. No se registró una acción porque el usuario pidió comprensión, no conservar un cambio. | `Axiom/apps/cli/src/commands/{workspace-setup,workspace-adapters,workspace-process-surfaces,scaffold}.ts`, `Axiom/packages/{document-bootstrap,workflow,installer,cli-commands}/src/`, tests focalizados de adapters/document-bootstrap/sync/workflow (6 archivos, 64 tests), `Axiom/docs/generated-files.md`, documentación oficial GitHub Copilot “Adding repository custom instructions” y VS Code “Custom instructions”. |
| 2026-08-09 | R-07 | Se verificó que Axiom reconoce diez targets declarados, pero la cobertura real se divide entre `configure`, `workspace setup` y `sync`. `github-copilot` y `copilot-vscode` comparten la instrucción general `.github/copilot-instructions.md`; `copilot-vscode` añade configuración nativa de VS Code y `vscode` solo genera `settings.json`/`extensions.json`. La documentación oficial de GitHub, Copilot CLI, VS Code y Visual Studio (incluido `view=vs-2026`) reconoce `.github/copilot-instructions.md` como superficie común de instrucciones. `visual-studio-2026` genera `.vs/AXIOM.md`, pero esa ruta es una convención propia de Axiom y su soporte real no se verificó ejecutando Visual Studio; el propio código marca el MCP de `.vs` como suposición documentada. Los generadores dedicados de `antigravity`, `visual-studio-2026` y `codex` existen, pero `sync` todavía no los invoca y deja `generatedFilesCount: 0`. LiteLLM tiene un generador que escribe `litellm.config.json`, aunque el registry y varias pruebas del instalador lo tratan como proxy sin archivos. | La consolidación exacta de `github-copilot`, `copilot-vscode` y `vscode` requiere elegir un nombre público y decidir si la configuración opcional del editor queda fuera del target de instrucciones. Falta verificar en una instalación real de Visual Studio 2026 si consume `.vs/AXIOM.md` o basta `.github/copilot-instructions.md`; también hay que decidir si `.vs/mcp.json` se conserva como salida verificada o como supuesto. La retirada de LiteLLM debe distinguir el target declarativo del paquete/generador y de cualquier uso futuro externo de LiteLLM. | `Axiom/packages/adapters/README.md`, `Axiom/packages/model-routing/src/support-matrix.ts`, `Axiom/axiom.config/profiles.yaml`, `Axiom/packages/installer/src/registry.ts`, `Axiom/packages/cli-commands/src/commands/{configure,sync}.ts`, `Axiom/apps/cli/src/commands/{workspace-adapters,native-mcp-config}.ts`, `Axiom/packages/adapters/{github-copilot,visual-studio-2026,antigravity,codex,litellm}/src/`, `Axiom/docs/{installation.md,cli/sync.md,generated-files.md}`, documentación oficial de GitHub Copilot, Copilot CLI, VS Code y Visual Studio. |

| 2026-08-09 | R-06 (cierre ACC-024) | Se implementó y verificó la unificación de instrucciones Copilot: ambos targets escriben `.github/copilot-instructions.md`; `.vscode/` conserva settings/extensions/MCP; las process surfaces usan `.github/instructions/*.instructions.md`; `configure`, `sync`, `workspace setup` y `github-copilot` convergen en `@axiom/document-bootstrap`; la migración legacy conserva contenido humano y es conservadora ante conflictos; la selección conjunta de targets se deduplica con precedencia determinista. La plantilla bundleada y las copias on-disk tienen contenido idéntico, con diferencias de saltos `LF`/`CRLF` dependientes del checkout. También se consolidó el contrato de `axiom scaffold increment|bug|plan`: templates del proyecto primero, fallback bundleado, árbol no-clobber por archivo y `metadata.yml` bajo el artifact store. | Ninguna bloqueante. La review independiente final quedó en `OK`; el único blocker encontrado (lockfile ausente y posible pérdida de cola humana) se corrigió y se volvió a validar. No se reauditó R-07 ni se modificaron sus claims. | Consolidación en `Axiom.Spec/specs/{02_Requisitos_No_Funcionales,03_Modelo_Operativo_y_Datos,04_Flujos_SDD_y_Ciclo_de_Vida,05_Interfaces_Operativas,06_Integraciones_y_Capacidades,08_Glosario}.md`, `Axiom.Spec/context/architecture/{02-modelo-de-datos-y-configuracion,04-adapters-y-model-routing}.md`, `Axiom.Spec/context/operations/02-doctor-troubleshooting-y-telemetria.md` y `Axiom.Spec/specs/increments/_archive/INC-20260809-unify-copilot-instruction-surfaces/`; runtime Copilot/document-bootstrap, registry, tests focalizados `13/146`, build y package-lock solo fueron evidencia de la sesión previa. |
| 2026-08-13 | R-07 (consolidación en spec y contexto) | Se consolidó la verdad verificada de R-07 en la spec canónica y el contexto técnico: el conjunto activo de targets quedó en **8** (`opencode`, `claude-code`, `github-copilot`, `vscode`, `cursor`, `antigravity`, `visual-studio-2026`, `codex`); `copilot-vscode` es alias legacy de `github-copilot` y `litellm` fue retirado (sus estados legacy fallan explícitamente). `visual-studio-2026` delega en `.github/copilot-instructions.md` y no genera `.vs/AXIOM.md`; no recibe fichero MCP (schema no verificado). `sync` cubre los 8 targets activos y registra `writtenFiles`; Cursor declara `.cursor/rules/axiom-common.mdc` como superficie potencial no-clobber. El filtro MCP project-bound (`filterProjectBoundMcpServers`) aplica también en worktrees, limpia solo IDs Axiom stale y mantiene Codex/Antigravity cerrados por defecto. Se corrigieron los conteos activos a 42 packages / 8 adapters. En esta sesión se corrigieron además claims activos desactualizados que quedaban en las specs 03, 06 y 08 (menciones de "10 targets"/"9 paquetes", `litellm` como activo y `visual-studio-2026` generando `.vs/AXIOM.md`/`.vs/mcp.json`), marcándolos como retirados o históricos. | Visual Studio MCP sigue sin schema verificado; no se escribe `.vs/mcp.json`. Codex/Antigravity requieren un binding seguro para cualquier futura integración user-global. Queda pendiente de decisión el estado de las carpetas activas `specs/increments/INC-20260809-*` (README sin `metadata.yml`) frente a sus homólogas archivadas en `_archive/`. | Consolidación en `Axiom.Spec/specs/{02_Requisitos_No_Funcionales,03_Modelo_Operativo_y_Datos,05_Interfaces_Operativas,06_Integraciones_y_Capacidades,07_Gobierno_y_Seguridad,08_Glosario}.md` y `Axiom.Spec/context/architecture/{02-modelo-de-datos-y-configuracion,04-adapters-y-model-routing}.md`. En esta sesión se actualizaron `Axiom.Spec/specs/03_Modelo_Operativo_y_Datos.md`, `Axiom.Spec/specs/06_Integraciones_y_Capacidades.md` y `Axiom.Spec/specs/08_Glosario.md` (corrección de claims activos desactualizados). No se modificó código, configuración de producto, tests ni documentación operativa del runtime. |

| 2026-08-13 | R-07 (consolidación adicional) | Se re-verificó la consolidación de R-07 contra el código y se corrigieron **3 claims activos desactualizados** que quedaban en archivos canónicos: (1) `Axiom.Spec/specs/05_Interfaces_Operativas.md` decía "los 10 `ADAPTER_TARGETS`" en el step `adapters` del wizard, pero `ADAPTER_TARGETS` en `apps/cli/src/commands/init.ts` tiene **8** targets; (2) `Axiom.Spec/specs/08_Glosario.md` repetía "los 10 `ADAPTER_TARGETS`" en la entrada "Adapters (multi-select en el wizard)"; (3) `Axiom.Spec/specs/06_Integraciones_y_Capacidades.md` decía "**9** packages `@axiom/adapters-*`" en "Integraciones esperadas", pero el conteo real es **8** (LiteLLM retirado, `copilot-vscode` alias legacy sin package). Los tres se corrigieron a 8. El resto de la consolidación previa (specs 02, 03, 07, contexto architecture 02/04, references 01, `TECHNICAL_CONTEXT.md`) ya reflejaba correctamente 8 targets / 8 adapters / 42 packages, LiteLLM retirado y VS2026 sin `.vs/AXIOM.md`. | Ninguna nueva. Se mantienen las dudas previas: Visual Studio MCP sin schema verificado (no se escribe `.vs/mcp.json`), Codex/Antigravity requieren binding seguro para integración user-global, y el estado de las carpetas activas `specs/increments/INC-20260809-*` frente a sus homólogas archivadas. | En esta sesión se actualizaron `Axiom.Spec/specs/05_Interfaces_Operativas.md`, `Axiom.Spec/specs/08_Glosario.md` y `Axiom.Spec/specs/06_Integraciones_y_Capacidades.md`. Se verificaron sin cambios `Axiom.Spec/specs/{02,03,07}.md`, `Axiom.Spec/context/architecture/{02,04}.md`, `Axiom.Spec/context/references/01-inventario-de-packages.md` y `Axiom.Spec/context/TECHNICAL_CONTEXT.md`. |
| 2026-08-14 | R-07 (análisis de dudas abiertas) | Se registraron 3 acciones de análisis (`ACC-033`, `ACC-034`, `ACC-035`) para las dudas abiertas de R-07 y se ejecutaron los análisis. **Duda 1 (Visual Studio MCP):** el generador `visual-studio-2026` delega en `github-copilot` y no escribe MCP; la duda no se puede resolver sin una instalación real de VS2026 (verificación externa), así que no emerge acción de implementación y se mantiene como investigación externa. **Duda 2 (Codex/Antigravity user-global):** los generadores de `codex` y `antigravity` solo escriben `.codex/AGENTS.md` y `.antigravity/AGENTS.md` (no MCP); no existe ningún mecanismo de binding seguro implementado, solo la nota informativa y el cierre por defecto — emerge **ACC-036** (implementar binding seguro). **Duda 3 (carpetas `INC-20260809-*`):** confirmado que son residuos — `INC-20260809-mcp-project-target-filter` está vacía y las otras cuatro contienen solo un `README.md` con estado `closed`, mientras que sus homólogas en `_archive/` tienen la estructura completa (`metadata.yml`, requisitos, criterios, receipts, candidate-freeze) — emerge **ACC-037** (archivar/limpiar los residuos). | Visual Studio MCP sigue sin schema verificado; la verificación requiere una instalación real de VS2026 (externa). Codex/Antigravity requieren el binding seguro de ACC-036. Las carpetas activas `INC-20260809-*` requieren la limpieza de ACC-037. | En esta sesión se registraron `ACC-033`, `ACC-034`, `ACC-035` (análisis) y `ACC-036`, `ACC-037` (derivadas). No se modificó spec ni contexto técnico; solo el plan. |
| 2026-08-16 | R-09 (consolidación en spec y contexto) | Se consolidó la verdad verificada de R-09 en la spec canónica y el contexto técnico. La mayor parte ya estaba reflejada: `axiom doctor` puramente diagnóstico con `--deep` never-fail (TC-018/TC-019), `axiom audit` read-only vía `auditTrailVerify`, `AuditTrailSink` append-only con sidecar SHA-256 y retención `P365D`, telemetría local con sinks `local-audit-trail`/`local-log`, el panel de telemetría del launcher (ACC-032) y el aprendizaje continuo opt-in (`axiom learn --from-audit`). Se añadieron al contexto de operations `02-doctor-troubleshooting-y-telemetria.md` tres hechos verificados que faltaban: (1) el gate `gateOnTelemetrySink` existe como pre-condición de mutación en `sync`/`configure` pero con `local-only` **siempre pasa** por `local-only-bypass` (hoy no bloquea ninguna mutación); (2) los emisores reales son `tool-routing/events.ts` y `workflow/hooks.ts`; (3) el audit trail se escribe siempre pero su contenido solo se explota por `axiom learn --from-audit` (opt-in) y `axiom context` (bloque `recentLessons`), y los contadores del bus no tienen superficie pública fuera del launcher y los tests. | Ninguna nueva. Se mantienen las dudas previas de R-09: el `--deep` del doctor no se ejecutó en la sesión de auditoría (requiere probes reales de procesos; se verificó por lectura de código que es never-fail), y el check ADO de conectividad solo aplica a proyectos con plugin ADO (en local-only devuelve `null`). | En esta sesión se actualizó `Axiom.Spec/context/operations/02-doctor-troubleshooting-y-telemetria.md`. Se verificaron sin cambios `Axiom.Spec/specs/{04,05,07}.md`, `Axiom.Spec/context/architecture/03-ciclo-de-vida-cli-y-orquestacion.md` y `Axiom.Spec/context/TECHNICAL_CONTEXT.md`. |
| 2026-08-17 | R-10 (investigación ampliada) | Se verificó que el lifecycle de proyecto (`init`, `join`, `configure`, `sync`, `start`, `audit`, `doctor`, `upgrade`) prepara, mantiene y comprueba un proyecto Axiom, mientras que el ciclo SDD real de artefactos sigue en `@axiom/workflow` y los comandos `axiom-increment`, `axiom-bug`, `axiom-plan`, `axiom-role` y `axiom-qa-e2e`. Los 19 intent commands de `@axiom/orchestrator` son stubs sin consumidor CLI; `axiom sdd advance`, `axiom plan` y `axiom role start` aparecen en contratos materializables, pero no existen como comandos. Cavekit es una librería pura, privada y sin consumidor runtime. Se verificaron divergencias reales entre rutas en resolución YAML/default, efectos declarados, confirmación, archive y gate QA, y documentación activa que sobredeclara parte de esos comportamientos. | Decidir si se retiran los intents y contratos de comandos inexistentes o se implementan; retirar o integrar Cavekit; definir la política única de configuración, confirmación, efectos, archive y QA antes de cambiar runtime. ACC-007 conserva evidencia histórica de 2026-08-04, pero no certifica por sí sola el HEAD posterior auditado ni toda la consolidación que declara. No se registraron acciones ACC nuevas: quedan pendientes de decisión expresa. | `Axiom/packages/{orchestrator,workflow,cavekit-discipline}/src`, `Axiom/apps/cli/src/commands/{axiom-increment,axiom-bug,axiom-plan,axiom-role,axiom-qa-e2e,integrate,qa-archive-gate,app-launcher,app-api}.ts`, `Axiom/axiom.config/{workflows,agents-catalog}.yaml`, `Axiom/axiom.spec/target-axiom-{agents,skills}/`, `Axiom.Spec/specs/{04_Flujos_SDD_y_Ciclo_de_Vida,05_Interfaces_Operativas,07_Gobierno_y_Seguridad}.md`, `Axiom.Spec/context/architecture/03-ciclo-de-vida-cli-y-orquestacion.md`, `Axiom.Spec/decisions/{0015,0028}*.md`, `INC-20260804-launcher-sdd-lifecycle`. |

| 2026-08-19 | R-10 (cierre definitivo) | **R-10 queda cerrada.** `INC-20260818-r10-closure-correction` corrigió el cierre prematuro de R-10: dejó ocho targets públicos, rechazó `copilot-vscode` por API/CLI y conservó solo su migración interna desde `init.json`; retiró mocks LiteLLM activos; alineó `configure`, `sync`, Copilot, lifecycle y Cavekit; y consolidó el broker único `axiom-mcp-broker` con `kind=axiom` y protocolo `server/discover`. Tres revisiones independientes sucesivas agotaron los blockers documentales y la terminal concluyó `BLOCKERS: NO`, AC1–AC8 PASS. Core ejecutó `verify` y `archive` con receipts de éxito; metadata final `archived`. | Ninguna bloqueante. Visual Studio MCP y el binding user-global seguro de Codex/Antigravity siguen siendo límites externos/futuros ya documentados, no condiciones de R-10. | Integración estable en `Axiom.Spec/specs/00..08` y `context/**`; evidencia: focal `146/146`, sync `11/11`, MCP complementario `59/59`, build limpio, Vitest `330/330` archivos y `3300/3300` tests, doctor PASS (`45/60`, 0 fallos) y readiness PASS. Freeze final `d3e7a728d17b8abd6048bd4f608aa895554baac6c15f8ba3e958b2c0c49f9ee4`; receipts `increment-verify` e `increment-archive` exitosos. |
| 2026-09-11 | R-14 | Se verificó el mapa de control de operador e interfaces. `axiom model` (`show`/`set`/`unset`/`reset`/`validate`) resuelve por clase de tarea sobre `axiom.config/model-routing-policy.yaml` (4 clases, 9 task classes) y 7 slots del ciclo SDD, persiste overrides en `.axiom-state/<projectKey>/model-assignments.json`, corre `MRC-001..MRC-004` y proyecta a OpenCode; el `SUPPORT_MATRIX` declara `opencode` multi-mode, `claude-code` single-mode y los 6 targets restantes `fallback-only`, es decir la palanca existe siempre pero solo dos destinos la obedecen. `axiom upgrade` es rollback-first (checkpoint previo, migraciones, `ManagedState`, restore automático ante fallo, fan-out multi-repo solo desde el repo de control, `sync`/`doctor` una vez al final) y `axiom rollback` expone el restore a mano fuera del gate de upgrade y sin mutar ante un id inexistente; los checkpoints viven bajo `checkpoints/<id>/` con manifest y retención por defecto de 5. `axiom self-update` es la superficie transaccional Git (`status`/`check`/`plan`/`apply`/`recover`, envelope JSON schemaVersion 1) e independiente de `upgrade`. `axiom app` es la única interfaz guiada tras la retirada de la TUI. Evidencia ejecutada: `29/259` y `6/126` (total 35 archivos / 385 tests PASS). | Cinco hallazgos abiertos, registrados como ACC-081..ACC-085 y no ejecutados: (1) `packages/tui/` sigue trackeado con `package.json`, `tsconfig.json` y el huérfano `src/flows/preview.ts`, enlazado como junction en `node_modules/@axiom/tui` y presente en `package-lock.json`, pese a ACC-005 `validado`; `src/index.ts` sí fue borrado en `c4df64c` y el paquete no está en las project references. (2) Conviven dos generaciones de self-update: `apps/cli/src/commands/self-update.ts` (shim + `install-global.mjs`) sigue en el árbol con 16 tests vivos pero sin registro en `apps/cli/src/index.ts`; solo `registerSelfUpdateContract` está conectado. (3) La API del launcher expone 8 operaciones de self-update, pero el frontend estático no tiene panel de self-update ni de model-routing (paneles reales: ADO suggestions, rama de rol Git, workflow ADO y telemetría). (4) 19 artefactos compilados están versionados dentro de `src/` y 148 bajo `packages/adapters/*/dist/`, porque `.gitignore` solo cubre `packages/*/dist/` y `apps/*/dist/`. (5) `RUNTIME_VERSION` es el literal `'0.1.0'` escrito a mano en `packages/versioning/src/version.ts`, desacoplado de `EMBEDDED_BUILD_METADATA`. No se ejecutó `upgrade` ni `self-update apply` reales: la auditoría no acciona mutaciones. | `Axiom/packages/model-routing/src/{slots,support-matrix,checks,types}.ts`, `Axiom/packages/cli-commands/src/commands/{model,upgrade,rollback}.ts`, `Axiom/packages/versioning/src/{index,checkpoints,managed-state,upgrade,version}.ts`, `Axiom/apps/cli/src/commands/{app,app-api,app-self-update,self-update,self-update-contract,self-update-status}.ts`, `Axiom/apps/cli/src/index.ts`, `Axiom/apps/cli/static/launcher/{index.html,launcher.js,panels.js}`, `Axiom/packages/launcher/src/{action-catalog,adapter-routing,agent-routing,prompt-builder}.ts`, `Axiom/packages/tui/`, `Axiom/axiom.config/model-routing-policy.yaml`, `Axiom/.gitignore`, `Axiom/packages/doctor/src/checks.ts` (TC-009), `Axiom/docs/cli/{model,upgrade,rollback,self-update,support-matrix,tui}.md`, suites `packages/{model-routing,versioning,launcher,core,user-workspace}/tests/` y `apps/cli/tests/{model,upgrade,upgrade-fanout,rollback,self-update*,app-self-update,tui-retirement*}.test.ts`. |
| 2026-09-11 | R-14 (definición del lote ACC-081..ACC-086) | Se registró `ACC-086` con la decisión expresa del usuario sobre destinos que no enrutan modelo: mostrar el modelo recomendado, declarar la selección como manual y avisar al copiar el prompt según el adapter, leyendo el nivel de soporte del `SUPPORT_MATRIX`. Se definió el lote en siete incrementos secuenciales con una fase 0 de decisiones (`D-01` self-update superviviente, `D-02` destino de los `dist/` de adapters frente a `TC-009`, `D-03` fuente única de versión de runtime, `D-04` contrato de recomendación y aviso, `D-05` barrido previo a la retirada de la TUI). Se creó mediante Core el plan `PLAN-INC-20260911-r14-operator-control-surfaces` y los dos primeros incrementos, `INC-20260911-r14-tui-package-removal` e `INC-20260911-r14-operator-decisions-baseline`, ambos en `specifying`, con contenido redactado y enlazados al plan con `axiom-increment link-plan`. No se implementó nada de producto. | Hallazgo verificado en esta sesión y no cubierto por ninguna acción: el workflow `increment` admite **una sola instancia en vuelo** y no existe comando soportado para reasignarla. `workflow-state.json` guarda un único registro por workflow ligado a un `metadataId`; el wrapper CLI rechaza todo subcomando distinto de `create` cuando el id no coincide y el estado no es terminal (`ID mismatch: se recibió 'A', pero workflow-state.json está ligado a 'B'`); `create` solo es legal desde `draft`, por lo que re-ligar un artefacto que ya está en `specifying` se rechaza (`No transition for command 'increment-create' from state 'specifying'`); y `axiom state` es únicamente un inspector read-only. Consecuencia: crear los siete incrementos por adelantado dejaría ligado al último creado y bloquearía a los anteriores, de modo que los incrementos 3 a 7 se crean en su turno. El orden resultante empieza por la retirada del paquete de la TUI, que es el único trabajo del lote sin dependencia de decisiones. No se registró acción para esta restricción; queda a la espera de petición expresa. | `Axiom.Spec/specs/plans/PLAN-INC-20260911-r14-operator-control-surfaces/`, `Axiom.Spec/specs/increments/INC-20260911-r14-tui-package-removal/`, `Axiom.Spec/specs/increments/INC-20260911-r14-operator-decisions-baseline/`, `Axiom.Spec/.axiom-state/Axiom.Spec/workflow-state.json`, `Axiom/axiom.config/workflows.yaml`, `Axiom/apps/cli/src/commands/{axiom-increment,scaffold,state-cmd}.ts`, `Axiom/packages/workflow/src/governed-transition-runner.ts`, `Axiom/packages/model-routing/src/support-matrix.ts`, `Axiom/packages/launcher/src/{prompt-builder,adapter-routing}.ts`, `Axiom/packages/doctor/src/checks.ts` (TC-009). |
| 2026-09-11 | R-14 (lote completo definido y desbloqueado) | Se registró `ACC-087` para corregir el bloqueo por instancia única del estado de workflow, a petición expresa del usuario. El lote quedó definido en **ocho incrementos**, todos creados con Core, en `specifying` y enlazados a `PLAN-INC-20260911-r14-operator-control-surfaces` con `axiom-increment link-plan`: `workflow-instance-selection` (ACC-087), `operator-decisions-baseline` (fase 0), `tui-package-removal` (ACC-081), `build-artifact-hygiene` (ACC-084), `single-self-update-surface` (ACC-083), `runtime-version-identity` (ACC-085), `model-recommendation-notice` (ACC-086) y `launcher-operator-panels` (ACC-082). La restricción de instancia única se usó como palanca de arranque: `workflow-instance-selection` se creó en último lugar para quedar ligado en `workflow-state.json` y ser el único que puede avanzar, y es precisamente el que elimina la limitación. Verificado tras la creación: el record de `increment` apunta a `INC-20260911-r14-workflow-instance-selection` en `specifying`; `axiom index validate` devolvió OK con 17 metadata escaneados antes de los seis últimos y se revalidó al cierre. El plan documenta fase 0 con `D-01`..`D-05`, secuencia con dependencias, restricción y su neutralización, estrategia E2E por incremento, riesgos y gates. No se implementó nada de producto ni se tocó el repositorio `Axiom`. | Ninguna nueva. Las dudas propias de `ACC-087` (forma de la selección, historia de instancias terminales y si `bug` y `plan` migran en el mismo incremento) se resuelven dentro de su incremento porque no existe forma de precederlo, y no condicionan a los otros siete. Riesgo asumido y documentado: el incremento 1 toca el estado de todos los workflows de artefacto, así que una regresión ahí afectaría al ciclo SDD completo; si se alarga, el bypass por estado terminal permite continuar el lote en modo secuencial. | `Axiom.Spec/specs/plans/PLAN-INC-20260911-r14-operator-control-surfaces/README.md`, los ocho `Axiom.Spec/specs/increments/INC-20260911-r14-*/`, `Axiom.Spec/.axiom-state/Axiom.Spec/workflow-state.json`, acciones `ACC-081`..`ACC-087`. |
| 2026-09-11 | R-14 (ejecución y cierre de lote ACC-081..ACC-087) | **R-14 queda cerrada.** Se ejecutaron y archivaron los ocho incrementos del lote conforme al orden de gobernanza: (1) `INC-20260911-r14-workflow-instance-selection` (ACC-087) migró `workflow-state.json` a `schemaVersion: 2`, habilitó selección explícita mediante `--id` y subcomando `select`, y desbloqueó el lote; (2) `INC-20260911-r14-operator-decisions-baseline` formalizó las decisiones D-01 a D-06 como artefactos `decision` enlazados al plan; (3) `INC-20260911-r14-tui-package-removal` (ACC-081) eliminó físicamente `packages/tui/`, junction en `node_modules` y saneó `package-lock.json`; (4) `INC-20260911-r14-build-artifact-hygiene` (ACC-084) desversionó los 19 artefactos compilados bajo `src/` y 148 bajo `packages/adapters/*/dist/`, extendió `.gitignore`, ajustó `TC-009` con honestidad diagnóstica (WARN ante falta de build) y añadió gate anti-regresión; (5) `INC-20260911-r14-single-self-update-surface` (ACC-083) eliminó `self-update.ts` legacy y consolidó `self-update-contract.ts` como superficie única; (6) `INC-20260911-r14-runtime-version-identity` (ACC-085) derivó la identidad de runtime desde los metadatos de build en `@axiom/versioning/src/generated-version.ts` eliminando el literal manual; (7) `INC-20260911-r14-model-recommendation-notice` (ACC-086) implementó el cálculo de clase recomendada y aviso dependiente del adapter en `craftPrompt` y en `axiom model show`; (8) `INC-20260911-r14-launcher-operator-panels` (ACC-082) añadió al launcher las pantallas de Model Routing y Actualización con sus respectivos endpoints en `app-api.ts`. Los 8 incrementos completaron su ciclo Core (`refine` → `specify` → `plan` → `plan-approve` → `freeze` → `verify` → `archive`) con receipts emitidos y auditados. | Ninguna bloqueante. Cobertura completa de los 8 incrementos. | Reconciliación en `specs/00..08` y `context/**`. Validación global observada: build limpio, doctor PASS (48/61 OK, 0 fallos), readiness PASS, 51 tests focales de workflow PASS, 42 tests de versioning PASS, 72 tests de launcher PASS, 15 tests de app-self-update PASS, `git diff --check` limpio y `axiom index validate` OK (21 metadatas válidos). |
| 2026-09-12 | R-15 | **R-15 queda cerrada.** Se mapeó la zona documental y se verificó su estado. `apps/cli/src/index.ts` registra 47 familias de comandos y `Axiom/docs/**` (42 archivos) documenta unas 13; el otro set, `Axiom.Spec/specs/manuales/**` (15 archivos), cubre el ciclo SDD con otro dueño y otro estilo. Ninguna prueba, check de doctor ni script valida los manuales del runtime: la única prosa con gate real es el material de catálogo, con 20 skills y 14 agentes cuyas 34 entradas de `skills-catalog.yaml` y `agents-catalog.yaml` existen en disco y son verificadas por `TC-010`/`TC-011`. Las plantillas están triplicadas: 45 en `Axiom.Spec/templates/`, 45 homónimas en `Axiom/axiom.spec/templates/` con siete divergentes en contenido, y copias bundleadas en código con un test dorado que solo compara títulos de sección; la copia declarada canónica es la desactualizada, con vocabulario retirado en R-04. `Axiom/README.md` mezcla foto histórica y presente, enlaza a `openspec/` inexistente y describe `dist/` materializado tras la desversión de ACC-084. Cuatro manuales de `docs/configuration/files/` describen YAML no materializados y cinco YAML reales no tienen manual. El repositorio canónico mantiene dos formatos de decisiones, raíces duplicadas `increments/` y `bugs/`, y una carpeta `axiom.spec/` vacía. Se verificó además que el cierre documental hoy es prosa en `axiom-role-close-doc` y `axiom-spec-integrator`, mientras `axiom integrate` solo fija `status: archived` e `integration.status: 'integrated'`, mueve la carpeta y emite receipts, sin comprobar documentación. | Decisiones tomadas el 2026-09-12: la fuente única de plantillas es la copia del runtime; el manual único es el del runtime y el conjunto canónico se deja expirar con `Axiom.Spec` sin fusionarse; la copia suelta de ACC-038 se borra; y la petición de manual instalable se separa en ACC-095. Nomenclatura registrada como duda sin acción: conviven `axiom.spec/` como carpeta dentro de un proyecto, default histórico de proyección de artefactos y marcador de layout `self-hosted`, `<projectName>.axiom` como repositorio autoral que crea la adopción, y `<projectName>.spec` como convención anterior que dio nombre a `Axiom.Spec`. La revisión de plantillas sobrantes se aplaza a la instalación de Axiom sobre Axiom. Duda abierta sin acción registrada: al archivar quedan carpetas residuales en la raíz activa. Las 23 carpetas de `specs/increments/` que hoy parecen activas no tienen `metadata.yml` y todas tienen su gemela en `_archive/`; en `INC-20260911-r14-tui-package-removal` el receipt de archive es de 2026-09-12 00:42:20 y los cinco documentos que quedaron en la ruta activa fueron reescritos a las 02:24:09, casi dos horas después, con `metadata.yml`, `receipts/` y `context/` ya movidos y el resultado del archivado todavía sin commitear. No se estableció la causa de esa reescritura posterior. Sobre la nomenclatura, el desfase es cronológico y no un residuo de instalación fallida: el contexto canónico ya documenta que `Axiom/` se auto-adoptó en `INC-20260708-product-repo-self-bootstrap` con la convención de entonces, mientras el repositorio autoral `<projectName>.axiom` llegó después con `INC-20260724-adopt-creates-axiom-repo`. No se integró conocimiento estable nuevo en `specs/**` ni en `context/**`: todo lo hallado es trabajo pendiente, y la distinción entre `Axiom.Spec/` y `axiom.spec/` ya está consolidada en `context/TECHNICAL_CONTEXT.md`. Acciones registradas: ACC-088 a ACC-095, todas en estado propuesto y sin ejecutar. Siguiente área recomendada: R-16, pruebas, coherencia y cierre del inventario. No se ejecutó build ni suites en esta sesión: la auditoría fue de lectura y la zona no tiene comando de validación documental. | `Axiom/README.md`, `Axiom/AGENTS.md`, `Axiom/docs/**`, `Axiom/axiom.config/*.yaml`, `Axiom/axiom.spec/{templates,target-axiom-skills,target-axiom-agents}/`, `Axiom/apps/cli/src/index.ts`, `Axiom/apps/cli/src/commands/integrate.ts`, `Axiom/packages/cli-commands/src/commands/{validate-changes,workspace-adapter-templates}.ts`, `Axiom/packages/workflow/src/artifact-skeleton.ts`, `Axiom/packages/adapters/*/src/generator.ts`, `Axiom/packages/config-validation/src/{schemas,validator}.ts`, `Axiom/packages/doctor/src/checks.ts`, `Axiom/axiom.config/workflows.yaml`, `Axiom.Spec/README.md`, `Axiom.Spec/specs/README.md`, `Axiom.Spec/specs/manuales/**`, `Axiom.Spec/specs/{decisions,adr,archive}/`, `Axiom.Spec/{decisions,increments,bugs,templates,context,technical-context}/`. |
| 2026-09-12 | R-15 (definición del lote) | Se definió el lote de ejecución de R-15 con comandos de Axiom: el plan `PLAN-INC-20260912-r15-documentation-single-source` (`draft`, `targetRepos: axiom-code-builder`, `taskType: documentation-and-governance`) y seis incrementos creados en `specifying` y enlazados a él. Agrupación: (1) `INC-20260912-r15-docs-truth-baseline` = ACC-091 + ACC-092 + ACC-093 más la decisión `D-01` de estructura del manual; (2) `INC-20260912-r15-manual-command-coverage` = ACC-089 con prueba de cobertura derivada del programa Commander; (3) `INC-20260912-r15-template-single-source` = ACC-090; (4) `INC-20260912-r15-canonical-roots-dedup` = ACC-094; (5) `INC-20260912-r15-docs-closure-verification` = ACC-088; (6) `INC-20260912-r15-manual-distribution` = ACC-095. El orden coloca el gate documental en quinta posición de forma deliberada: instalado antes, bloquearía el cierre de los propios incrementos que corrigen la documentación. La instancia activa quedó seleccionada en el primer incremento. | `axiom-increment list` y `axiom index validate` no completaron dentro del tiempo disponible en esta sesión; no se pudo determinar si es lentitud por el volumen del repositorio o un bloqueo. La verificación se hizo sobre disco y sobre `workflow-state.json`: seis instancias registradas, `activeInstanceId` en el primer incremento, `links.planId` correcto en los seis y ningún placeholder sin rellenar. Las acciones siguen en estado `propuesto` hasta su ejecución. | `Axiom.Spec/specs/plans/PLAN-INC-20260912-r15-documentation-single-source/`, `Axiom.Spec/specs/increments/INC-20260912-r15-*/`, `Axiom.Spec/.axiom-state/Axiom.Spec/workflow-state.json`, comandos `axiom axiom-plan create`, `axiom axiom-increment create`, `link-plan`, `link-increment` y `select`. |
| 2026-09-13 | R-16 | **R-16 queda cerrada.** Se ejecutó la validación completa del runtime en el HEAD `a51262a`: suite Vitest completa **357/358 archivos, 3764/3765 tests** (500 s) con un único fallo; `doctor` PASS **48/61 OK, 0 fallos, 2 advertencias** (BC-001 sin scope documentacional, CC-004 con 3 capabilities opcionales sin provider); `readiness:first-project` PASS; scripts `node --test` en verde (instalador 18/18, release-validator 8/8, generadores 3/3, self-update-process 1/1); gate documental `docs-command-coverage` 5/5. Cobertura: 358 archivos de test (123 en `apps/cli/tests`), todos los packages con `src` tienen tests salvo `cli-commands` (cubierto desde `apps/cli/tests`); los 8 adapters tienen entre 1 y 3 tests propios. **Hallazgo principal:** el único fallo de la suite es `artifact-hygiene.test.ts` — los **148 archivos bajo `packages/adapters/*/dist/` siguen trackeados en Git** pese a que `INC-20260911-r14-build-artifact-hygiene` (ACC-084) y el cierre de R-14 registran su desversión. Verificado con `git ls-files` (148 entradas, `H` en el índice), reflog (ningún commit los retiró) y los commits del 13-09: `95cab5e` retiró los 19 artefactos bajo `src/` y el paquete TUI pero no tocó `dist/`; `a51262a` contiene los fixes de los grupos A/B/C/D/E/G del plan de baseline pero **no** el Grupo F (`git rm -r --cached`). `PLAN-BASELINE-TESTS-20260913.md` declara cierre con 358/358 en verde, lo que no se reproduce en el HEAD actual: la afirmación de cierre es prematura respecto al Grupo F. Inventario de artefactos: 29 carpetas de incremento activas sin `metadata.yml` (residuos tipo ACC-037: 6 con solo `review-ledger.md`, 1 vacía, 4 con solo `README.md`, 8 de R-14/R-15 con documentos pero sin metadata, resto mixto); 8 bugs `BUG-20260910-baseline-*` en `specifying` con estructura completa, cuya baseline de origen ya no se reproduce (el plan del 13-09 los declara ajustados); `workflow-state.json` en schema 2 con instancias archivadas. Acciones `propuesto` restantes: 9 de R-07, 7 de R-10, 13 de R-13 y 4 de R-15 (ACC-091..093, ACC-095). | (1) El Grupo F del plan de baseline no llegó al HEAD: decidir si se commitea la desversión de los 148 `dist/` o se corrige el registro del plan; hasta entonces la suite completa no puede declararse verde. (2) Los 8 bugs baseline en `specifying` necesitan decisión: cerrarlos como no reproducibles o ejecutarlos. (3) `docs-command-coverage.test.ts` tarda 310 s en un solo archivo (5 tests): candidato a optimización, no bloqueante. (4) Las dudas externas previas se mantienen (Visual Studio MCP, binding user-global Codex/Antigravity). | `Axiom/vitest.config.ts`, `Axiom/package.json`, `Axiom/scripts/*.mjs`, `Axiom/apps/cli/tests/artifact-hygiene.test.ts`, `Axiom/apps/cli/tests/docs-command-coverage.test.ts`, `Axiom/packages/adapters/*/dist/` (índice Git), `Axiom.Spec/plans/PLAN-BASELINE-TESTS-20260913.md`, `Axiom.Spec/specs/increments/` (activos y `_archive/`), `Axiom.Spec/specs/bugs/`, `Axiom.Spec/.axiom-state/Axiom.Spec/workflow-state.json`, commits `95cab5e` y `a51262a`. |
| 2026-09-13 | R-16 (remediación y cierre de dudas) | Se ejecutaron las dos remediaciones pendientes del cierre de R-16. **Grupo F:** `git rm -r --cached` de los 8 `dist/` de adapters (148 archivos des-trackeados, intactos en disco, ignorados por `.gitignore`), commiteado en `Axiom` como `3ac7305`; `git ls-files` devuelve 0 y el árbol queda limpio. **Bugs baseline:** los 8 `BUG-20260910-baseline-*` se cerraron por el camino gobernado (`fix-plan` → `verify` → `archive` con `--confirm`, ejecutado desde el repo de spec) y quedaron físicamente archivados en `specs/bugs/_archive/`; `axiom-bug list` ya no los registra. **Certificación final de la suite:** dos pasadas completas con interferencia de carga produjeron solo timeouts (3 y 5 archivos; todos pasan aislados: 86/86 y 62/62 + 44/44); la pasada limpia con `--minWorkers=1 --maxWorkers=4` certificó **358/358 archivos, 3765/3765 tests, matriz ACC-076 35/35, exit 0** (898 s). El plan de baseline quedó corregido con la nota de cierre real. | La suite completa sigue siendo sensible a la carga paralela (timeouts de 30/120 s bajo contención); la política vigente es pasada limpia o workers limitados para certificar. Los 29 residuos de incrementos activos sin `metadata.yml` siguen sin decisión (tipo ACC-037). Las 33 acciones `propuesto` se contrastaron contra el código: la mayoría de sus objetivos ya están implementados por vías distintas (ver detalle en la conversación); requieren decisión de cierre o redefinición, no ejecución literal. | `Axiom` commit `3ac7305`, `Axiom.Spec/specs/bugs/_archive/BUG-20260910-*` (8), `Axiom.Spec/plans/PLAN-BASELINE-TESTS-20260913.md` (nota de corrección y certificación), logs de suite en `%TEMP%\vitest-full*.log`. |
| 2026-09-14 | R-16 (reconciliación del registro de acciones y ACC-096) | Se reconciliaron las 33 filas `propuesto` del registro de acciones contra el código real, lote por lote. **Pasan a `validado` (28):** ACC-025/026/027/028 (R-07, ejecutadas por los incrementos `INC-20260809-*` archivados y el cierre de R-10), ACC-033/034/035 (análisis completados en la sesión R-07 del 2026-08-14), ACC-039/041/042/043/044 (R-10, verificadas contra `governed-transition-runner.ts`, workflows.yaml y la ausencia del gate QA duplicado), ACC-057..067 y ACC-069 (R-13, verificadas contra `topology/src/validate.ts` con 98 invariantes, loader con lock+atomic write, `workspace-structural-plan/transaction.ts`, markers `AXIOM:MANAGED` y el loader estricto de workspace.json), y ACC-091/092/093/095 (R-15, verificadas contra el árbol: 0 menciones `openspec`, manuales históricos marcados, 5 YAML con manual nuevo, `profiles-overlays-targets.md` renombrado, `distributeManual` en workspace-setup). **Siguen `propuesto` (5):** ACC-036 (binding seguro Codex/Antigravity, no implementado), ACC-037 (5 carpetas residuales `INC-20260809-*` sin limpiar), ACC-040 (Cavekit sigue en el árbol como residuo inerte sin project references), ACC-045 (sigue existiendo `DEFAULT_WORKFLOWS` manual paralelo a `workflows.yaml`), ACC-068 (parcial: `workspace adapters` aún no repara process surfaces). Se registró **ACC-096** (rendimiento de tests) a petición expresa del usuario, con la evidencia medida de la pasada certificada: los tres peores archivos concentran 1352 s (`self-update` 579 s, `workspace-incremental` 498 s, `docs-command-coverage` 275 s en solo 5 tests). | La reconciliación se basa en lectura de código y configuración, no en ejecución de suites por lote; los estados `validado` reflejan que el objetivo de cada acción está implementado y verificado en el árbol actual, con la referencia al incremento archivado que lo ejecutó cuando existe. ACC-096 queda pendiente de ejecución como incremento propio. | `Axiom/packages/topology/src/{validate,loader}.ts`, `Axiom/packages/workflow/src/{governed-transition-runner,default-workflows}.ts`, `Axiom/apps/cli/src/commands/{workspace-structural-plan,workspace-structural-transaction,workspace-axiom-yaml,workspace-setup,workspace-incremental,repo,topology}.ts`, `Axiom/packages/user-workspace/src/workspace-state.ts`, `Axiom/packages/document-bootstrap/src/manual-distribution.ts`, `Axiom/packages/cavekit-discipline/`, `Axiom.Spec/specs/increments/INC-20260809-*` (activos), logs `%TEMP%\vitest-full3.log`. |

## Registro inicial de acciones (snapshot histórico del 2026-09-12)

La tabla siguiente conserva la fotografía de la auditoría que originó R-15.
Sus estados y evidencias describen el estado previo a la ejecución del lote y
no son el estado actual; el cierre consolidado de R-15 que aparece más abajo es
la referencia vigente para `ACC-088..ACC-095`.

Cada entrada representa una petición explícita del usuario. Una propuesta no autoriza cambios en el código ni en la estructura del repositorio.

| ID | Área | Tipo | Estado | Propuesta | Evidencia o motivo | Dependencias |
| --- | --- | --- | --- | --- | --- | --- |
| ACC-001 | R-00 | modificar | validado | Crear `Axiom.Spec/decisions/` y mover ahí los ADR que estaban almacenados en `Axiom/docs/` (`0015-cavekit-discipline-post-mvp.md`, `0019-operator-control-plane-runtime.md`, `0026-integration-hardening-and-target-parity.md`, `0027-toolchain-provider-expansion-and-repair.md`, `0028-workflow-ux-and-archive-safety-completion.md`, `0029-memory-recall-and-context-repair.md`, `0030-operator-app-plugins-and-external-bridge.md`, `0031-adr-cmm-replaces-graphify-and-codegraph.md`). | `Axiom.Spec/decisions/` contiene los ocho ADR byte-a-byte; los ocho orígenes fueron eliminados después de verificar igualdad y SHA-256; las referencias activas usan el destino canónico. La secuencia final de freeze y apply quedó probada con receipts válidos. | Resuelta: ACC-002 se ejecutó después de poblar `decisions/`; el barrido posterior confirmó que las rutas antiguas no aparecen como referencias activas. |
| ACC-002 | R-00 | documentar | validado | Actualizar `AGENTS.md` raíz del workspace, `Axiom.SDD/AGENTS.md` y `Axiom.Spec/README.md` para que reflejen la estructura real de `Axiom.Spec` (incluir `decisions/` una vez creada en ACC-001, y las carpetas reales `bugs/`, `increments/`, `plans/`, `templates/`, `technical-context/` que hoy no se mencionan en el README). | Los tres documentos describen ahora `specs/`, `context/`, `bugs/`, `increments/`, `technical-context/`, `plans/`, `templates/`, `prompts/` y `decisions/`, distinguiendo raíces top-level existentes de las specs canónicas bajo `specs/`. | Resuelta después de ACC-001; la estructura documentada y la ruta canónica numerada fueron revalidadas. |
| ACC-003 | R-00 | investigar | validado | Analizar el contenido y propósito real de `Axiom/axiom.spec/` (contiene hoy `increments/`, `plans/`, `target-axiom-agents/`, `target-axiom-skills/`, `templates/`) para decidir si su contenido es útil (y en tal caso moverlo/reintegrarlo donde corresponda o eliminarlo si no aporta valor) o si es infraestructura legítima de runtime/instalación de Axiom que debe renombrarse para no confundirse con el repo sibling `Axiom.Spec/`. | ADR-0032 fija `Axiom.Spec/` como repo canónico y `Axiom/axiom.spec/` como baseline product-owned materializable; inventario, consumidores activos y `specRepo.ref: ../Axiom.Spec` verifican que debe conservarse en su ubicación actual. | Resuelta: la investigación y la revalidación de consumidores confirman conservar la ubicación actual y distinguirla de `Axiom.Spec/`. |
| ACC-004 | R-01 | modificar | validado | Normalizar la compilación de `@axiom/cli-commands` para que su API pública se emita en la ruta convencional `packages/cli-commands/dist/index.js` y `dist/index.d.ts`, eliminando la dependencia de `rootDir: "../.."` y de salidas bajo `dist/apps/` o `dist/packages/`. Para conseguirlo habrá que reorganizar o separar bajo la raíz del paquete la implementación compartida que hoy se selecciona desde `apps/cli/src/commands/`, actualizar `main`, `types`, referencias, aliases, consumidores (`apps/cli`, `@axiom/tui`, `@axiom/mcp-tools`), pruebas y documentación. Como trabajo relacionado, revisar y normalizar la salida de `@axiom/tui` si se mantiene el mismo criterio de empaquetado. | Validado en `INC-20260804-cli-commands-package-output`: 14 módulos trasladados al owner físico, entrypoints convencionales, build/runtime y suite de consumidores verdes; la salida histórica TUI quedó reconciliada antes de ACC-005. | Dependencia completada antes de ACC-006/007/008 y ACC-005; build, runtime, readiness, tests y review independiente documentados en el README y receipt `verify`. |
| ACC-005 | R-14 | eliminar | validado | Retirar la TUI como superficie operativa pública: quitar el registro de `axiom tui`, eliminar `@axiom/tui` y su driver, pantallas, flujos, pruebas y documentación dedicada cuando la paridad del launcher esté demostrada, y limpiar referencias, aliases, dependencias, project references y textos históricos. Mantener los comandos CLI/Core y los `run*` compartidos que sigan siendo usados por la CLI o el launcher; retirar solo la interfaz TUI y no la lógica de negocio subyacente. | Validado en `INC-20260804-tui-retirement`: paridad completa sin capacidad exclusiva, registro/dependencia retirados, dist stale limpiado, regresiones directas de `axiom tui` y ausencia de subcomando, build/doctor/readiness y 23 archivos/221 tests verdes. | Depende de ACC-006 y ACC-007; ejecutado al final del lote, después de ACC-004 y con ACC-008 integrado, preservando runners compartidos y marcando la historia TUI como histórica. |
| ACC-006 | R-13 | modificar | validado | Convertir el launcher servido por `axiom app` y distribuido con la CLI en la única interfaz guiada de Axiom. Debe leer el registry, los ficheros de configuración, specs, planes y estado real, y lanzar los comandos canónicos de Axiom con preview, confirmación y resultado visible, sin duplicar la lógica de negocio. La superficie debe cubrir instalación en proyecto nuevo, unión a proyecto existente, selección de proyecto, configuración, workspace setup, adopción/migración de spec y contexto técnico, y creación desde cero. | Validado en `INC-20260804-launcher-onboarding-migration`: setup/adopt, preview/dry-run, ownership/overlap guards, partial success, provenance, registry/doctor refresh e idempotencia sobre primitives canónicos; 7 archivos/90 tests focalizados verdes. | Depende de ACC-004 y de `workspace setup/adopt`, `bootstrap from-legacy-sdd`, `bootstrap from-context`, `user-workspace` y `project-resolution`; habilita ACC-007 y precede ACC-005. |
| ACC-007 | R-10 | modificar | validado | Completar la superficie del launcher para el ciclo SDD: crear y listar incrementos y bugs abiertos, crear y aprobar planes, iniciar y completar implementaciones por rol, ejecutar validaciones, archivar artefactos y mostrar relaciones entre spec, plan, roles, repositorios, implementación y estado. Cada operación debe leer el estado de ficheros existente y delegar en los comandos/workflows canónicos con preview y confirmación cuando haya mutación. | Validado en `INC-20260804-launcher-sdd-lifecycle`: registry aditivo con provenance, acciones canónicas, gates preview/confirm, worktree async, validate/archive y rollback con `inconsistent`; 12 archivos/104 tests focalizados y E2E launcher verdes. | Depende de ACC-006 y del workflow real; precedió ACC-008 y se integró antes de ACC-005. `increment-change` y `plan-archive` permanecen honestamente no ejecutables donde no existe wrapper canónico. |
| ACC-008 | R-12 | modificar | validado | Integrar plugins externos en el launcher mediante un contrato declarativo y seguro: descubrirlos, mostrar sus acciones, distinguir lectura de mutación local/externa, generar preview, exigir confirmación y delegar en comandos o scripts permitidos. Azure DevOps debe usar las rutas existentes de `tracker`/`tracker-ado` y `external-sync`, conservando credenciales fuera de la UI y mostrando resultados, errores y enlaces externos. No se debe habilitar ejecución arbitraria de shell por una entrada de plugin. | Validado en `INC-20260804-launcher-safe-plugins`: schema/discovery tolerante, DTO explícita, handler allowlisted, validación de campos, redacción de secretos/URLs, NullTracker local-only y gates de confirmación; 20 archivos/178 tests y E2E verdes. | Depende de ACC-006 y ACC-007; ejecutado después de ambos y antes de ACC-005, con `tracker`/`tracker-ado`/`external-sync` existentes y sin shell arbitrario. |

| ACC-014 | R-02 | documentar | validado | Corregir el comentario antiguo de `@axiom/cli-commands` para que describa la exportación actual de `runX` y `registerX`. | El barrel exporta wrappers `registerX` para que `apps/cli/src/index.ts` registre los comandos de Commander; la corrección elimina la contradicción documental sin cambiar comportamiento. | Ninguna. |
| ACC-015 | R-03 | documentar | validado | Corregir la documentación de `axiom init` y de los manuales que describen su scaffold para que coincidan con el código actual. | `init` ya no escribe `axiom.config/topology.yaml` ni crea el repo hermano; sí escribe `AGENTS.md` best-effort, persiste `init.json`, acepta `--role`, aliases de profile y `--no-register`, y registra el proyecto user-level de forma best-effort. Se actualizaron manuales de CLI, instalación, archivos generados, configuración, uso y overview; `git diff --check` y las assertions documentales pasan. | Ninguna. |
| ACC-016 | R-03 | investigar | validado | Analizar si el overlay `enterprise` tiene funcionalidad real antes de eliminarlo. | Se verificaron `configure`, el gate de `sync`, `start`, `gateway start/status`, `doctor`, el loader de telemetría y `routeTool`. El modo tiene comportamiento probado, pero acotado a política, snapshot local y routing declarativo; no hay daemon ni broker remoto. Se conserva provisionalmente para decidir su destino con el usuario. | Ninguna. |
| ACC-017 | R-04 | eliminar | validado | Eliminar `product-owner` como perfil funcional seleccionable y fijar `builder` como la única configuración funcional del producto. Retirar los aliases de perfil `analista`/`arquitecto`, el flag `--profile` y los selectores equivalentes de `init`, workspace y launcher; dejar el perfil builder materializado de forma implícita, no como una opción que el usuario deba elegir. Simplificar las restricciones derivadas del eje funcional para que todos los usuarios compartan las capabilities, providers, alcance operativo y roles de implementación de builder. Mantener separados los roles de equipo/código (`backend`, `frontend`, `qa-e2e`, etc.) y las fases o responsabilidades del workflow: no son el perfil funcional que se está retirando. Leer o migrar de forma compatible estados existentes que contengan `functionalProfile`, sin conservar product-owner como opción activa. | Petición expresa del usuario: `builder` será la única configuración, sin presentarlo como “principal” ni pedir elegir otro perfil. Hoy el eje funcional decide `enabledCapabilities`, `preferredProviders`, `defaultMutationScope`, `activatesImplementationRoles` y `degradedModeBlocks`; por tanto hay que retirar el contrato de `product-owner`, no solo ocultar el selector. | Coordinar con ACC-018 porque ambos forman parte del profile triple; definir compatibilidad para `init.json`, `install-profile.json`, `workspace.json`, registros y proyectos ya creados; conservar las descripciones históricas solo en artefactos archivados. |
| ACC-018 | R-04 | eliminar | validado | Eliminar `standard`, `enterprise` y todo el eje seleccionable de overlays, dejando `local-only` como la única política efectiva e implícita para todos los proyectos, sin llamarla “principal” ni ofrecer otra modalidad. Retirar del código y de la documentación el provider `axiom-gateway`, los modos `gateway`/`gateway-first`, `axiom gateway start/status`, `gateway-state.json`, el check `GW-001`, las flags `--gateway`/`--no-gateway`, las expectativas de gateway y las diferencias de riesgo/permisos por overlay. Retirar también `generated-snapshots` del registry, tipos, fallbacks, router, doctor, providers, tests y documentación, sustituyendo cualquier referencia activa por comportamiento local explícito. Mantener `axiom-mcp-broker`, `sdd-mcp-server` y `spec-mcp-broker` porque son servidores MCP reales e independientes del gateway. No eliminar el backend JSON local de memoria. | Petición expresa del usuario: no habrá tipos `enterprise`, `standard` ni `gateway`, y `generated-snapshots` tampoco debe permanecer como contrato declarativo sin implementación. La implementación actual de gateway es declarativa/incompleta: `axiom-gateway` solo declara `sdd.commandResolution` y no tiene `ProviderClient` ejecutable; la auditoría tampoco encontró un generador o cliente ejecutable de `generated-snapshots`. | Coordinar con ACC-017 por la eliminación del triple; conservar el audit trail local y decidir su retención común; auditar todas las referencias de `gateway`, `standard`, `enterprise`, `gateway-state.json` y `generated-snapshots` en código, tests, configuración y docs; verificar que el backend JSON de memoria y el broker MCP no dependan de esos identificadores. |
| ACC-019 | R-04 | documentar | validado | Reconciliar toda la documentación activa con la forma única del producto: `builder` implícito, política local-only implícita, sin `product-owner`, aliases, `standard`, `enterprise`, gateway ni selección de overlay. Corregir además los hechos ya detectados: `config-validation` acepta `structural-code-intel`, `installProfile` solo cae al default ante ausencia de `profiles.yaml`, el modelo separa 16 capabilities provider-routed de 3 MCP-only y `CC-004` informa capabilities huérfanas requeridas/opcionales. Explicar el audit trail como parte permanente de la operación local y conservar las referencias retiradas únicamente como historia explícita en archivos archivados. | La documentación operativa y varias specs activas todavía presentan el profile triple, overlays y gateway como superficies vigentes. La documentación debe cambiar después de que el código y la configuración se hayan simplificado para no describir una forma intermedia. | Depende de ACC-017, ACC-018 y ACC-020; usar como evidencia el código, configuración y pruebas focalizadas de `config-validation`, `capability-model`, `installer`, `doctor`, `tool-routing`, `sync`, telemetría y MCP. |
| ACC-020 | R-09/R-11 | modificar | validado | Mantener el audit trail como capacidad transversal y no opcional: conservar el `AuditTrailSink`, su log append-only, sidecar de integridad, retención local y `axiom audit`; dejarlo habilitado por defecto para la única política local y asegurar que las mutaciones sigan dejando trazas aunque ya no exista un overlay enterprise. Mantener intactos los servidores y handlers MCP reales (`axiom-mcp-broker`, `sdd-mcp-server`, `spec-mcp-broker`) y sus pruebas; retirar solo referencias que los presenten como gateway enterprise. | Petición expresa del usuario: toda traza es útil para evolución futura y auditar no estorba; el MCP se conserva. El `AuditTrailSink` y el broker MCP tienen implementación y pruebas reales, a diferencia de `axiom-gateway`. | Coordinar con ACC-018 para eliminar el gate específico de enterprise sin eliminar la emisión común de trazas; definir la retención local única; validar `sync`, `audit`, MCP y doctor después de retirar las superficies gateway. |
| ACC-021 | R-04 | eliminar | descartado | Acción absorbida por `ACC-018`: la retirada de `generated-snapshots` queda incluida en la eliminación completa del eje gateway/overlays y no se ejecutará como incremento independiente. | Consolidación solicitada por el usuario para evitar dos acciones con el mismo resultado. `ACC-018` contiene ahora el alcance completo de registry, tipos, router, fallbacks, doctor, tests, configuración y documentación de `generated-snapshots`; se mantiene esta fila solo para conservar la trazabilidad del pedido original. | Resuelta: continuar únicamente con `ACC-018`. |
| ACC-022 | R-05 | eliminar | validado | Retirar `gateway` y `hybrid` del contrato público `ProjectMode` de `@axiom/project-resolution`: dejar `ProjectResolution.mode` en `local-only`, normalizar a `local-only` cualquier estado legacy que todavía los contenga y actualizar exports, doctor, tests y documentación activa. Conservar separados el `mode` topológico de `axiom.yaml` (`installed-multi-repo`, `one-axiom-per-product`), `TopologyManifest.mode` (`single-repo`/`multi-repo`) y los modos internos de `tool-routing`; no eliminar esos conceptos ni los brokers MCP reales. | Validado en `Axiom.Spec/specs/increments/_archive/INC-20260806-remove-legacy-resolution-modes/`: `ProjectMode` público solo contiene `local-only`; v1/v2 `gateway`/`hybrid` normalizan a local-only; build, suites dirigidas, doctor, readiness, freeze, receipts y review independiente pasan. Metadata `archived`, integración `integrated`. | ACC-018/019 ya validadas; cierre conservó compatibilidad raw y no alteró topología, worktree ni routing. |
| ACC-023 | R-05 | modificar | validado | Unificar el estado runtime ligado a un proyecto bajo un único namespace físico `.axiom-state/<projectKey>/`, eliminando la bifurcación `.axiom-state/config/<projectName>/` frente a `.axiom-state/<projectName>/`. Centralizar el cálculo de `projectKey` como identidad estable de máquina (`projectId` slug, con derivación/migración definida para v1), y hacer que el store y los escritores usen helpers comunes en vez de concatenar rutas. Llevar a ese namespace `init.json`, `install-profile.json`, `members.yaml`, `workspace.json`, `last-start.json`, `last-sync.json`, `toolchain.lock`, checkpoints, tracker y mappings, app plugins, skills pending, `managed-state.json`, `model-assignments.json`, `components-state.json`, workflow state, bindings MCP y memoria; mantener subdirectorios internos solo cuando eviten colisiones reales. Reservar `.axiom-state/local/` para estado verdaderamente repo/operador-local como audit trail y sidecar, topology bindings y overrides locales; mantener separados `executions/<executionId>/`, `axiom.config/` declarativo y `~/.axiom/` user-level. Migrar rutas legacy con precedencia determinista, escritura atómica, idempotencia y sin doble escritura, y reconciliar código, tests, doctor, checkpoints, worktrees y documentación activa. | Validado en `Axiom.Spec/specs/increments/_archive/INC-20260806-unify-project-state-paths/`: helper canónico y migración legacy; checkpoint restore remap; toolchain aliases y cleanup multi-alias; worktree provider identity; build, suites, doctor, readiness, freeze, receipts y review independiente pasan. Metadata `archived`, integración `integrated`. | ACC-022 coordinada; `config` se conserva como label API, `local` y `executions` mantienen fronteras separadas, y la spec/context/docs activas quedaron reconciliadas. |
| ACC-024 | R-06 | modificar | validado | Unificar la superficie de instrucciones de Copilot y VS Code en un contrato único y coherente: `copilot-vscode` y `github-copilot` deben emitir las instrucciones generales en `.github/copilot-instructions.md`; `.vscode/` debe quedar reservado para configuración de VS Code (`settings.json`, `extensions.json`) y MCP (`mcp.json`); las instrucciones específicas por ruta deben emitirse bajo `.github/instructions/<name>.instructions.md`. Reemplazar los recorridos divergentes de `workspace setup`, `configure` y `sync` por un writer/renderizador común, con una plantilla canónica y fallback bundleado, contenido compatible entre targets y un único registro/documentación de archivos generados. El writer debe regenerar únicamente el bloque `AXIOM:GENERATED`, preservar byte a byte `TEAM:CUSTOM` y el contenido humano fuera de ese bloque, y migrar de forma explícita y sin pérdida la ruta legacy `.vscode/copilot-instructions.md` cuando exista; cubrir la migración, la idempotencia, el contenido común, la preservación y la ausencia de salidas duplicadas en tests. | Validado en `INC-20260809-unify-copilot-instruction-surfaces`: writer común y fallback bundleado; rutas `.github` reconciliadas; process surfaces deduplicadas; migración legacy atómica, reintentable y conservadora ante cola humana divergente; lockfile sincronizado; documentación activa y contexto técnico integrados; review independiente final `OK`; build y batería focalizada final `13/146` verdes. | Coordinar `Axiom/packages/document-bootstrap`, `Axiom/packages/cli-commands/src/commands/{configure,sync,workspace-adapter-templates}.ts`, `Axiom/apps/cli/src/commands/{workspace-adapters,workspace-process-surfaces}.ts`, `@axiom/installer` registry, tests y `Axiom/docs/generated-files.md`; conservar `.vscode/mcp.json` y `.vscode/settings.json` como superficies independientes. |

| ACC-025 | R-07 | eliminar | validado | Eliminar LiteLLM como destino de Axiom: retirar `litellm` de `adapterTargets`, `allowedTargets`, `SUPPORT_MATRIX`, perfiles, tipos, dispatchers de `configure`/`sync`/workspace, registry, documentación, tests y paquetes de adapter que ya no tengan consumidores. No eliminar ni alterar el ruteo de modelos que Axiom conserve para otros destinos. | Reconciliado el 2026-09-14 contra el código: `packages/adapters/litellm` no existe y `SUPPORT_MATRIX` no contiene ninguna referencia a `litellm`. Ejecutado por `INC-20260809-remove-litellm-target` (archivado) y el cierre de R-10 (`INC-20260818-r10-closure-correction` retiró los mocks LiteLLM activos). | Ninguna. |
| ACC-026 | R-07 | investigar | validado | Reducir la duplicación entre `github-copilot`, `copilot-vscode` y `vscode` mediante una superficie común de instrucciones: usar `.github/copilot-instructions.md` como salida general para GitHub Copilot, Copilot CLI, VS Code y Visual Studio; conservar `.vscode/settings.json`, `.vscode/extensions.json` y `.vscode/mcp.json` únicamente como configuración opcional del editor. Ampliar y resolver junto con `ACC-024` la elección de un único target público para la instrucción Copilot y decidir si VS Code queda como destino de configuración adicional o como variante interna, sin generar dos archivos de instrucciones para el mismo repositorio. Mantener compatibilidad con `AGENTS.md` e instrucciones por ruta solo donde la herramienta realmente las soporte. | Reconciliado el 2026-09-14: ejecutado por `ACC-024` (`INC-20260809-unify-copilot-instruction-surfaces`) y el cierre de R-10, que dejó 8 targets públicos, rechazó `copilot-vscode` por API/CLI y conservó solo su migración interna desde `init.json`. | Ninguna. |
| ACC-027 | R-07 | investigar | validado | Verificar y simplificar la proyección para Visual Studio 2026: comprobar en una instalación real qué consume GitHub Copilot Chat (`.github/copilot-instructions.md`, `AGENTS.md`, `.github/instructions/*.instructions.md` y MCP), y decidir si `.vs/AXIOM.md` aporta valor o debe desaparecer en favor del archivo común. Si el soporte efectivo coincide con VS Code, reutilizar el mismo contenido y dejar solo las diferencias de configuración nativa; retirar o marcar como no verificados los outputs y notas que sean únicamente convenciones de Axiom. | Reconciliado el 2026-09-14: ejecutado por `INC-20260809-visual-studio-common-instructions` (archivado) y el cierre de R-10; `visual-studio-2026` delega en `.github/copilot-instructions.md` y no genera `.vs/AXIOM.md`. La verificación en instalación real de VS2026 queda como límite externo documentado. | Ninguna. |
| ACC-028 | R-07 | modificar | validado | Completar `axiom sync` para que materialice todos los adapters que sigan siendo contratos activos, incluyendo `antigravity`, `visual-studio-2026` y `codex`, además de los destinos ya soportados. Cada rama debe llamar al generador dedicado correspondiente, devolver el número real de archivos escritos, conservar la política best-effort y actualizar registry, marker, documentación y pruebas. Los targets retirados o todavía no verificables deben fallar de forma explícita o quedar fuera del contrato, no degradarse silenciosamente a `generatedFilesCount: 0`. | Reconciliado el 2026-09-14: ejecutado por `INC-20260809-sync-all-adapters` (archivado); el cierre de R-10 verificó que `sync` cubre los 8 targets activos y registra `writtenFiles`. | Ninguna. |
| ACC-033 | R-07 | investigar | validado | Analizar la duda abierta de Visual Studio MCP: verificar en una instalación real de Visual Studio 2026 si consume `.github/copilot-instructions.md` como instrucción de repositorio y si existe un schema/path MCP nativo verificable (`.vs/mcp.json` u otro). El objetivo es decidir si `visual-studio-2026` debe recibir un fichero MCP nativo (y cuál) o si se mantiene sin fichero con warning explícito. No se debe inventar un schema no verificado: si la verificación real no confirma una ruta, se conserva la disciplina actual (nota informativa, sin fichero). | Reconciliado el 2026-09-14: el análisis se ejecutó en la sesión R-07 del 2026-08-14 y concluyó que la duda no se puede resolver sin una instalación real de VS2026 (verificación externa); se mantiene la disciplina actual (nota informativa, sin fichero MCP). Cerrada como investigación completada con límite externo documentado. | Ninguna. |
| ACC-034 | R-07 | investigar | validado | Analizar la duda abierta de Codex/Antigravity user-global: verificar cómo se puede impedir o detectar que un MCP project-scoped de otro proyecto quede activo cuando el target solo admite configuración user-global (`~/.codex/config.toml` `[mcp_servers]` para Codex, `~/.gemini/config/mcp_config.json` para Antigravity). El objetivo es decidir si Axiom debe implementar un binding seguro (resolver el proyecto al invocarse y aplicar el guard project-bound) o si basta con mantener la nota informativa y el cierre por defecto. | Reconciliado el 2026-09-14: el análisis se ejecutó en la sesión R-07 del 2026-08-14 y derivó en `ACC-036`. Cerrada como investigación completada. | Ninguna. |
| ACC-035 | R-07 | investigar | validado | Analizar la duda abierta de las carpetas activas `specs/increments/INC-20260809-*` frente a sus homólogas archivadas en `_archive/`: verificar si las carpetas activas (README sin `metadata.yml`) son artefactos residuales que deben archivarse o si tienen un propósito vigente. El objetivo es decidir si se archivan, se completan con `metadata.yml` o se conservan tal cual, sin eliminar nada de forma destructiva. | Reconciliado el 2026-09-14: el análisis se ejecutó en la sesión R-07 del 2026-08-14 y confirmó que son residuos, derivando en `ACC-037`. Cerrada como investigación completada. | Ninguna. |
| ACC-036 | R-07 | modificar | propuesto | Implementar un binding seguro para los targets user-global `codex` y `antigravity` que impida o detecte que un MCP project-scoped de otro proyecto quede activo en la configuración global (`~/.codex/config.toml` `[mcp_servers]` para Codex, `~/.gemini/config/mcp_config.json` para Antigravity). El mecanismo debe resolver el proyecto al invocarse y aplicar el guard project-bound (`filterProjectBoundMcpServers`), de modo que un MCP de un proyecto no aparezca como válido para otro por compartir la configuración global. Si no se puede resolver el proyecto actual de forma segura, el MCP debe quedar fuera y emitirse un warning accionable. | Reconciliado el 2026-09-14: sigue abierta. Los generadores de `codex` y `antigravity` solo escriben `AGENTS.md` con la nota informativa; no existe mecanismo de binding seguro. Única acción genuinamente pendiente de R-07 junto con la limpieza de ACC-037. | Ninguna nueva. |
| ACC-037 | R-07 | eliminar | propuesto | Archivar/limpiar las carpetas activas residuales `specs/increments/INC-20260809-*` que quedaron como residuo tras el archivado de sus homólogas completas en `_archive/`: `INC-20260809-mcp-project-target-filter` (vacía), `INC-20260809-remove-litellm-target`, `INC-20260809-sync-all-adapters`, `INC-20260809-unify-copilot-target` y `INC-20260809-visual-studio-common-instructions` (solo `README.md` con estado `closed`). El objetivo es eliminar la duplicación entre la carpeta activa (README o vacía) y la archivada completa (`metadata.yml`, requisitos, criterios, receipts, candidate-freeze), sin eliminar nada de forma destructiva: conservar la versión archivada completa como fuente de verdad. | Reconciliado el 2026-09-14: sigue abierta. Las 5 carpetas activas residuales persisten (1 vacía, 4 con solo `README.md`) frente a sus homólogas completas en `_archive/`. | Ninguna nueva. |

| ACC-038 | R-10/R-03 | documentar | validado | Corregir los dos contratos documentales del lifecycle de proyecto que contradicen la ejecución: actualizar el manual de `axiom upgrade` para reflejar que el primer upgrade puede inicializar `managed-state.json` y corregir el comentario de `@axiom/orchestrator` que declara gates sin I/O cuando algunos comprueban ficheros. No cambiar la semántica de upgrade ni de los gates; alinear manuales, comentarios y pruebas documentales con el comportamiento vigente. | Validado en `INC-20260817-r10-acc038-lifecycle-docs`: el manual ahora distingue primera ejecución real y dry-run; el comentario de gates declara lectura local read-only de filesystem. Suites focalizadas gates/upgrade: 2 archivos, 19 tests. Metadata archivada e integración `integrated`; la discrepancia de scope legacy y CLI bootstrapping queda como evidencia para ACC-044/045. | Ninguna. |
| ACC-039 | R-10 | eliminar | validado | Retirar de catálogos, contratos materializables, process surfaces, pruebas asociadas y documentación activa las promesas de comandos SDD públicos inexistentes: `axiom sdd advance`, `axiom plan` y `axiom role start`. Orientar cada instrucción al comando real correspondiente (`axiom state`, `axiom-increment`, `axiom-bug`, `axiom-plan` y `axiom-role`). No retirar en esta acción los 19 intent commands internos `not-implemented` de `@axiom/orchestrator`: su conservación o retirada requiere una decisión independiente. Mantener las referencias de incrementos y decisiones archivadas como historia. | Reconciliado el 2026-09-14 contra el código: `agents-catalog.yaml` y `skills-catalog.yaml` ya no contienen `sdd advance`, `axiom plan` ni `role start`. Ejecutado por el cierre de R-10 (`INC-20260818-r10-closure-correction`, archivado). Los 19 intents internos `not-implemented` de `@axiom/orchestrator` se conservan como decisión independiente pendiente. | Ninguna. |
| ACC-040 | R-10 | eliminar | propuesto | Retirar `@axiom/cavekit-discipline` del runtime activo: package, project references, lockfile generado, pruebas propias, referencias activas de README, inventarios, contexto y documentación que lo presenta como capacidad operativa. Retirar su dependencia directa no usada. No eliminar la historia de la decisión 0015 ni incrementos archivados: mediante Axiom/Core, registrar que la decisión queda superada por la retirada y conservarla como antecedente histórico. | Reconciliado el 2026-09-14: sigue abierta. `packages/cavekit-discipline` persiste en el árbol con `src`, `tests` y `dist`; sin embargo, ya no aparece en las project references de `tsconfig.json` y no tiene importadores productivos (0 referencias en el resto de packages y CLI). Es residuo inerte, no superficie viva. | Ninguna nueva. |
| ACC-041 | R-10 | modificar | validado | Crear un ejecutor común de transiciones gobernadas que consuma el resolvedor único de workflows de ACC-045 y aplique de forma coherente en CLI, launcher y MCP: validación de transición, preview, confirmación declarada, efectos locales, gate QA y persistencia. Para archive/integrate, centralizar la coordinación de workflow-state, metadata, `integration.status`, movimiento a `_archive` y recuperación compensatoria. Como decisión de alcance, retirar del grafo activo el efecto `tracker: close-external-work-item` mientras no exista un dispatcher productivo explícito y confirmado; no inventar sincronización externa automática. Mantener la máquina de estados pura y separar las operaciones de I/O en este ejecutor. | Reconciliado el 2026-09-14 contra el código: existe `packages/workflow/src/governed-transition-runner.ts` (`runGovernedTransition`) consumido por CLI, launcher y MCP; el gate QA duplicado ya no existe y las transiciones de bug/incremento aplican efectos y receipts de forma uniforme (verificado operativamente al cerrar los 8 bugs baseline el 2026-09-13). | Ninguna. |
| ACC-042 | R-10 | modificar | validado | Simplificar y hacer explícito el cierre de planes: un plan permanece en `draft` mientras se redacta y refina; la operación canónica de cierre/aprobación valida los requisitos definidos del plan, exige la confirmación gobernada de ACC-041 y lo mueve una sola vez a `plan-approved`. El launcher debe previsualizar y confirmar esa misma operación, delegando en el runner canónico. Solo `plan-approved` habilita el inicio de roles. No introducir estados independientes de “cerrado”, “validado” y “aprobado” ni duplicar validaciones de contenido entre interfaz y CLI. | Reconciliado el 2026-09-14 contra `axiom.config/workflows.yaml`: el workflow de plan mantiene la transición única `draft → plan-approved` con estados terminales declarados y el runner gobernado aplica confirmación y preview. | Ninguna. |
| ACC-043 | R-10 | modificar | validado | Eliminar la duplicación de `checkQaArchiveGate` y fijar un único contrato de QA para incrementos, bugs, CLI directa, launcher e integrate. Con `qaLane: parallel`, el archive puede continuar aunque QA esté pendiente, pero el resultado debe mostrar un aviso preciso y consistente. Con QA dentro del flujo inline o como rol requerido, el archive debe bloquearse hasta que la evidencia QA requerida esté correcta. El helper único debe expresar de forma explícita los estados pasado, fallido, cancelado y pendiente, y todos los callers deben usarlo. | Reconciliado el 2026-09-14 contra el código: no queda ningún helper `checkQaArchiveGate` ni `qaArchiveGate` duplicado; el gate QA vive en el runner gobernado y se aplicó uniformemente en el archive de los 8 bugs baseline (verificado el 2026-09-13: `qaLane=inline, evidence=passed`). | Ninguna. |
| ACC-044 | R-10 | investigar | validado | Revisar caso por caso las divergencias activas detectadas entre especificación, contexto, catálogos, comentarios y código de R-10, y decidir para cada una si corresponde cambiar runtime, configuración, tests o documentación. El informe debe separar contrato activo, evidencia ejecutable e historia archivada; no debe cambiar producto ni reescribir decisiones históricas. Debe incluir efectos declarados, commands con espacio frente a comandos con guion, `sdd advance`, approval, fallback YAML/default, semánticas de archive, Cavekit, ACC-007 y las discrepancias de lifecycle cercanas. | Reconciliado el 2026-09-14: la revisión caso por caso se ejecutó durante el cierre correctivo de R-10 (`INC-20260818-r10-closure-correction`, tres revisiones independientes, `BLOCKERS: NO`) y las decisiones quedaron registradas en ese incremento y en la consolidación de specs 00..08. | Ninguna. |
| ACC-045 | R-10 | modificar | propuesto | Eliminar el mantenimiento manual entre `axiom.config/workflows.yaml`, `DEFAULT_WORKFLOWS` y los mapas de subcomandos. Mantener `workflows.yaml` como fuente declarativa canónica y empaquetar o generar desde ella el default distribuible, sin una segunda representación editable a mano. Crear un único resolvedor para todos los consumers con regla fail-closed: YAML ausente usa el default empaquetado; YAML presente y válido gana; YAML presente inválido o con schema no soportado falla y nunca cae silenciosamente al default. Verificar además que los subcomandos alcanzables coincidan con el grafo declarado. | Reconciliado el 2026-09-14: sigue abierta. `packages/workflow/src/default-workflows.ts` mantiene la segunda representación manual y el build copia `workflows.yaml` a `dist` sin generar el default desde ella. El runner gobernado existe (ACC-041), pero el resolvedor único fail-closed no. | Ninguna nueva. |

| ACC-046 | R-11 | documentar | validado | Reconciliar todas las afirmaciones activas e históricas sobre MCP con el runtime vigente: existe un único broker gestionado y project-scoped, `axiom-mcp-broker`, lanzado únicamente como `axiom mcp serve --kind axiom`, que expone 25 acciones registradas de los dominios internos `sdd.*`, `spec.*`, `memory.*` y `axiom.*`. Marcar `sdd-mcp-server`, `spec-mcp-broker` y el antiguo kind `memory` exclusivamente como historia o IDs de limpieza de configuración obsoleta; no presentarlos como procesos activos. Aclarar que Engram es un MCP local de terceros y backend opcional de memoria, no un segundo broker gestionado de Axiom. Corregir también la relación real entre `mcp.yml`, `mcp-manifest.yaml`, el despacho directo de `@axiom/mcp-server` a `@axiom/mcp-tools` y el router genérico de providers, preservando los incrementos archivados como evidencia histórica. | El código actual restringe `McpServerKind` a `axiom`; `mcp-manifest.yaml` e `integrations.yaml` declaran una sola entrada; `createMcpServer` publica la unión completa y despacha directamente a `invokeMcpTool`; las pruebas focalizadas pasan 10 archivos y 97 tests. La spec y el contexto más recientes ya reflejan gran parte de esta verdad, pero `PLAN-REVISION-INTEGRAL-AXIOM.md` aún afirma en ACC-020 y en su cierre que se conservaron tres brokers activos, y algunos comentarios del runtime conservan el flujo anterior. | Coordinar con el cierre correctivo de R-10 y revisar `Axiom.Spec/specs/{03_Modelo_Operativo_y_Datos,05_Interfaces_Operativas,06_Integraciones_y_Capacidades}.md`, `Axiom.Spec/context/**`, este plan, `Axiom/packages/{mcp-server,mcp-tools}/src`, `Axiom/apps/cli/tests/mcp-serve.test.ts` y documentación operativa cercana. No reescribir receipts ni artefactos archivados; añadir contexto de supersesión cuando sea necesario. |
| ACC-047 | R-11/R-00 | documentar | validado | Aclarar de forma consistente en las tres superficies del agente `axiom-runtime-auditor` que el objeto de la auditoría es el runtime `Axiom/`; `Axiom.Spec/` se usa para localizar intención, contrastar y consolidar conocimiento, y `Axiom.SDD/` solo para leer las reglas de trabajo y alojar el propio agente, no como fuente de evidencia sobre el comportamiento del runtime salvo petición explícita del usuario. Indicar además que configuraciones locales de herramientas en `Axiom.SDD/` no demuestran integraciones del producto Axiom. | Petición expresa del usuario durante R-11. Las tres copias actuales del agente viven en `Axiom.SDD/.kiro/steering/axiom-runtime-auditor.md`, `.kiro/agents/axiom-runtime-auditor.md` y `.github/agents/axiom-runtime-auditor.agent.md`; ordenan leer las reglas de Axiom.SDD pero no hacen explícita esta prioridad de alcance, lo que permitió considerar inicialmente su `mcp.json` local como contexto de la zona. No se editan durante la auditoría por su límite de escritura. | Actualizar conjuntamente las tres copias o su fuente generadora si existe, comprobar que no divergen y mantener intactos los límites de escritura y consolidación del auditor. |

| ACC-048 | R-11/R-12 | modificar | validado | Endurecer y aclarar el intercambio de memoria de equipo mediante `axiom knowledge sync/pull`. `knowledge pull` debe dejar de exigir o aceptar `--increment`: su función canónica será traer todos los chunks pendientes del repo `<project>.axiom` e importar toda la memoria compartida disponible, general o ligada a cualquier incremento. `knowledge sync` seguirá acotado por `--increment` y `--phase`, pero deberá conservar en cada chunk la evidencia completa requerida por `MemoryEntry` (`rationale`, `source` y metadata estable), exportar únicamente entradas marcadas explícitamente `visibility: project-shared` y tratar `private` o visibilidad ausente como no compartible, informando cuántas entradas se omiten y por qué. La detección de secretos debe revisar todos los campos de texto serializados, no solo `text`. La operación debe ser preview por defecto; escribir chunks y crear el commit requerirá confirmación explícita, y enviar al remoto requerirá además `--push` explícito. Las llamadas Git deben usar argumentos sin interpolación de shell. El marcador de chunks importados debe trasladarse a `.axiom-state/<projectKey>/` mediante los helpers canónicos, con lectura/migración compatible del `.engram/.imported` antiguo, para que nunca termine versionado. Un chunk solo se marcará importado cuando todas sus entradas válidas se hayan guardado; corrupciones y fallos parciales deben quedar visibles y ser reintentables. Añadir validación de schema, escrituras atómicas, idempotencia y pruebas focalizadas de sync/pull, privacidad, secretos, evidencia, Git preview/confirm/push, chunks corruptos, fallos parciales y dos miembros del mismo proyecto. | Validado en `INC-20260820-r11-knowledge-sync-hardening`: sync/pull preservan evidencia, privacidad y reintentos; el flujo confirma mutaciones locales, separa `--push` y mueve el marker personal al estado project-scoped. La suite focalizada `apps/cli/tests/knowledge-sync.test.ts` pasa 18/18; build y revisión independiente sin blockers. | Mantener `.engram/chunks/` y `manifest.json` como intercambio append-only versionable; la base local y marcadores personales permanecen fuera de Git. |

| ACC-049 | R-11 | modificar | validado | Convertir la selección de contexto recomendado en una regla determinista y única basada en etiquetas. Cada documento de `Axiom.Spec/context/**` podrá declarar etiquetas explícitas y estables —por ejemplo `memory`, `engram`, `mcp`, `git` o `security`— mediante metadata legible por el generador; si no las declara, se conservará como fallback la etiqueta general derivada de su carpeta. `axiom context index` deberá incorporar esas etiquetas al índice generado sin editar manualmente `technical-context/indexes/*.index.yml`. Ante una petición con `taskTags`, la respuesta deberá incluir, en orden y sin duplicados: documentos `mandatory.always`, documentos `mandatory.whenTags` cuyas condiciones se cumplan y entradas `available` que coincidan con al menos una etiqueta solicitada. Sin etiquetas de tarea solo se devolverá el contexto obligatorio, nunca todos los documentos disponibles. `spec.recommendedContextList` y `spec.implementationContextRead` deben usar exactamente el mismo selector; la lectura compuesta aceptará `taskTags` opcionales y los combinará de forma explícita y documentada con las señales estructuradas disponibles del plan o rol, sin inferencia por IA ni análisis libre del texto. La salida debe distinguir claramente contexto obligatorio de recomendado, mantener rutas confinadas al spec repo y conservar los niveles de presupuesto que deciden si se devuelve solo la referencia o también el contenido. Actualizar el índice real de `Axiom.Spec`, etiquetar los documentos propietarios con vocabulario pequeño y reutilizable, y cubrir generación, normalización de etiquetas, coincidencia, ausencia de tags, deduplicación, fallback `repo`, presupuesto e aislamiento mediante pruebas. | Validado en `INC-20260820-r11-context-tag-selection`: selector común con orden obligatorio/ALL/ANY y deduplicación; frontmatter `tags` opcional con fallback folder/`repo`; `taskTags` ausente o vacío no recomienda disponibles; presupuesto y confinamiento se mantienen. La batería focalizada de ocho archivos pasa 96/96, seguida de build y revisión independiente sin blockers. | Índice derivado regenerado exclusivamente por `axiom context index`; no se introducen scoring, IA, búsqueda semántica, dependencias externas ni índices obligatorios. |
| ACC-050 | R-13 | eliminar | validado | Retirar por completo `~/.axiom/registry.json`, sus tipos/exports, el error `legacy-registry-not-migrated`, el migrador y todas las ramas activas; mantener `~/.axiom/projects.yml` schema 2 como único catálogo user-level. | Validado en `INC-20260829-r13-user-project-registry`: código, tests, fixtures y documentación activa ya no leen, escriben ni prometen migrar v1; `projects.yml` ausente se trata como catálogo vacío y un `registry.json` residual queda intacto e ignorado. | Se conservaron únicamente referencias históricas dentro de artefactos archivados; no se reescribieron receipts. |
| ACC-051 | R-13 | modificar | validado | Mantener `axiom projects use` únicamente como actualización explícita de recencia, ordenar `projects list` de forma determinista y sustituir `--print-cwd` por `--print-path` shell-neutral. | Validado: `useProjectV2` solo cambia `lastUsedAt`; la resolución continúa basada en cwd/projectId. La lista ordena por recencia descendente e id ascendente; `--role` prevalece, luego se prefiere `axiom` y finalmente la key lexicográficamente menor; `--print-path` emite solo path + salto de línea. | CLI y documentación incluyen consumo seguro desde PowerShell, `cmd.exe` y POSIX, con cobertura de paths especiales. |
| ACC-052 | R-13 | modificar | validado | Validar de forma completa `projects.yml` antes de exponerlo y separar errores tipados de schema, dominio, I/O y lock. | Validado: el parser de schema cerrado comprueba clave/id, nombre, ISO timestamps, repos no vacíos, role/key, paths y ownership; YAML inválido, schema futuro o shape parcial devuelve `invalid-projects-file` y jamás se sobrescribe silenciosamente. | CLI, launcher y MCP reutilizan el mismo loader; los mensajes sanitizan causas y no filtran el contenido del catálogo. |
| ACC-053 | R-13 | modificar | validado | Endurecer la identidad y el ownership: contrato explícito de ID/Unicode, unicidad de path canónico y upsert sin fusiones accidentales. | Validado: ID explícito exige slug ASCII; la derivación NFKC elimina diacríticos y colapsa separadores; derivación vacía exige `--id`. Realpath/case-folding según plataforma detecta symlinks, junctions y equivalencias; `id-collision`, `path-collision` e `identity-conflict` fallan sin mutar. | Una reejecución solo es idempotente si coinciden ID, nombre e inventario ya poseído; topología posterior debe reutilizar este contrato. |
| ACC-054 | R-13 | modificar | validado | Coordinar todas las mutaciones de `projects.yml` y sustituir el tmp fijo por lock local acotado, temporal único y reemplazo atómico recuperable. | Validado: `@axiom/core` aporta lock multiproceso + writer PID/UUID; `@axiom/user-workspace` encierra load→mutate→save. Pruebas subprocess/barrier cubren lost updates, timeout, crash y dos races remediados: reclaimer tardío contra generación sucesora y ventana `mkdir`→publicación de owner. | Diseño final: lease previo a `mkdir`, owner fsynced antes del handoff, observación fence→owner y reclaim claims por generación/epoch; recuperación solo de artefactos propios huérfanos. |
| ACC-055 | R-13 | modificar | validado | Sustituir `stale` por disponibilidad física precisa y resolubilidad Axiom separada, sin borrado automático. | Validado: cada repo expone `present-directory`, `missing` o `not-directory`; el rollup es `available`, `partial` o `unavailable`. La resolubilidad solo se calcula mediante probe explícito; `projects add` no la exige y `projects join` sí. | CLI, launcher y MCP conservan entradas problemáticas para diagnóstico y cubren repos mixtos, ficheros, symlinks y proyectos sin `axiom.yaml`. |
| ACC-056 | R-13 | modificar | validado | Unificar el contrato JSON de `axiom projects list|add|join|use` en éxito y error, con stdout limpio y exit code honesto. | Validado: los cuatro wrappers emiten exactamente `{schemaVersion:1, ok, command, data?|error?}`; diagnósticos humanos van a stderr, los fallos terminan en 1 y los tests E2E cubren home inválido, catálogo corrupto, duplicado, not-found y éxito. | Launcher y consumidores reutilizan los runners/modelos tipados; no se mezcló este contrato con otros dominios CLI. |
| ACC-057 | R-13 | eliminar | validado | Eliminar por completo el schema 1 de topología y mantener `schemaVersion: 2` como único contrato runtime. El manifest canónico tendrá un solo `axiomRepo` que reúne control/SDD/spec, `codeRepos[]` asociados a team/code roles y `legacyRepos[]` opcional con cero, una o dos fuentes antiguas de SDD y Spec, siempre `kind: legacy`, `mode: read-only-source` e identificadas explícitamente por su función legacy sin depender de convenciones ambiguas de ID. Retirar `sddRepo`, `specRepo`, `roleCodeRepositories`, `normalizeLegacyShape`, campos espejo, warning `deprecated-legacy-shape`, defaults schema 1, branches v1 del loader, tests y documentación activa de compatibilidad. Actualizar todos los productores (`init` fallback, workspace setup/incremental/adopt, roles, member install) y consumidores (doctor, MCP, upgrade, freeze, knowledge, planes, write-scope, launcher) para usar directamente `axiomRepo`, `codeRepos` y `legacyRepos`, sin migrador ni compatibilidad v1. | Reconciliado el 2026-09-14 contra el código: 0 referencias a `schemaVersion: 1`, `normalizeLegacyShape` o `sddRepo` en `packages/topology/src`. Ejecutado por `INC-20260829-r13-topology-schema2-authority` (archivado). | Ninguna. |
| ACC-058 | R-13 | modificar | validado | Completar las invariantes semánticas del único schema 2: cada assignment debe apuntar a un `codeRepo` declarado y a un team/code role válido; cada code repo debe tener el ownership requerido; IDs de repos y roles deben ser únicos; el número y significado de assignments `primary` debe ser inequívoco; `mode` debe concordar con la distribución real; `axiomRepo` debe ser único y de kind `axiom`; `codeRepos` deben ser kind `code`; `legacyRepos` solo pueden representar las fuentes legacy SDD/Spec permitidas, sin duplicados y read-only. Refs vacíos, colisiones físicas y shapes desconocidos deben fallar con findings tipados. | Reconciliado el 2026-09-14 contra `packages/topology/src/validate.ts`: el validator contiene 98 referencias a invariantes (`duplicate-id` con scope global, validación de `primary` booleano, `primaryByRole`, kinds de repos) y las pruebas negativas por invariante viven en `packages/topology/tests`. Ejecutado por el lote estructural R-13 archivado. | Ninguna. |
| ACC-059 | R-13 | modificar | validado | Fijar una única fuente autoral de topología en el `axiomRepo` y eliminar el drift entre `axiom.yaml`, múltiples copias reancladas de `topology.yaml` y mutaciones locales de `roles`. Definir qué identidad/puntero mínimo conserva cada `axiom.yaml` de repo de código y hacer que repos, roles, assignments, QA lane y referencias legacy se lean y muten solo en el manifest canónico. Dejar de escribir copias autorales independientes en cada repo; si alguna proyección local sigue siendo necesaria, debe ser generada, marcada como derivada, reconciliable y nunca aceptada como segunda fuente de verdad. Todas las mutaciones `roles` deben localizar y escribir el mismo `axiomRepo`. | Reconciliado el 2026-09-14 contra `workspace-setup.ts`: el código declara explícitamente que `axiom.config/topology.yaml` se escribe UNA vez en `axiomRepo` y las mutaciones pasan por `resolveTopologyAuthority`/`prepareTopologyWrite`. Ejecutado por el lote estructural R-13 archivado. | Ninguna. |
| ACC-060 | R-13 | modificar | validado | Alinear `topology-bindings.yaml` con schema 2 y endurecer su resolución: permitir bindings para `axiomRepo`, cada `codeRepo` y las fuentes `legacyRepos` opcionales; almacenar paths absolutos canonicalizados; rechazar IDs desconocidos y valores vacíos/no-path en vez de persistir typos futuros; distinguir path ausente, no-directorio y URI no materializada; normalizar `--project-path` y overrides relativos antes de resolver. Un bindings YAML corrupto debe producir un error visible en consumidores sensibles, no caer silenciosamente a refs potencialmente incorrectos. | Reconciliado el 2026-09-14 contra `packages/topology/src/loader.ts`: 21 referencias a `axiomRepo`/`codeRepos`/`legacyRepos` en el loader de bindings y contrato cubierto por `packages/topology/tests/bindings-contract.test.ts` (21 tests). Ejecutado por `INC-20260829-r13-topology-bindings-persistence` (archivado). | Ninguna. |
| ACC-061 | R-13 | modificar | validado | Preservar errores topológicos y contratos JSON de extremo a extremo. `topology show|validate` debe devolver un envelope JSON estable en éxito y error, mantener el `TopologyError` real (`invalid-yaml`, `invalid-manifest`, schema no soportado, profiles inválido, I/O) y no recodificarlo como `invalid-repo-ref` ni emitir un manifest fallback que parezca real. Los mensajes humanos deben ir por el canal apropiado y las APIs de launcher/MCP deben reutilizar el mismo modelo de error. | Reconciliado el 2026-09-14 contra `apps/cli/src/commands/topology.ts`: 0 recodificaciones a `invalid-repo-ref`; el envelope JSON y el `TopologyError` real se preservan (7 referencias verificadas). Ejecutado por el lote estructural R-13 archivado. | Ninguna. |
| ACC-062 | R-13 | modificar | validado | Centralizar las escrituras de topología y bindings en un writer seguro: validación completa antes de persistir, archivo temporal único, reemplazo atómico, lock local u optimistic compare-and-swap para evitar lost updates, limpieza/recuperación tras fallo e idempotencia. Evitar que una mutación de roles serialice campos derivados o shapes de compatibilidad y definir una política explícita sobre comentarios humanos: preservarlos mediante edición estructurada o declarar el YAML generado y separar extensiones humanas, pero no borrarlos silenciosamente. | Reconciliado el 2026-09-14 contra `packages/topology/src/loader.ts`: el writer usa `withLocalFileLockSync` y `atomicWriteFileSync` de `@axiom/core` (24 referencias a primitivas de escritura segura). Ejecutado por el lote estructural R-13 archivado. | Ninguna. |
| ACC-063 | R-13 | modificar | validado | Hacer que la topología canónica schema 2 del `axiomRepo` sea la fuente local de resolución del workspace y que `projects.yml` quede como catálogo user-level opcional para descubrimiento y recencia, nunca como autoridad del grafo. Refactorizar `WorkspaceSetupSpec`/`WorkspaceRepoSpec` para representar exactamente un repo `kind: axiom` y cero o más repos `kind: code`, sin simular control y spec mediante dos entradas sobre el mismo path. Cada repo de código conservará en su `axiom.yaml` la identidad y el puntero mínimo necesarios para localizar el `axiomRepo`; desde cualquier repo resoluble, setup incremental y reparaciones deben cargar allí `codeRepos`, roles y assignments aunque el proyecto se haya creado con `--no-register`. Si existe una entrada en `projects.yml`, será una proyección reconciliable del `axiomRepo` y sus code repos, con metadata estructural explícita y sin inferir kinds por keys convencionales `sdd`/`spec`. | Reconciliado el 2026-09-14 contra `workspace-incremental.ts`: `resolveExistingProject` y `buildRepoSpecsFromResolvedProject` resuelven desde la identidad del proyecto, no desde keys convencionales del registro. Ejecutado por `INC-20260829-r13-workspace-resolution-surface` (archivado). | Ninguna. |
| ACC-064 | R-13 | eliminar | validado | Eliminar `axiom repo attach` y retirar `repo add --kind control|spec`. El alta o unión en el catálogo user-level quedará exclusivamente en `axiom projects add|join`; `axiom repo add` y su shorthand `axiom role add` solo incorporarán un `codeRepo` asociado a un team/code role al proyecto local resuelto. Retirar el parámetro ambiguo `<repoId>` que `attach` usa realmente como project ID, su migración legacy, evento, ayuda, tests y documentación; conservar `axiom discover` como operación de descubrimiento independiente si sigue teniendo consumidores. | Reconciliado el 2026-09-14 contra `apps/cli/src/commands/repo.ts`: 0 referencias a `attach`. Ejecutado por el lote estructural R-13 archivado. | Ninguna. |
| ACC-065 | R-13 | modificar | validado | Añadir un preflight común, completo y libre de escrituras para `workspace setup`, adopción, `repo add` y `role add`. Antes de crear directorios o ficheros debe canonicalizar y validar project ID, role keys, repo IDs y paths; rechazar nombres reservados, IDs duplicados, un mismo path bajo varias identidades, repos físicos compartidos entre proyectos, `axiom.yaml` foráneo o no interpretable, flags `create` incoherentes y cualquier solapamiento entre destinos escribibles y fuentes legacy/contexto read-only. La protección de ownership debe abarcar todos los outputs —topología, estado, AGENTS, configuración, MCP, skills, rules y registro—, no solo evitar reemplazar un `axiom.yaml`. Los conflictos deben devolver errores tipados y cero mutaciones. | Reconciliado el 2026-09-14 contra el código: el preflight vive en `workspace-structural-plan.ts` (`StructuralPreflightError` con kinds tipados) y su batería es `apps/cli/tests/workspace-structural-preflight.test.ts`. Ejecutado por `INC-20260829-r13-structural-mutation-safety` (archivado). | Ninguna. |
| ACC-066 | R-13 | modificar | validado | Introducir un coordinador de mutación estructural para setup, adopción y operaciones incrementales: calcular primero el estado deseado, validarlo, preparar escrituras y aplicar como una unidad recuperable `axiom.yaml`, topología, bindings, `projects.yml` cuando se solicite registro y estado estructural indispensable. Usar staging/backup o journal con rollback/roll-forward determinista, preservar concurrencia segura y separar el commit estructural de outputs derivados best-effort. Un fallo de topología, bindings, ownership o registro solicitado no puede terminar en mensaje de éxito ni `exitCode: 0`. Definir resultados tipados que distingan `created`, `updated`, `unchanged`, `skipped`, warnings derivados y error estructural, junto con envelopes JSON y códigos de salida coherentes para CLI/launcher. | Reconciliado el 2026-09-14 contra el código: existe `workspace-structural-transaction.ts` con baterías de transacción, concurrencia y recuperación (`workspace-structural-transaction/concurrency/recovery.test.ts`). Ejecutado por `INC-20260829-r13-structural-mutation-safety` (archivado). | Ninguna. |
| ACC-067 | R-13 | modificar | validado | Definir `axiom.yaml` como documento gestionado por secciones y sustituir la reescritura completa por una reconciliación que actualice únicamente identidad, puntero al `axiomRepo` y campos derivados propiedad de Axiom, preservando comentarios, campos de extensión y contenido humano fuera del bloque gestionado. Si un documento no puede reconciliarse de forma segura, la operación debe fallar en preflight sin reemplazarlo. Retirar el mapa recíproco duplicado que ACC-059 haga innecesario y documentar con precisión qué contenido puede regenerarse. | Reconciliado el 2026-09-14 contra `workspace-axiom-yaml.ts`: 5 referencias a markers `AXIOM:MANAGED` y reconciliación seccional. Ejecutado por el lote estructural R-13 archivado. | Ninguna. |
| ACC-068 | R-13 | modificar | propuesto | Reconciliar las reparaciones granulares con el setup actual mediante un catálogo común de pasos y ownership. Cada comando debe validar todos sus targets antes de escribir y rechazar paths ajenos al proyecto; `workspace adapters` debe reparar también las process surfaces que pertenecen al adapter sin invadir la responsabilidad separada de `mcp-config`; `workspace rules` debe reconciliar la sección correspondiente de `AGENTS.md`; `config-scaffold` debe cubrir las declaraciones, políticas y catálogos que el setup vigente considera reparables; `mcp-config` debe validar `--path` antes de tocar el MCP canónico. Mantener explícita la diferencia entre “añadir/habilitar” y “reparar/regenerar”, y devolver resultados tipados por paso. | Reconciliado el 2026-09-14: parcialmente ejecutada. `buildWorkspaceDerivedOutputs` reconcilia AGENTS y derivados (test `workspace-step-reconciliation.test.ts` verifica preservación del preámbulo humano) y `workspace rules` ya reconcilia AGENTS (4 referencias), pero `workspace adapters` sigue sin reparar process surfaces (0 referencias). Queda ese hueco granular. | Ninguna nueva. |
| ACC-069 | R-13 | modificar | validado | Definir y validar un schema tipado único para `.axiom-state/<projectKey>/workspace.json`, compartido por setup, adapters, providers, MCP y launcher. JSON inválido, schema futuro o campos con tipos incorrectos deben producir un error visible y nunca convertirse silenciosamente en arrays vacíos ni ser sobrescritos por una operación add. Las actualizaciones de adapters/providers deben ser atómicas y concurrentemente seguras, preservar todos los campos declarados, diferenciar alta de no-op y mantener timestamps con semántica precisa. | Reconciliado el 2026-09-14 contra `packages/user-workspace/src/workspace-state.ts`: loader estricto con errores tipados (`invalid-json`, `invalid-shape`, `invalid-type`, `invalid-value`) que exige `projectId`/`createdAt`/`updatedAt` (36 referencias verificadas; el Grupo E del plan de baseline confirmó el loader estricto). Ejecutado por el lote estructural R-13 archivado. | Ninguna. |
| ACC-070 | R-13.4 | modificar | validado | Cerrar el control plane HTTP del launcher como superficie estrictamente local y autenticada, con sesión, same-origin, schemas, límites, headers, browse confinado y SSE gobernado. | Validado en `INC-20260829-r13-launcher-control-plane`: la matriz cubre 9 casos ACC-070 con servidor real, auth/origin/body/schema/security headers, browse confinado, límites/cleanup/backpressure SSE y ausencia de mutación donde aplica. | Se conserva Doctor como diagnóstico y la provenance worktree-scoped/shared; lifecycle y receipts se gestionan por Core. |
| ACC-071 | R-13.4 | modificar | validado | Hacer seguros y verificables los transportes de entrega del launcher, con endpoints configurados, allowlist, SSRF/redirect/DNS guards, timeout, límites y estados honestos de clipboard/VS Code. | Validado en `INC-20260829-r13-launcher-control-plane`: la matriz cubre 7 casos ACC-071 con HTTP/HTTPS hermético, redirect, timeout, respuesta acotada, endpoint rechazado y ack/evidence de clipboard; no se presenta VS Code ficticio como delivered. | No hay red externa ni modo remoto; el lifecycle se gestiona por Core. |
| ACC-072 | R-13.4 | modificar | validado | Vincular preview y confirmación de forma transaccional mediante token single-use, TTL y binding de proyecto/acción/payload, con required fields y fail-closed en las superficies declaradas. | Validado en `INC-20260829-r13-launcher-control-plane`: la matriz cubre 9 casos ACC-072 de execute/replay, mismatch, expiry, race, stale craft y required fields en catálogo, onboarding, plugins, roles/Git y ADO. | La revisión independiente confirmó el contrato CA-F3; el lifecycle se gestiona por Core. |
| ACC-073 | R-13.4/R-10 | modificar | validado | Unificar catálogo, routing, previews CLI e identidad explícita sin fallback accidental ni comandos ficticios. | Validado en `INC-20260829-r13-launcher-catalog-ado`: routing fail-closed, previews `axiom ...`, IDs caller-owned y mismatch antes de receipt/transición/mutación; la matriz cubre ACC-073-01..03. | No redefine ACC-041/045; reutiliza el workflow vigente. |
| ACC-074 | R-13.4/R-12 | modificar | validado | Alinear ADO con semántica local-first, resultados local/remoto separados y URLs externas limitadas a HTTP(S). | Validado en `INC-20260829-r13-launcher-catalog-ado`: bridge fake hermético, preview/confirmación local-first, fallo remoto no destructivo y href seguro; la matriz cubre ACC-074-01..02. | ADO permanece opcional y sin red externa real. |
| ACC-075 | R-13.4/R-09 | modificar | validado | Gobernar el panel de telemetría mediante tail bounded/validated, orden explícito, aislamiento por proyecto y separación project/process, sin `lessons`. | Validado en `INC-20260829-r13-launcher-telemetry-regression`: `readAuditTrailTail` sancionado, corrupción/orden/límites/aislamiento y proyección real; la matriz cubre 5 casos ACC-075. | La lectura directa de `audit.log` no vuelve al launcher; el lifecycle se gestiona por Core. |
| ACC-076 | R-13.4 | agregar | validado | Añadir una matriz hermética de regresión que ejercite el servidor y wrappers efectivos para ACC-070..ACC-075, incluidos negativos y ausencia de mutación. | Validado en `Axiom/apps/cli/tests/r13-acc-076-matrix.test.ts`: 35 casos PASS, 0 FAIL, 0 TIMEOUT, 14 comprobaciones de no-mutación y un resumen exacto; la batería focal conjunta terminó con 12 suites y 233 tests PASS. | Typecheck, build, doctor, readiness y `git diff --check` también pasan; la evidencia lifecycle se gestiona mediante Core. |
| ACC-077 | R-13.5-A | modificar | validado | Definir un contrato real de descubrimiento e inventario global de versiones de Axiom y exponerlo en CLI y launcher. Existe un único CLI user-level compartido por todos los proyectos locales; actualizarlo actualiza el runtime ejecutable con el que operan todos, mientras `ManagedState.runtime.version` conserva únicamente la compatibilidad/migración project-scoped y no representa otra instalación del CLI. El modelo distinguirá de forma explícita: (1) `publishedVersion`, última release SemVer publicada en el repositorio Git upstream canónico de Axiom; (2) `downloadedVersion`, versión/tag/commit del checkout local de `Axiom/`; y (3) `installedVersion`, versión efectiva del único CLI user-level. Al iniciar, el launcher hará un `git fetch` best-effort y no bloqueante únicamente del remoto canónico configurado —sin checkout, merge, pull ni modificación del working tree—, detectará si las referencias descargadas anuncian una release nueva y mostrará estados tipados como actualizado, actualización disponible, checkout atrasado/adelantado, instalación desalineada, desconocido u offline. El cache offline conservará timestamp y procedencia sin presentarse como dato fresco. CLI y launcher ofrecerán además una operación explícita preview→confirmación que, en una acción separada del check, seleccione la release publicada, haga pull/checkout seguro de esa versión en el repo local y actualice el CLI global para todos los proyectos; nunca se aplicará automáticamente al arrancar. | Decisión expresa del usuario y evidencia de R-13.5-A. Hoy `self-update --check` solo relee el `package.json` más cercano a `@axiom/user-workspace`; no existe consulta de releases, canal, manifest ni fuente remota. `targetVersion` lo aporta el operador, `lastSeenVersion` no tiene productor, `install.json` no gobierna decisiones y el comparador contradice SemVer para prereleases/build metadata. Todos los packages están actualmente en `0.1.0` y son privados, sin política que mantenga la versión del CLI en lockstep. Se descarta instalar runtimes side-by-side por proyecto: el único binario user-level es la instalación operativa y los estados de migración de cada proyecto son un eje distinto. | La release autoritativa será una referencia SemVer publicada y verificable del remoto Git canónico, no la punta arbitraria de una rama ni un target libre aportado por el cliente. R-13.5-B debe fijar pull/checkout, working tree dirty/detached, fast-forward o tag, build/dependencias, verificación post-update, rollback y locking antes de implementar el apply. Coordinar `@axiom/user-workspace`, `@axiom/versioning`, `self-update`, launcher y la auditoría R-14. Validar checkout local atrasado/adelantado/dirty/detached, tag inválido o prerelease, remoto incorrecto, fetch fallido, cache stale y ausencia de red mediante repos Git locales herméticos. |
| ACC-078 | R-13.5-B | modificar | validado | Sustituir el falso apply actual por una actualización global Git transaccional, verificable y recuperable, separada de la mera instalación del shim. `self-update --check` solo descubrirá/inventariará según ACC-077; la operación de actualización resolverá su target desde una release SemVer ya obtenida del remoto canónico y no aceptará como autoridad una versión arbitraria aportada por el cliente. Preview y apply compartirán un plan inmutable. Apply adquirirá un lock user-level global; validará remoto, referencia publicada, checkout local, branch/tag y working tree; rechazará dirty/detached o divergencias no gobernadas sin stash, reset ni clobber implícitos; y registrará commit, versión efectiva, shim, manifest y estado previo. Después hará pull/avance fast-forward o preparará la release en staging, instalará dependencias determinísticamente desde lockfile, ejecutará el build completo y probará por ruta absoluta el nuevo CLI (`--version` exacta, `--help`, carga de módulos y smoke/doctor mínimo). Solo tras esa verificación activará o reconciliará el único shim/binlink global y persistirá en `install.json` la versión realmente observada. Un no-op, target ausente, build fallido, versión distinta o manifest no persistible nunca podrá devolver `applied`. | El apply vigente no hace fetch/pull/checkout, no recibe el target en `install-global.mjs`, no instala dependencias, no construye `dist/` y no verifica la versión posterior. En Windows busca el backup en `%USERPROFILE%/.axiom/.local/bin/axiom.cmd`, pero la instalación real está en `%USERPROFILE%/.local/bin/axiom.cmd`; el entorno auditado confirma que solo existe la segunda. En POSIX, encontrar cualquier `axiom` en PATH causa exit 0 sin relink. Un probe demostró que ese no-op se devuelve como `outcome:'applied'` y persiste `99.0.0`; otro demostró exit 0 aunque falle la escritura del manifest. Los smokes actuales solo comprueban fichero no vacío o `which axiom`, y las 48 pruebas verdes sustituyen el boundary por mocks o helpers aislados. | El rollback abarcará versión ejecutable completa —commit/release, dependencias, build, entry activo y manifest—, no solo el shim. Preferir staging + activación atómica; si se actualiza el checkout principal, exigir preflight limpio, conservar el commit anterior y demostrar restauración completa. Un helper externo realizará la actualización iniciada desde el launcher, emitirá progreso acotado y provocará cierre/reinicio explícito para no mezclar módulos viejos y nuevos. Unificar paths y helpers Windows/POSIX, distinguir `installed`/`unchanged`/`failed`/`recovery-required`, tratar timeout/signal/spawn error, usar temporales únicos y lock recuperable, y mantener `axiom upgrade` project-scoped separado. Cubrir con repos Git locales herméticos release anterior/nueva, dirty/detached/diverged, dos updaters, fallo en cada fase, no-op real, versión post-build distinta, rollback y reinicio del launcher, sin red externa. |
| ACC-079 | R-13.5-C | modificar | validado | Endurecer el contrato de estado y automatización de `self-update`. Migrar explícitamente `install.json` de v1 a un manifest v2 tratado como cache/receipt y nunca como autoridad: distinguir `publishedVersion`, `downloadedVersion` e `installedVersion`; conservar ref, commit y procedencia verificables; y separar `lastCheckAt`, `lastUpdateAttemptAt`, `lastUpdateSuccessAt` y estado de recuperación. Un manifest ausente no equivaldrá a una instalación real `0.0.0`; el schema será cerrado y validará SemVer, timestamps ISO, paths, refs y campos admitidos. La persistencia reutilizará un writer atómico común de `@axiom/core`, con lock user-level, temporales únicos, recuperación y protección frente a lost updates. Sustituir la precedencia implícita de flags por operaciones mutuamente exclusivas —preferiblemente `status`, `check`, `plan`, `apply` y `recover`, o validación estricta equivalente— y garantizar que `--dry-run` nunca alcance una ruta mutante. Adoptar un envelope JSON v1 uniforme como el de `projects`: stdout será siempre JSON parseable en modo `--json`, el diagnóstico humano irá a stderr, se usará `process.exitCode`, `noop` será éxito estructurado y error o `recovery-required` tendrá salida no cero. Status, preview y dry-run serán realmente read-only y no crearán `~/.axiom`. | R-13.5-C verificó que `install.json` solo lo consumen los flujos de registro de check/apply, no gobierna decisiones, acepta SemVer y timestamps inválidos, sintetiza `0.0.0` cuando falta, descarta `lastSeenVersion` y extensiones al reescribir y usa `install.json.tmp` fijo sin lock ni coordinación `load→mutate→save`. La CLI acepta flags incompatibles por precedencia: `--check` ignora apply/dry-run y `--target-version X --apply --dry-run` entra en apply, por lo que una simulación explícita puede alcanzar mutación. Probes del wrapper compilado mostraron errores con `--json` como texto humano en stdout y shapes/códigos variables; preview y dry-run crean el home user-level. Las 48 pruebas focales pasan, pero no cubren matriz completa de flags, envelopes/streams, códigos de salida, migración, concurrencia, corrupción ni ausencia de escrituras. | Complementa ACC-077 —inventario y autoridad de versiones— y ACC-078 —actualización Git transaccional— sin duplicarlas. Cubrir migración v1→v2 sobre una instalación existente, archivo ausente/truncado/schema futuro, temporal interrumpido, dos escritores y dos updaters, preservación de campos declarados, matriz exhaustiva de operaciones incompatibles, todos los outcomes JSON, stdout/stderr/exit code y no-mutación byte a byte de status/preview/dry-run. Reutilizar la primitiva segura de archivos locales de Core y coordinar el mismo contrato entre CLI y launcher. |
| ACC-080 | R-13.5-D | agregar | validado | Formalizar y certificar la entrega del único CLI global como release Git del monorepo completo, no como tarball aislado de `@axiom/cli`. Definir y hacer cumplir una convención única de referencia SemVer en el remoto canónico, commit exacto y versión autoritativa de producto; generar desde esa identidad `axiom --version`, build metadata, manifest instalado y estado mostrado por CLI/launcher, rechazando cualquier release si tag/ref, versión de producto, CLI compilado o commit no concuerdan. Declarar la compatibilidad Node/npm y construir siempre desde lockfile. Añadir un gate de release sobre clean clone que ejecute instalación determinista, build/typecheck, suites focales, `node --test scripts/install-global.test.mjs`, `axiom --version`, `axiom --help` y smoke/doctor mínimo. Certificar el recorrido con E2E herméticos sobre remoto Git local, dos releases, home y npm prefix temporales: discover/check/plan/apply, build y activación reales, versión posterior, shim/binlink efectivo, manifest/procedencia, no-op, fallos, rollback y `recovery-required`, paridad CLI/launcher y matriz Windows/POSIX. Integrar el instalador en la validación estándar y alinear después la documentación runtime y canónica: un solo CLI user-level, release Git, confirmación, JSON, recuperación, troubleshooting y una página `docs/cli/self-update.md` indexada; retirar el package npm y la TUI como rutas vigentes. | R-13.5-D verificó que el checkout no tiene tags locales (`git describe` devuelve `c6ad403`), no existen workflows `.github`, scripts o metadata de release ni gate tag↔versión, y Commander devuelve un literal `0.1.0`. Todos los workspaces consultados son privados y carecen de contrato de publicación. `npm pack --dry-run --json --ignore-scripts` produjo 674 entradas —102 fuentes, 151 tests y un tsbuildinfo—, cero dependencias bundled y ningún `scripts/install-global.mjs`; el CLI declara dependencias internas privadas `@axiom/*`, por lo que el tarball no es autocontenido. `dist/` no está versionado y el modelo real es checkout completo→`npm ci`→build→shim/npm link. Los tests de apply mockean el boundary, readiness no incluye self-update y `npm test` excluye `scripts/**`; la documentación afirma package disponible, TUI, apply/rollback real y preview puro en contradicción con el runtime auditado. | Complementa ACC-077 como productor/certificador de `publishedVersion`, ACC-078 como gate de artefacto y recorrido transaccional, y ACC-079 como contrato observable, sin reimplementar sus mecanismos. Mantener `private: true` mientras npm no sea un canal soportado; un package standalone requerirá otro incremento con bundle de dependencias, assets, instalador y prueba aislada. Las pruebas no tocarán el home/PATH real ni dependerán de red externa para Git; usarán repos bare, homes/prefixes y shims temporales, fault injection y ejecución del binario por ruta absoluta. No afirmar ausencia de tags remotos sin fetch: el gate deberá obtener y validar las refs del remoto canónico. |
| ACC-081 | R-14 | eliminar | validado | Retirar físicamente el paquete residual `@axiom/tui`, que ya no es una superficie operativa. Eliminar `Axiom/packages/tui/` completo (`package.json`, `tsconfig.json` y el módulo huérfano `src/flows/preview.ts`), sacar el workspace del árbol de `packages/*` y del `package-lock.json` regenerado, retirar el junction `node_modules/@axiom/tui` y comprobar que no queda alias, project reference, dependencia declarada ni import activo. Reemplazar la aserción indirecta de `apps/cli/tests/tui-retirement.test.ts` —que hoy pasa porque `packages/tui/dist/` no existe— por una comprobación positiva de ausencia del paquete y del subcomando. Conservar `docs/cli/tui.md` como página explícitamente histórica y no reintroducir lógica: las previews vigentes son las del launcher. | Petición expresa del usuario. ACC-005 quedó `validado` retirando el registro de `axiom tui`, el driver, pantallas, flujos y pruebas; `packages/tui/src/index.ts` fue borrado en `c4df64c`. Pero `git ls-files` sigue devolviendo `packages/tui/package.json`, `packages/tui/tsconfig.json` y `packages/tui/src/flows/preview.ts` (último commit `ff25c77`, posterior al lote de retirada); `node_modules/@axiom/tui` existe como junction al paquete y `package-lock.json` conserva su entrada de workspace con 8 dependencias internas. El paquete no está en las project references de `tsconfig.json` ni tiene alias en `vitest.config.ts`, así que hoy no se compila ni se testea: es residuo inerte, no una superficie viva. | ACC-005 es el antecedente y no debe reabrirse: esta acción cierra su residuo físico. ACC-080 ya cubre la retirada documental de la TUI como ruta vigente; no duplicar esa parte. Regenerar el lockfile es el único efecto colateral esperado sobre el árbol de dependencias; verificar que `npm run build`, `npm test`, doctor y readiness siguen en verde después de la retirada. |
| ACC-082 | R-14/R-13 | agregar | validado | Añadir al launcher las dos pantallas de operador que hoy no existen: (1) model routing, que muestre política base, slots, override efectivo por slot y nivel de soporte real del target, y permita `set`/`unset`/`reset` y `validate` con preview y confirmación; y (2) actualización de Axiom, que muestre versión publicada, descargada e instalada con su relación tipada y frescura/procedencia del cache, y permita `check`, `plan`, `apply`, `recover` y `cancel` con progreso acotado y cierre/reinicio explícito. Ambas pantallas deben delegar en los comandos y endpoints canónicos existentes sin duplicar lógica ni reimplementar SemVer, Git o mutaciones de fichero, y honrar los gates ya vigentes del control plane (sesión, same-origin, grant single-use con TTL y binding de proyecto/acción/payload). El estado de soporte por target debe presentarse con honestidad: donde el routing por slot no se obedece, la pantalla debe decirlo en lugar de sugerir un control efectivo. | Petición expresa del usuario. Se verificó que `apps/cli/src/commands/app-self-update.ts` implementa el adaptador headless y `app-api.ts` importa y enruta las 8 operaciones (`status`, `check`, `plan`, `apply`, `recoverPlan`, `recover`, `cancel`, `getOperation`), con 11 tests en `apps/cli/tests/app-self-update.test.ts`. En cambio, `apps/cli/static/launcher/{index.html,launcher.js,panels.js,transport.js}` no contiene ninguna superficie de self-update ni de model-routing: los paneles reales son ADO suggestions, rama de rol Git, workflow ADO y telemetría. `axiom model` no tiene endpoint ni pantalla; solo existe como CLI. Tras la retirada de la TUI el launcher es la única interfaz guiada, así que estos dos mandos quedan hoy sin ventanilla. | Depende de ACC-070..ACC-076 (control plane, transportes, grants, catálogo y matriz de regresión) y reutiliza sus gates sin redefinirlos. La pantalla de actualización consume el contrato de ACC-077..ACC-080 y el resultado de ACC-083: no debe cablearse contra la generación de self-update que quede descartada. El eje de model routing debe respetar el `SUPPORT_MATRIX` de `@axiom/model-routing` como fuente de verdad del soporte real. La cobertura DOM del launcher sigue siendo un riesgo residual conocido; conviene extender la matriz hermética existente en lugar de crear una paralela. |
| ACC-083 | R-14 | investigar | validado | Analizar y decidir cuál de las dos implementaciones de self-update se conserva y eliminar la otra por completo, incluidos sus tests, tipos, helpers, referencias y documentación asociada. El análisis debe determinar si `apps/cli/src/commands/self-update.ts` aporta todavía algo no cubierto por `self-update-contract.ts` —por ejemplo la instalación inicial del shim vía `scripts/install-global.mjs`, que es un caso distinto de la actualización de una instalación ya gestionada— y, si aporta, separar explícitamente «instalar» de «actualizar» en lugar de mantener dos rutas de actualización solapadas. El resultado debe dejar una sola ruta alcanzable desde la CLI, sin código muerto sostenido por sus propias pruebas, y sin perder la cobertura de los escenarios que hoy solo verifica la implementación antigua. | Petición expresa del usuario. Se verificó que `apps/cli/src/index.ts` solo invoca `registerSelfUpdateContract(program)`; `registerSelfUpdate` está exportado en `self-update.ts` (27,7 KB) pero no lo importa ningún módulo de producción: el único consumidor es `apps/cli/tests/self-update.test.ts`, con 16 tests que pasan. Ambas generaciones convivieron desde el commit que introdujo el motor transaccional (`feat(cli): add transactional self-update engine and dedicated spec-repo workspace support`, 2026-09-10), es decir la antigua no se retiró al aterrizar la nueva. La documentación activa `docs/cli/self-update.md` describe únicamente el contrato nuevo, por lo que la implementación antigua tampoco está documentada como vigente. | Depende del contrato cerrado por ACC-077..ACC-080; no debe reabrirlo ni reimplementarlo. Coordinar con ACC-082 para que la pantalla del launcher se cablee contra la ruta superviviente. Antes de borrar, comprobar si `scripts/install-global.mjs`, `findInstallGlobal`/`runInstallGlobal`, el backup/restore del shim y `node --test scripts/install-global.test.mjs` (18/18) siguen siendo el camino de instalación inicial soportado; si lo son, conservarlos con un nombre y una superficie que no se confundan con la actualización. |
| ACC-084 | R-14/R-01 | modificar | validado | Sanear los artefactos compilados versionados y cerrar el hueco de exclusión que los permite. Retirar del control de versiones los 19 artefactos que viven dentro de carpetas `src/` (`packages/model-routing/src/{assignments,mutate,slots,types}.{js,js.map,d.ts,d.ts.map}` y `apps/cli/src/commands/{mcp,repair,toolchain}.d.ts.map`), decidir explícitamente el destino de los 148 ficheros versionados bajo `packages/adapters/*/dist/`, y extender `.gitignore` para cubrir salidas anidadas (`packages/*/*/dist/`) y cualquier `.js`/`.d.ts`/`.map` emitido dentro de `src/`. Añadir una comprobación automática que falle si vuelven a aparecer artefactos compilados versionados, de modo que la higiene no dependa de una inspección manual. | Petición expresa del usuario. `git ls-files` devuelve exactamente 19 artefactos compilados bajo rutas `src/` y 148 bajo `packages/adapters/*/dist/`. El `.gitignore` actual solo declara `packages/*/dist/`, `apps/*/dist/`, `packages/*/*.tsbuildinfo` y `apps/*/*.tsbuildinfo`, por lo que los `dist/` de adapters —que están un nivel más abajo, en `packages/adapters/<target>/dist/`— quedan fuera de la exclusión, igual que cualquier emisión dentro de `src/`. `packages/model-routing/src/types.js` comparte commit con su `.ts` (`17416d2`), así que hoy no hay evidencia de que estén desincronizados; el problema es de higiene y de ambigüedad de fuente, no una divergencia demostrada. | Riesgo verificado a resolver antes de tocar los adapters: `TC-009` (`runAdapterRuntimeCoverageCheck`) exige `src/generator.ts` y `dist/index.js` presentes para los 8 packages `@axiom/adapters-*` y falla si alguno no está; si se dejan de versionar esos `dist/`, un clone limpio sin build previo haría fallar el check. La acción debe decidir entre construir antes de doctor, relajar el check con una señal honesta o conservar deliberadamente esos artefactos, y documentar la elección. Coordinar con ACC-004 (normalización de salidas de `@axiom/cli-commands`) para no reintroducir raíces mezcladas. |
| ACC-085 | R-14 | modificar | validado | Sustituir la versión de runtime escrita a mano por una identidad derivada de una única fuente. `packages/versioning/src/version.ts` debe dejar de exportar el literal `RUNTIME_VERSION = '0.1.0'` y obtener la versión de la misma identidad de producto que ya gobierna la release del CLI (tag/ref, commit y versión efectiva materializados en la metadata de build), de forma que `ManagedState.runtime.version`, el `--target-version` por defecto de `axiom upgrade`, `axiom --version` y el manifest instalado no puedan divergir. La derivación debe ser determinista y funcionar en test y bajo bundling sin leer `package.json` en caliente, y debe fallar de forma explícita si la identidad no está disponible en lugar de inventar un valor por defecto. | Petición expresa del usuario. `packages/versioning/src/version.ts` declara `RUNTIME_VERSION = '0.1.0'` con un comentario que reconoce el problema («la mantenemos acá para evitar leer el `package.json` en runtime… una iteración futura puede sincronizarla con `package.json` vía un build step»). Esa constante es la fuente del `runtime.version` persistido en el `ManagedState` y el default de `--target-version` en `axiom upgrade`. En paralelo, `apps/cli/src/generated-build-metadata.ts` ya es generado por `scripts/generate-cli-build-metadata.mjs` y expone `version`, `releaseTag`, `commit`, `buildId` y `dirty`: existe ya un productor de identidad, pero el paquete de versionado no lo consume. | Complementa ACC-080, que fijó la identidad de release del único CLI global, y no debe reimplementar su convención de tags ni su gate. Distinguir con claridad los dos ejes que R-13.5-A ya separó: la versión del CLI instalado y la versión de compatibilidad/migración project-scoped del `ManagedState`; unificar la fuente no significa colapsar los dos conceptos. Comprobar el efecto sobre las migraciones existentes, sobre el default de `--target-version` y sobre los estados ya persistidos con `0.1.0`, y prever el caso de ejecución desde un checkout sin release publicada. |
| ACC-086 | R-14 | modificar | validado | Cerrar el hueco de enrutado de modelos en los destinos que no lo obedecen mediante recomendación explícita y aviso dependiente del adapter, en lugar de ofrecer un mando sin efecto. Cuando el destino seleccionado no enruta modelo por slot, la superficie debe mostrar el **modelo recomendado** para ese slot —derivado de la política y de los overrides vigentes— y declarar que la selección es **manual**: el usuario debe elegirlo a mano en su herramienta. Al copiar el prompt, la salida debe incluir un aviso dependiente del adapter seleccionado que indique el modelo recomendado y que hay que seleccionarlo manualmente; en los destinos que sí enrutan, el aviso no debe aparecer. El nivel de soporte por destino se leerá del `SUPPORT_MATRIX` como única fuente de verdad y el aviso deberá diferenciar los tres casos reales (routing por slot, routing global y sin routing). La limitación declarada hoy en la política (`whenPerSubagentRoutingUnsupported: use-default-and-record-limitation`) debe quedar efectivamente registrada y visible, no solo declarada. | Decisión expresa del usuario ante la duda abierta de R-14. Verificado: `SUPPORT_MATRIX` declara `opencode` como `multi-mode`, `claude-code` como `single-mode` y `github-copilot`, `vscode`, `cursor`, `codex`, `antigravity` y `visual-studio-2026` como `fallback-only`; en esos seis la elección de `axiom model set` no tiene efecto sobre la herramienta. `axiom.config/model-routing-policy.yaml` declara `fallback: whenPerSubagentRoutingUnsupported: use-default-and-record-limitation`, pero no se verificó ninguna superficie donde esa limitación quede registrada o se muestre al usuario. La construcción del prompt del launcher (`packages/launcher/src/prompt-builder.ts`) y la tabla por adapter (`adapter-routing.ts`) ya varían la cabecera/mención y el verbo de lanzamiento por destino manteniendo el cuerpo idéntico, por lo que existe el punto de inserción natural del aviso sin duplicar lógica. | Precede y condiciona ACC-082: la pantalla de model routing debe mostrar la recomendación y el carácter manual de la selección con este mismo contrato, sin inventar un segundo criterio de soporte. El aviso debe alcanzar toda copia de prompt, no solo la pantalla de modelos. No convierte `fallback-only` en routing real ni promete comportamiento del IDE: es una recomendación honesta más una acción manual del usuario. Verificar el comportamiento en los tres niveles de soporte y que el snapshot por adapter siga demostrando cuerpo idéntico y variación solo en cabecera/verbo/aviso. |
| ACC-087 | R-14/R-10 | modificar | validado | Permitir varios artefactos en vuelo y selección explícita del que se opera, eliminando el bloqueo por instancia única del estado de workflow. Sustituir el registro único por `workflowId` de `workflow-state.json` por un estado por instancia identificada (`workflowId` + `metadataId`), de modo que crear un segundo incremento no re-ligue ni bloquee a los anteriores. Añadir un comando soportado de selección/activación explícita del artefacto sobre el que se va a operar, y sustituir el rechazo actual por `ID mismatch` por un error accionable que indique cómo seleccionar. Extender `axiom state` para listar todas las instancias en vuelo con su estado y su siguiente paso recomendado, sin dejar de ser read-only. Migrar el schema existente de forma determinista, atómica e idempotente, sin perder el registro vigente ni los receipts, y mantener fail-closed todo lo demás: ninguna transición debe volverse legal por el cambio, ninguna instancia debe heredar estado o `vars` de otra y no debe existir re-ligado implícito. El alcance cubre los workflows con transición identificada por artefacto (`increment`, `bug`, `plan`); los carriles `qa-e2e` y `role` conservan su semántica actual. | Petición expresa del usuario y hallazgo verificado el 2026-09-11. `packages/workflow/src/state-store.ts` documenta y persiste un único record por `workflowId` en `.axiom-state/<projectKey>/workflow-state.json` con `schemaVersion: 1`. `runGovernedTransition` ya resuelve el estado por artefacto cuando el id pedido no coincide con el ligado (sintetiza desde `metadata.yml#status` o desde `initialState`), pero el wrapper CLI de `apps/cli/src/commands/axiom-increment.ts` rechaza antes cualquier subcomando distinto de `create` con `ID mismatch: se recibió 'A', pero workflow-state.json está ligado a 'B'` mientras el estado ligado no sea terminal. Probado en vivo: `create` solo es legal desde `draft`, así que re-ligar un artefacto ya en `specifying` se rechaza con `No transition for command 'increment-create' from state 'specifying'`, y `axiom state` es solo un inspector. `reconcileGovernedWorkflowState` existe pero únicamente lo consume `axiom-role`. Consecuencia operativa demostrada: al crear varios incrementos queda ligado el último y los anteriores no pueden avanzar hasta que el ligado alcance estado terminal. | Es la primera pieza del lote R-14 y desbloquea el resto: se ejecuta antes que los demás incrementos precisamente porque el estado queda ligado al último artefacto creado. Coordinar `@axiom/workflow` (`state-store`, `governed-transition-runner`, `recommend-next`), los wrappers `axiom-increment`, `axiom-bug`, `axiom-plan`, `axiom-qa-e2e`, `axiom-role`, `state-cmd`, el ciclo SDD del launcher y `validate-changes`. Cubrir con pruebas: dos y tres instancias simultáneas, selección explícita, id inexistente, instancia terminal, migración desde `schemaVersion: 1` con y sin record previo, ausencia de herencia de `vars`, y que ninguna transición ilegal pase a ser legal. No convertir el cambio en una vía para editar estado a mano ni para saltarse gates de aprobación. |
| ACC-088 | R-15 | agregar | validado | Convertir la verificación documental en un paso obligatorio y comprobable del cierre, en lugar de una obligación redactada en prosa. Al final del archivado/integración de un incremento o bug debe existir una verificación explícita que recorra toda la documentación afectada: spec canónica numerada, manuales, contexto técnico, decisiones, planes y READMEs de runtime y de paquetes. La verificación debe declarar qué documento se revisó, qué se actualizó y qué quedó sin cambios con motivo explícito, y debe fallar o advertir de forma accionable cuando la documentación afectada no fue revisada. Debe ser ejecutable como comando o gate, no solo una instrucción de skill, y no debe abrir un segundo camino de archivado paralelo al gobernado. | Verificado el 2026-09-12. El cierre documental hoy solo existe como prosa en dos skills del catálogo: `axiom-role-close-doc` (contexto técnico por rol, que se declara explícitamente incompetente sobre la spec funcional) y `axiom-spec-integrator` (conocimiento durable hacia la spec canónica y archivado). El camino ejecutable no comprueba documentación: `apps/cli/src/commands/integrate.ts` selecciona la transición declarada de archive, fija `status: archived` e `integration.status: 'integrated'`, mueve la carpeta a `_archive/` y emite receipts; `axiom.config/workflows.yaml` declara como efecto único el `status` en `metadata.yml` con `requiresApproval: true`; `validate-changes` valida write-scope y legalidad de transición, no frescura documental. Ninguna prueba, check de doctor ni script del runtime valida `Axiom/docs/**`: la única prosa con gate real es el material de catálogo que `TC-010`/`TC-011` verifican por source y hash. | Habilita la permanencia de ACC-089 a ACC-094: sin este paso, cualquier corrección documental vuelve a desalinearse. Debe coordinarse con ACC-041, ACC-043 y ACC-045 para reutilizar el ejecutor común de transiciones gobernadas y el contrato único de QA en vez de duplicarlos. No sustituye la investigación puntual de ACC-044. |
| ACC-089 | R-15 | modificar | validado | Convertir `Axiom/docs/**` en el único manual de producto de Axiom, completo y veraz. Decisión explícita del usuario el 2026-09-12: no hay fusión de sets. El conjunto del repositorio canónico es material interno de esta instalación, no se conserva como fuente ni se migra, y desaparecerá cuando desaparezca `Axiom.Spec`; el conjunto del runtime es el que se amplía y se corrige. El manual debe cubrir todas las familias de comandos registradas explicando para cada una qué es, para qué sirve, cómo se usa, qué archivos toca y con qué se conecta, con contenido verificado contra el comportamiento vigente, y debe servir por igual a una persona que necesita entender Axiom y a un agente que lo lee para operarlo. Toda referencia de consumo y de actualización debe apuntar a ese destino único; ningún otro lugar puede alojar un set paralelo. La cobertura faltante identificada incluye el ciclo SDD completo, el launcher, memoria, toolchain, MCP, index, scaffold, normalize, integrate, bootstrap, eject, repair, workspace, rollback, bindings, topology, roles, member-install, external-sync, state, phase y freeze. La distribución del manual a los proyectos se separa en ACC-095 y no bloquea esta acción. | Verificado el 2026-09-12. `apps/cli/src/index.ts` registra 47 familias de comandos; `Axiom/docs/cli/` tiene manual propio para unas 13 y su índice lista una docena con numeración rota que salta del 8 al 10. `Axiom.Spec/specs/manuales/` aporta 15 manuales que sí cubren el ciclo SDD, el launcher y el plugin de tracker, con otro dueño y otro estilo. Los dos sets se solapan en configuración y ciclo de vida y ninguno tiene validación automática. Verificado además que hoy ningún manual viaja al proyecto adoptante: el inventario de `workspace-setup` siembra en el repositorio de especificación `specs/README.md`, los nueve documentos `00..08`, `context/TECHNICAL_CONTEXT.md`, `context/README.md`, las carpetas `specs/{increments,bugs,archive}` y `context/{architecture,integrations,operations,references}`, los YAML de `axiom.config`, `axiom.skills.lock` y `.axiom/mcp.yml`, sin `docs/**` ni `templates/`; `docs/generated-files.md` confirma que lo que recibe un proyecto por target son ficheros de instrucciones y estado, no manuales. `Axiom.Spec/specs/manuales/README.md` se declara además curado para esta instalación concreta, con el plugin de Azure DevOps ya configurado. | Define el destino único que necesitan ACC-092 y ACC-093 y precede a ACC-095, que distribuye el resultado; ACC-088 evita que la cobertura vuelva a quedar incompleta. El destino queda dentro de `Axiom/`, por lo que no reabre la frontera de ADR-0032: el conjunto canónico se deja expirar con `Axiom.Spec` en lugar de migrarse. |
| ACC-090 | R-15 | eliminar | validado | Eliminar la duplicación de plantillas y dejar una sola fuente. Decisión explícita del usuario el 2026-09-12: la fuente única es la copia del runtime `Axiom/axiom.spec/templates/`, por ser la más reciente y la única con vocabulario vigente, y el contrato debe declararla como la buena en lugar de seguir presentando `Axiom.Spec/templates/` como canónica. Antes de retirar la copia del repositorio canónico hay que comprobar plantilla por plantilla si conserva contenido útil ausente en la del runtime e incorporarlo; el resto se elimina. Código, adapters, workflow, tests y documentación deben apuntar a la fuente elegida, el override present-file-wins de los adapters queda alineado con ella y el test dorado debe comparar la fuente completa, no solo títulos de sección de unos pocos archivos. La revisión de qué plantillas sobran se aplaza al momento de instalar Axiom sobre Axiom, cuando se pueda observar cuáles se usan de verdad; esta acción no retira ninguna por sospecha. | Validado en `INC-20260912-r15-template-single-source`: 45/45 plantillas comparadas, exactamente 7 divergencias documentadas, `Axiom.Spec/templates/` retirado, seis plantillas sin consumidor conservadas, vocabulario retirado ausente, golden test completo y generación de los cuatro adapters sin regresión. Las referencias activas a la copia antigua fueron reconciliadas también en `specs/03` y `specs/04`. La revisión independiente final fue OK; typecheck, build, doctor, readiness y diff check pasan. La suite global `npm test` no concluyó por repetición en `workspace-step-reconciliation.test.ts` y queda registrada como advertencia, sin invalidar la evidencia focal 97/97. | La elección de copia ya está decidida y no queda pendiente. Retirar `Axiom.Spec/templates/` cambia la estructura declarada en `Axiom.Spec/README.md`, alineada en ACC-002, y debe reconciliarse con la frontera de ADR-0032; el repositorio canónico deja de alojar plantillas. Seis plantillas del conjunto no las consume ningún fuente del runtime (`getting-started-template.md`, `troubleshooting-template.md`, `onboarding-member-template.md`, `discovery-provider-overview-template.md`, `artifact-baseline-matrix.md`, `agent-log-template.md`), verificado sobre 440 archivos de `packages/*/src` y `apps/*/src`: esa decisión queda aplazada a la instalación de Axiom sobre Axiom. Duda de nomenclatura registrada el 2026-09-12: la carpeta `axiom.spec/` dentro del runtime no es una instalación simulada sino el default vigente del producto. `packages/workflow/src/artifact-store.ts` declara `DEFAULT_SPEC_REL_PATH = 'axiom.spec'` como default histórico de proyección de artefactos, e `inferInitLayout` en `apps/cli/src/commands/init.ts` usa `axiom.spec/target-axiom-skills` y `axiom.spec/target-axiom-agents` como marcadores que clasifican el repositorio como `self-hosted`. En paralelo, `defaultAxiomRepoSiblingPath` en `workspace-adopt.ts` crea el repositorio autoral como `../<projectName>.axiom` y documenta que espeja la convención anterior `../<projectName>.spec`, la que da nombre a `Axiom.Spec`. Quedan por tanto tres nombres derivados de axiom y spec con tres significados distintos; la coherencia de nomenclatura no se resuelve en esta acción. ACC-088 mantiene la fuente única al día. |
| ACC-091 | R-15 | documentar | validado | Corregir `Axiom/README.md` para que su información sea correcta y vigente: separar de forma inequívoca la foto histórica del estado actual, actualizar las afirmaciones superadas, alinear el inventario de comandos y paquetes con el runtime real y eliminar los enlaces muertos. | Reconciliado el 2026-09-14 contra el árbol: 0 menciones a `openspec` en `README.md` y `docs/README.md`; la afirmación sobre `dist/` materializado ya no aparece. Ejecutado por `INC-20260912-r15-docs-truth-baseline` (archivado). Los enlaces a `../Axiom.Spec/decisions/0019…` y `0031…` siguen apuntando a la raíz antigua de decisiones tras ACC-094 y quedan cubiertos por el gate documental de ACC-088. | Ninguna. |
| ACC-092 | R-15 | documentar | validado | Resolver los manuales que describen archivos inexistentes y cubrir los archivos reales sin manual. Para `docs/configuration/files/onboarding.md`, `scaffolding-contract.md`, `command-protocol.md` y `local-overlay-policy.md`: retirar la información o marcarla de forma inequívoca como contrato histórico no materializado, también en el índice que los lista. Documentar los YAML vigentes que hoy no tienen manual propio. | Reconciliado el 2026-09-14 contra el árbol: los cuatro manuales declaran «Histórico: … (no materializado)» en su primera línea y los cinco YAML vigentes (`agents-catalog`, `skills-catalog`, `toolchain-catalog`, `mcp-manifest`, `workflows`) tienen manual propio. Ejecutado por `INC-20260912-r15-docs-truth-baseline` (archivado). | Ninguna. |
| ACC-093 | R-15 | documentar | validado | Corregir los índices de documentación para que reflejen lo que ya pasó a histórico y lo que existe realmente, incluyendo los nombres de archivo que conservan vocabulario retirado. | Reconciliado el 2026-09-14 contra el árbol: `docs/configuration/profiles-overlays-targets.md` fue renombrado a `configuration-policy-targets.md`; `docs/README.md` menciona `docs/cli/tui.md` (2 referencias históricas). Ejecutado por `INC-20260912-r15-docs-truth-baseline` (archivado). | Ninguna. |
| ACC-094 | R-15 | eliminar | validado | Dejar un único formato y una única raíz de decisiones en el repositorio canónico y limpiar solo lo que ninguna operación usa. Alcance acotado tras la verificación del 2026-09-12: unificar los nueve documentos numerados a mano de `Axiom.Spec/decisions/` con los artefactos gestionados de `specs/decisions/`, resolver el choque de numeración entre `decisions/0032-axiom-spec-boundary-and-runtime-baseline.md` y `specs/adr/ADR-0032-toolchain-versioning/`, retirar las raíces legacy `increments/` y `bugs/` de primer nivel, y eliminar la carpeta vacía `Axiom.Spec/axiom.spec/`. Decisión explícita del usuario el 2026-09-12: la copia suelta `increments/INC-20260817-r10-acc038-lifecycle-docs` se borra, una vez comprobado que su artefacto real completó el ciclo y está archivado con receipts bajo `specs/increments/_archive/`. Las raíces activas `specs/increments/`, `specs/bugs/` y `specs/archive/` se conservan: son las que usan los comandos y las que reciben el histórico al archivar, así que quedarse vacías es su estado normal. Toda transición de artefacto gestionado debe ejecutarse con comandos Axiom: no se editan IDs, estados ni índices a mano y no se borra historia con receipts sin decisión explícita registrada. | Verificado el 2026-09-12. Conviven dos formatos de decisión: nueve documentos numerados a mano en `decisions/` y siete artefactos `DEC-*` con `metadata.yml` en `specs/decisions/`, más un ADR gestionado en `specs/adr/` cuyo número 0032 choca con un documento distinto también numerado 0032 en `decisions/`. Las raíces de primer nivel no son las que usa el runtime: `resolveSpecArtifactRelPath` devuelve `specs` para esta autoridad schema-2, y la transición de archive mueve la carpeta dentro de esa misma raíz a `_archive/`. La copia suelta `increments/INC-20260817-r10-acc038-lifecycle-docs` tiene metadata gestionada con `status: specifying`, sin receipts y con `links.planId: null`, y fue commiteada el 2026-08-20 en `4661172` durante R-10; el artefacto real de ACC-038 sí completó su ciclo y vive archivado en `specs/increments/_archive/INC-20260817-r10-acc038-lifecycle-docs`, por lo que retirar la copia suelta no pierde historia. Esa copia además es invisible para la validación: `index-cmd` escanea solo la raíz resuelta, lo que explica que R-14 reportara 21 metadatas válidos cuando el repositorio tiene 22 fuera de `_archive`. `Axiom.Spec/README.md` sigue afirmando que la raíz `increments/` está vacía. La raíz `bugs/` conserva dos READMEs legacy sin metadata y `Axiom.Spec/axiom.spec/` es una carpeta sin ningún archivo, con el nombre que el artifact-store usa como proyección por defecto. | Se solapa parcialmente con ACC-035 y ACC-037, que cubren residuos bajo `specs/increments/INC-20260809-*`: conviene ejecutarlas juntas o declarar la frontera entre ambas. Tras la limpieza hay que reconciliar la estructura declarada en `Axiom.Spec/README.md`, alineada en ACC-002. |
| ACC-095 | R-15 | agregar | validado | Distribuir el manual de producto a los proyectos que instalan o actualizan Axiom. Petición explícita del usuario el 2026-09-12: el manual único que resulte de ACC-089 debe materializarse en el proyecto adoptante para que el equipo pueda leer allí qué es cada comando y cómo se usa, y para que un agente que lo lea entienda Axiom sin salir del repositorio. La acción debe definir el destino dentro del proyecto, el momento en que se escribe (instalación, adopción y actualización), la idempotencia frente a ediciones locales y la política de sobrescritura, distinguiendo el manual genérico de producto, que sí se distribuye, del material específico de una instalación concreta, que no. | Reconciliado el 2026-09-14 contra el código: `workspace-setup.ts` importa `distributeManual` de `@axiom/document-bootstrap` y ejecuta el paso derivado `manual-distribution` dentro de la transacción estructural; el mecanismo vive en `packages/document-bootstrap/src/manual-distribution.ts`. Ejecutado por `INC-20260912-r15-manual-distribution` (archivado). | Ninguna. |
| ACC-096 | R-16 | modificar | propuesto | Reducir drásticamente el tiempo de ejecución de la suite completa de Vitest, que hoy tarda entre 500 y 1020 s según la carga, con una cola de archivos extremadamente lentos dominada por I/O de filesystem y procesos reales. Evidencia medida en la pasada certificada del 2026-09-13 (898 s, 358 archivos): `packages/core/tests/self-update.test.ts` 579 s (44 tests), `apps/cli/tests/workspace-incremental.test.ts` 498 s (42), `apps/cli/tests/docs-command-coverage.test.ts` 275 s (5), `apps/cli/tests/workspace-command.test.ts` 198 s (19), `apps/cli/tests/self-update-status.test.ts` 178 s (28), `workspace-structural-transaction` 75 s, `workspace-adopt` 72 s, `workspace-step-reconciliation` 65 s. La acción debe: (1) diagnosticar por causa raíz los tres peores archivos (self-update, workspace-incremental, docs-command-coverage) distinguiendo I/O real necesario de espera evitable (polling, sleeps, spawns redundantes, rebuilds por test); (2) introducir compartición de fixtures/builds entre tests del mismo archivo donde sea seguro; (3) evaluar `pool: 'forks'` con `isolate: false` o `fileParallelism` ajustado para el perfil I/O-heavy, sin ocultar fugas; (4) definir un presupuesto objetivo medible (por ejemplo, suite completa bajo 300 s en la máquina de referencia) y un gate que avise si se supera; (5) no ampliar timeouts para enmascarar lentitud (restricción ya vigente del Grupo G) ni debilitar aserciones. | Petición expresa del usuario el 2026-09-14 tras certificar la suite en R-16. La pasada limpia con `--minWorkers=1 --maxWorkers=4` tardó 898 s; con interferencia llegó a 1020 s. Los tres peores archivos concentran más de 1350 s de CPU de test en 91 tests, y `docs-command-coverage` gasta 275 s en 5 tests (55 s por test) reconstruyendo el programa Commander y escaneando documentación. | No romper la certificación vigente (358/358, 3765/3765). Coordinar con la restricción del Grupo G (no ocultar fugas con timeouts) y con `testTimeout: 30000` vigente en `vitest.config.ts`. Medir antes y después con la misma metodología (pasada limpia, workers limitados) para que la comparación sea válida. |

## Cierre técnico R-13.4 — Launcher (2026-09-08)

**R-13.4 queda validado técnicamente para ACC-070..ACC-076.** La implementación y la evidencia cubren el control plane HTTP/SSE, transportes configurados, confirmación server-side, catálogo/routing, ADO local-first, telemetría bounded y la matriz hermética de regresión. La revisión independiente posterior a la matriz confirmó el cumplimiento funcional de ACC-070..ACC-076 y no encontró blockers de comportamiento.

La validación final observada terminó con **12 suites y 233 tests PASS**; la matriz informa **35 casos PASS, 0 FAIL, 0 TIMEOUT** y **14 comprobaciones de no-mutación**. `npm run typecheck`, `npm run build`, `npm run doctor` (48/61 OK, 0 fallos, 2 advertencias, 11 omitidos), `npm run readiness:first-project` y `git diff --check` pasan. Las advertencias/omitidos de Doctor son diagnósticos informativos y no sustituyen la autorización por grant.

La provenance se mantiene explícita y reproducible a nivel de worktree: los candidate freezes y phase receipts los genera Core; la matriz fija el conjunto ejecutado; y el inventario de rutas compartidas reconoce que varias superficies pertenecen a más de una lane R-13. No se crea commit ni se ejecuta Git mutante. Los tres incrementos R-13 y su estado lifecycle se gobiernan por sus artifacts y comandos Core, sin duplicar metadata, receipts o índices en este plan.

La integración estable de los contratos se conserva en `Axiom.Spec/specs/00..08` y `context/**`; las afirmaciones de esta sección describen únicamente comportamiento y evidencia ya observados.

## Cierre técnico R-11 — ACC-046..ACC-049 (2026-08-20)

R-11 valida cuatro correcciones independientes: broker MCP único y project-scoped (ACC-046), alcance probatorio del auditor (ACC-047), intercambio de memoria confirm-gated y reintentable (ACC-048), y selección de contexto por etiquetas explícitas y deterministas (ACC-049). La evidencia focal es 23 archivos/165 tests para ACC-046, 18/18 para ACC-048 y 8 archivos/96/96 para ACC-049; `npm run build` y `git diff --check` completan la verificación del lote. La integración canónica única reconcilia `specs/00`, `03`, `04`, `05`, `06` y `08`; `01`, `02` y `07` fueron revisadas sin cambios necesarios. El contexto técnico estable se actualiza en `context/integrations/01-capabilities-providers-y-toolchain.md`; el índice se regenera por Core, nunca a mano.

Tipos permitidos: `eliminar`, `modificar`, `agregar`, `investigar`, `documentar` y `riesgo`. Estados permitidos: `propuesto`, `validado`, `descartado` y `pospuesto`.

## Ejecución y cierre del lote ACC-004..ACC-008

El lote se ejecutó en este orden de dependencia: `ACC-004` -> `ACC-006` ->
`ACC-007` -> `ACC-008` -> `ACC-005`. La TUI no se retiró hasta completar y
validar la paridad del launcher y las superficies CLI/MCP. Las cinco acciones
quedan `validado`; sus incrementos están `closed`, con metadata `archived`,
integración canónica `integrated`, review independiente resuelta y carpetas
físicamente archivadas en `specs/increments/_archive/`.

Evidencia consolidada del lote:

- `npm run build` pasa; `npm run doctor` devuelve PASS (46/61 OK, 0 fallos,
   3 warnings y 12 omitidos); `npm run readiness:first-project` pasa.
- ACC-004: consumidores focalizados 31 archivos/402 tests y CLI completa
   133 archivos/1268 tests; entrypoint `dist/index.js` y declaración
   `dist/index.d.ts` resolubles.
- ACC-006: batería final 7 archivos/90 tests; preview/dry-run, ownership,
   solapamiento, adopción parcial, provenance e idempotencia cubiertos.
- ACC-007: batería focalizada 12 archivos/104 tests, más E2E de launcher y
   regresiones de worktree/archive; rollback inconsistente señalizado.
- ACC-008: batería focalizada 20 archivos/178 tests y E2E; DTO explícitas,
   allowlist de handlers, validación estricta, redacción y `NullTracker`
   local-only verificados.
- ACC-005: batería final dirigida 23 archivos/221 tests, regresiones directas
   de `axiom tui` y de la invocación sin subcomando, dist stale limpiado y
   claims activos de TUI reconciliados como launcher/CLI/MCP o históricos.

La integración canónica única se aplicó en `specs/00..08` y `context/**`.
Se preservaron los registros históricos y no se ejecutaron mutaciones Git.
La revisión de auditoría se reanudó en **R-04: Configuración y modelo de
capacidades**; la siguiente área es **R-05: Estado, filesystem y aislamiento**.

## Cierre de la auditoría

El lote abierto por R-04 queda cerrado: `ACC-017`, `ACC-018`, `ACC-019` y
`ACC-020` están `validado`; `ACC-021` conserva `descartado` porque quedó
absorbida por `ACC-018`. R-05 queda cerrado con `ACC-022` y `ACC-023` en
`validado`, sus incrementos archivados con integración canónica y el contexto
técnico reconciliado. R-06 queda cerrado con `ACC-024` en `validado`, su
incremento archivado bajo `specs/increments/_archive/`, receipts íntegros,
review independiente `OK` y la integración estable aplicada. No quedan
acciones abiertas derivadas de R-04, R-05 o R-06.

**R-10 queda `pending` desde 2026-08-18, en ronda correctiva de cierre.** El claim anterior de `closed` se retira: la revisión independiente halló compatibilidad activa de `copilot-vscode` en `configure` y en entradas/tipos de `init`, además de documentación operativa e informes de cierre contradictorios. Los ACC-039..045 y la reparación de coherencia permanecen físicamente archivados como evidencia histórica; `axiom state` no los expone como candidatos reactivables y el workflow Core actual está terminal `archived`, por lo que no se simulará una reapertura manual. R-10 sólo podrá volver a `closed` cuando un único artefacto correctivo creado y recorrido legalmente por Core cubra la retirada pública del alias, la normalización interna de estados legacy, la reconciliación documental/canónica, la corrección de los registros humanos y una re-revisión independiente sin blockers. Las cifras de validación y receipts anteriores son históricas y no certifican esta ronda correctiva.

Decisiones registradas durante la ejecución:

- `builder` y `local-only` son configuración implícita; `adapterTarget`, roles
   de equipo/código y fases SDD siguen siendo ejes independientes.
- La política de retención local única es `P365D`; el audit trail append-only,
   el sidecar SHA-256 y `axiom audit` permanecen transversales y read-only.
- El registry activo tiene cuatro providers locales: `filesystem`, `serena`,
   `cmm` y `engram`. `CC-004` distingue 16 provider-routed de 3 MCP-only y
   reporta `13/16` con warning no bloqueante para las tres opcionales restantes.
- Los brokers `axiom-mcp-broker`, `sdd-mcp-server` y `spec-mcp-broker` no son
   gateway y se conservaron sin convertirlos en providers tradicionales.
- Se fijó `testTimeout: 30000` en Vitest porque la suite I/O-heavy fallaba bajo
   el timeout por defecto de 5 segundos aunque pasaba de forma aislada; con el
   ajuste, `npx vitest run` termina en `326/326` archivos y `3313/3313` tests.
- La ruta de lifecycle que se ejecuta desde `Axiom/` puede crear candidatos
   auxiliares bajo `Axiom/axiom.spec/increments/`, mientras el scope canónico de
   freeze/receipts está en `Axiom.Spec/specs/increments/`; ACC-022/ACC-023 se
   cerraron usando explícitamente el scope canónico y no se eliminó ningún
   artefacto product-owned de forma destructiva.

La siguiente área de auditoría recomendada es **R-07: Targets y adapters**. El
bug independiente `BUG-20260702-cli-commands-tsconfig-missing-emit`
queda cerrado el 2026-08-06: `@axiom/cli-commands` tiene ownership único de
los comandos compartidos, la compilación focalizada emite sus 11 módulos y los
smoke checks del CLI compilado pasan. La retirada de gateway de `ACC-018` dejó
un fuente app-owned residual que compilaba contra tipos retirados; se eliminó
`apps/cli/src/commands/gateway.ts`, se limpiaron sus salidas stale y el build
raíz limpio vuelve a pasar.

## Supersesión R-11 — ACC-046 (2026-08-20)

ACC-046 queda **validado** mediante `INC-20260819-r11-mcp-documentation`. La evidencia ejecutable fija un único broker MCP gestionado y project-scoped: `axiom-mcp-broker`, arrancado solo con `axiom mcp serve --kind axiom`. El binding efectivo es `.axiom/mcp.yml`, cargado y validado por `resolveMcpServerContext`; `mcp-manifest.yaml` e `integrations.yaml` son catálogo y política de materialización. El dispatcher llama directamente a `@axiom/mcp-tools` y publica los 25 handlers de `sdd.*`, `spec.*`, `memory.*` y `axiom.*`; no usa el router genérico de providers. Engram es un MCP local de tercero; su carácter de backend de memoria opcional en R-11 fue superado por R-12, donde es obligatorio y único para la persistencia de memoria.

Esta supersesión prevalece sobre cualquier afirmación anterior de este plan que presente `sdd-mcp-server`, `spec-mcp-broker` o el kind `memory` como brokers/procesos activos, incluidas las formulaciones históricas de ACC-018/ACC-020 y de las decisiones de cierre. Esos identificadores se conservan solo como historia o limpieza compatible. Las correcciones textuales de writers CLI se limitaron a documentación materializada y no cambiaron protocolo, handlers, registro de tools ni semántica de comandos.

Validación independiente: `npx vitest run packages/mcp-server/tests packages/mcp-tools/tests apps/cli/tests/mcp-serve.test.ts apps/cli/tests/mcp-real-manifest.test.ts apps/cli/tests/workspace-mcp.test.ts apps/cli/tests/native-mcp-config.test.ts apps/cli/tests/workspace-process-surfaces.test.ts --reporter=dot` terminó con 23 archivos y 165 tests PASS (incluido el stderr esperado de path-guard); `npm run build` PASS. La review independiente posterior no encontró desviaciones funcionales. Las specs 03/05/06 y `context/integrations/01-capabilities-providers-y-toolchain.md` ya contenían los hechos estables y no requirieron reescritura.

## Supersesión R-11 — ACC-047 (2026-08-20)

ACC-047 queda **validado** mediante `INC-20260820-r11-auditor-scope`. Las tres superficies mantenidas de `axiom-runtime-auditor` contienen la misma regla normativa: `Axiom/` es el objeto y la evidencia primaria del runtime; `Axiom.Spec/` aporta intención, contraste y consolidación; `Axiom.SDD/` aporta contrato de trabajo y alojamiento, pero su configuración local —incluido MCP— no demuestra integraciones del producto salvo petición explícita de auditar ese repositorio. La comprobación determinista confirmó el bloque común 3/3 y `git diff --check` no reportó errores; no hay build o suite aplicable a estos Markdown. No se modificaron `specs/00..08` ni `context/**` porque el cambio no altera conocimiento técnico del producto.
## Cierre definitivo R-12 — memoria y disciplina de captura (2026-08-22)

**R-12 queda cerrado.** Esta supersesión prevalece sobre cualquier afirmación anterior de este plan que describa Engram como backend opcional de memoria. Engram sigue siendo un MCP local de terceros y no un broker gestionado de Axiom, pero es el único backend de persistencia de memoria soportado por el runtime: no existe backend ni fallback JSON, y la ausencia, fallo de handshake o fallo de tools se comunica como `MemoryError` visible. TC-024 comprueba `engram --version` y falla con remediación explícita cuando el binario no está disponible.

Se retiraron `axiom learn`, las lecciones derivadas de audit y `recentLessons`; el audit trail se conserva como trazabilidad. Se retiró `--manual` de `axiom memory add`: invocar el comando es siempre una acción explícita. Las skills `axiom-increment` y `axiom-bug` capturan decisiones y bugs mediante `axiom memory add`, informan id/error y clasifican conocimiento confirmado sin secretos como `project-shared`; `private` permanece legible localmente, nunca es un secreto y queda fuera de Knowledge Sync. `MemoryKind: 'learning'` se conserva como tipo semántico general y no reintroduce `axiom learn`.

Los cuatro incrementos de R-12 están archivados con metadata y receipts gobernados: `INC-20260821-r12-remove-learn`, `INC-20260821-r12-remove-memory-manual-flag`, `INC-20260821-r12-engram-only-memory` e `INC-20260821-r12-agent-memory-capture-discipline`. El bug de cierre `BUG-20260822-164731-wbhehi` también está archivado: corrigió la query FTS vacía de `freeze` mediante el ancla `rationale`, cubrió el rechazo en el stub hermético y permitió regenerar freezes reales de R-12.

La evidencia final incluye 94 pruebas dirigidas del lote R-12, 26 pruebas de regresión de freeze/Engram, `npm run build`, smoke de memoria CLI, freezes reales, `npm run doctor` PASS con TC-024, `npm run readiness:first-project` PASS, `axiom index validate` correcto, índice técnico regenerado por CLI y `git diff --check` sin errores. Los README humanos archivados y los criterios del bug quedaron reconciliados con sus metadata y receipts; no se alteraron IDs, estados estructurales ni recibos inmutables. La integración estable permanece en `specs/00..08`, manuales y `context/**` ya reconciliados.
## Registro histórico — auditoría read-only y handoff de R-13 estructural (baseline previa a implementación, 2026-08-29)

> Esta sección conserva la evidencia y el brief tal como se observaron antes de implementar B→F. Todos los estados, STOP/GO y frases en presente de este bloque describen exclusivamente aquella baseline; no son el estado vigente. El estado final se registra en los artifacts gobernados y en la síntesis de cierre añadida al final del plan.

### Alcance, precedencia y baseline preservado

Esta sección consolidó el resultado de la auditoría previa de `INC-20260829-r13-structural-mutation-safety` (ACC-065..ACC-067) y fijó el orden de entrega del bloque estructural de R-13. La evidencia primaria fue el runtime `Axiom/`; los incrementos congelados expresaban el estado deseado, no probaban que existiera. La auditoría detallada correspondió a E y trató B, C, D y F como gates posteriores.

En aquella sesión no se modificaron los documentos congelados, `metadata.yml`, receipts, IDs, estados, índices, código, configuración, tests ni Git. El hash `c9ed6b8aec1051df6912cc622c00ae5e5ca9950874467add41cace10885bcc04` es histórico y fue sustituido por freezes posteriores.

Estado observado en esa baseline histórica:

1. A, ACC-050..ACC-056: validado; proporciona identidad/catálogo y primitives por archivo (`@axiom/core`) que deben reutilizarse.
2. B, `INC-20260829-r13-topology-schema2-authority` (ACC-057..ACC-059): `plan-approved`.
3. C, `INC-20260829-r13-topology-bindings-persistence` (ACC-060..ACC-062): `plan-approved`; depende de B.
4. D, `INC-20260829-r13-workspace-resolution-surface` (ACC-063, ACC-064 y ACC-069): `plan-approved`; depende de A, B y C.
5. E, `INC-20260829-r13-structural-mutation-safety` (ACC-065..ACC-067): `plan-approved`; depende de B, C y D.
6. F, `INC-20260829-r13-workspace-step-reconciliation` (ACC-068): `plan-approved`; solo se implementa después de D y E.

El orden obligatorio es **B → C → D → E → F**. Cada unidad debe quedar implementada, validada y revisada antes de usar sus contratos en la siguiente. No se adapta E a topology v1 ni se crea una segunda capa temporal de compatibilidad: hacerlo duplicaría contratos destinados a retirarse en B.

### Resultado de la auditoría de E

#### Cumplimiento observado en aquella baseline

Existían primitives aprovechables, pero todavía no constituían cumplimiento de ACC-065..ACC-067:

- `packages/filesystem-truth/src/path-canonical.ts#canonicalizePath` resolvía `realpath`, symlinks/junctions, nombres Windows 8.3 y ancestros existentes.
- `packages/core/src/local-file.ts` aportaba `acquireLocalFileLockSync`, `withLocalFileLockSync` y `atomicWriteFileSync` para coordinación y reemplazo por archivo.
- `packages/user-workspace/src/registry.ts#upsertProjectReposV2` protegía su propio read-modify-write con lock.
- Setup, adopción, operaciones incrementales y launcher ya contenían validaciones parciales y writers reutilizables.

Ninguna de esas piezas implementaba entonces un preflight global sin escrituras, una transacción recuperable multiarchivo o una reconciliación seccional de `axiom.yaml`.

#### Desviaciones verificadas entonces

**ACC-065 — todavía no estaba implementada en esa baseline.**

- `apps/cli/src/commands/workspace-setup.ts#writeOneRepo` podía crear el directorio antes de completar el ownership check. Un `axiom.yaml` foráneo o inválido producía warning y el flujo podía continuar con otros outputs.
- `apps/cli/src/commands/workspace-incremental.ts#resolveExistingProject` llamaba a `ensureHomeDir`; por tanto la resolución usada antes de una operación podía mutar home.
- Los guards `prepareWorkspaceInput`, `sourceDoesNotOverlapDestination` y `destinationOwnershipError` de `app-onboarding.ts` eran exclusivos del launcher y no formaban una frontera común para CLI, setup, adopt, repo y role.
- No había una comparison key pública y única para equivalencia física/case-folding; duplicar lowercasing o canonicalización privada podía producir decisiones distintas entre superficies.
- No se prevalidaban conjuntamente todos los outputs estructurales y derivados antes del primer `mkdir`, lock, temporal, journal, log, telemetría o modificación del registro.

**ACC-066 — todavía no estaba implementada en esa baseline.**

- `workspace-setup.ts#runWorkspaceSetup` aplicaba writers de forma secuencial y mezclaba estructura indispensable con materialización derivada best-effort.
- `workspace-adopt.ts#runWorkspaceAdopt` encadenaba setup, restauración MCP, topología legacy best-effort y migración sin una unidad transaccional común.
- `workspace-incremental.ts#runRepoAdd` degradaba fallos de topology, bindings o registry a warnings y podía devolver `exitCode: 0` después de un estado parcial.
- `packages/core/src/local-file.ts` coordinaba un recurso, pero todavía no existían coordinador multi-recurso, journal de operación, rollback/roll-forward ni recuperación global.
- `upsertProjectReposV2` encerraba el lock dentro del API de registro; faltaban una operación pura/unlocked o una composición explícita que permitiese incluir `projects.yml` en una transacción externa sin inversión de locks.
- `packages/topology/src/loader.ts#saveLocalBindings` usaba un temporal fijo, no lockeaba y todavía pertenecía al contrato schema 1 observado.
- En esa baseline no existían `StructuralMutationPlan`, `StructuralMutationResult`, `structural-transactions` ni el estado `recovery-required`.

**ACC-067 — todavía no estaba implementada en esa baseline.**

- `workspace-setup.ts#writeOneRepo` volvía a renderizar el documento completo para una identidad que consideraba propia; comentarios y extensiones no emitidos por el renderer se perdían.
- Todavía no existían los delimitadores exactos `# AXIOM:MANAGED:START` y `# AXIOM:MANAGED:END` ni un merger por offsets que preservase bytes exteriores.
- El dogfood `Axiom/axiom.yaml` era markerless y narrativo. No reunía evidencia suficiente para convertirlo automáticamente sin una decisión de migración explícita.

#### Flujos observados entonces y punto de fallo

1. **Setup:** wrapper CLI/launcher → `runWorkspaceSetup` → creación/escritura de repos → topology/bindings/estado/registro → outputs derivados. Un fallo tardío podía dejar recursos anteriores aplicados.
2. **Adopción:** `runWorkspaceAdopt` → setup → restauración MCP → tratamiento legacy/migración. Los pasos posteriores no estaban protegidos por una intención persistida y recuperable.
3. **Repo/role add:** resolución existente —que podía crear home— → alta física → mutación de topology/bindings/registry. Algunos fallos se convertían en warnings y el resultado global podía parecer exitoso.
4. **Launcher:** añadía guards propios y confirmación, pero terminaba delegando en runners con las mismas fronteras parciales. `confirmed` no sustituía preflight, ownership ni atomicidad.

#### Riesgos identificados entonces

- Estado híbrido tras crash entre replaces o tras un fallo tardío de registro.
- Lost update o deadlock si se combinaban project lock, resource locks y el lock interno de `projects.yml` sin orden único.
- `EXDEV` si staging/backup se colocaba en un directorio central de otro volumen.
- Rollback destructivo si un operador modificaba un archivo después del inicio y no se comparaban hashes.
- Alias físicos por symlink, junction, case-folding o nombre 8.3 que evadiesen ownership o solapamientos.
- Conversión markerless que borrase contenido humano o aceptase una identidad ambigua.
- Resultados/exit codes que presentasen como éxito una estructura incompleta.

#### Validación observada entonces

La auditoría fue deliberadamente read-only. Se inspeccionaron código, configuración dogfood, tests existentes y artefactos lifecycle, pero no se ejecutaron Vitest, build, doctor o readiness porque podían crear `dist`, temporales, cachés o estado. En aquella auditoría todavía no existían receipt `increment-verify`, matriz verde, review independiente ni evidencia de integración para E.

#### Recomendación emitida entonces

La recomendación de aquella baseline fue mantener E en `plan-approved`/pendiente y no ejecutar `increment-verify` ni cierre hasta que B, C y D estuvieran aplicadas y verificadas, E cumpliese la matriz indicada abajo, una revisión independiente no encontrase blockers y el conocimiento estable se integrase. El mensaje de commit sugerido entonces, sin crearlo, fue `feat(workspace): make structural mutations recoverable`.

### Flujo objetivo obligatorio para E

El único flujo de mutación estructural será:

1. Recibir y normalizar una intención, con `operationId`, reloj y generador de IDs inyectables para pruebas.
2. Ejecutar `planStructuralMutation` en modo estrictamente read-only: leer, `stat`, `realpath`, parsear y calcular; nunca crear home, directorios, locks, temporales, journal, logs o telemetría.
3. Devolver preview o rechazo tipado. Un rechazo conserva un snapshot byte/metadata idéntico de todos los roots observados.
4. En apply, adquirir primero el project lock y después los resource locks en orden por comparison key canónica.
5. Recalcular el plan bajo lock y rechazar si cambió cualquier precondición.
6. Recuperar o bloquear ante journals previos incompletos antes de preparar una operación nueva.
7. Crear journal y staging/backup adyacente a cada destino; persistir intención, hashes y estado `prepared` antes del primer replace.
8. Aplicar todos los recursos estructurales requeridos y registrar cada frontera durable.
9. Ante fallo, hacer rollback exacto cuando los hashes sean conocidos; si existe contenido desconocido, persistir `recovery-required`, devolver exit no cero y no tocarlo a ciegas.
10. Marcar `committed` solo cuando toda la unidad estructural sea válida.
11. Ejecutar después los outputs derivados. Sus fallos se devuelven como warnings tipados y no revierten una estructura ya válida.
12. Emitir el mismo resultado/envelope desde CLI y launcher.

### Invariantes cerradas

1. **Cero escrituras de preflight:** incluye `mkdir`, home, lock, tmp, journal, logs, telemetría, registry y derivados.
2. **Una sola canonicalización:** `@axiom/filesystem-truth` exporta canonical path y comparison key de plataforma; ningún consumidor implementa su propio lowercasing.
3. **Ownership exhaustivo:** el plan enumera path exacto y owner esperado de cada output estructural y derivado.
4. **Solapamiento bidireccional:** destino dentro de fuente read-only y fuente dentro de destino fallan; también fallan dos identidades sobre el mismo objeto físico.
5. **Replan bajo lock:** preview nunca se aplica si el filesystem cambió.
6. **Orden de locks:** project lock primero; recursos en orden canónico; no se adquiere home si `--no-register`.
7. **Registro solicitado es estructural:** si se solicita, un fallo de `projects.yml` revierte la unidad. `--no-register` omite por completo acceso/mutación de home.
8. **Staging co-localizado:** temporales y backups residen junto al destino para no cruzar volúmenes.
9. **Journal recuperable:** `.axiom-state/<projectKey>/structural-transactions/<operationId>/` usa estados `prepared | committing | committed | rolling-back | rolled-back | recovery-required`.
10. **Recovery por hashes:** todo-after completa `committed`; mezcla conocida before/after hace rollback; hash desconocido deja `recovery-required`.
11. **No rollback ciego:** una edición humana posterior al inicio jamás se sobrescribe si su hash no coincide.
12. **Resultados honestos:** exit 0 solo para `committed`; ningún fallo estructural se degrada a warning.
13. **Idempotencia:** reintento de la misma intención converge a `unchanged` o completa recovery sin duplicar registro ni outputs.
14. **Contenido humano:** solo el bloque Axiom de `axiom.yaml` es propiedad del writer; prefijo y sufijo se preservan byte a byte.
15. **Fail-closed markerless:** solo se convierte un renderer legacy conocido con identidad inequívoca y sin campos conflictivos; ambiguo, inválido o desconocido se rechaza.
16. **Topología única:** E consume exclusivamente los contratos schema 2 y writers autorales producidos por B/C/D.

### Recursos y frontera transaccional

Recursos estructurales requeridos, según la operación: `axiom.yaml` de cada repo afectado, topology autoral schema 2, `topology-bindings.yaml`, `.axiom-state/<projectKey>/workspace.json`, `init.json`, `~/.axiom/projects.yml` solo cuando se solicite registro y directorios creados por la operación. Un directorio solo puede retirarse en rollback si fue creado por esa operación y continúa vacío/poseído.

Quedan post-commit como derivados: AGENTS, configuración materializada, MCP, adapters, skills, rules, process surfaces, base de spec, code-intel e imports. Aunque no pertenezcan al rollback estructural, sus destinos se incluyen en el preflight de ownership y solapamiento.

### Contratos mínimos a implementar

Ubicación preferida: junto a los comandos en `Axiom/apps/cli/src/commands/`; no crear un paquete `packages/structural-mutation` salvo evidencia nueva que justifique ese coste.

- `workspace-structural-types.ts`: `StructuralMutationIntent`, `StructuralMutationPlan`, `StructuralResourceChange`, `StructuralMutationResult`, `ResourceOutcome`, warnings y errores estables.
- `workspace-structural-plan.ts`: `planStructuralMutation(...)`, comparación de snapshots, preflight común y enumeración completa de outputs.
- `workspace-structural-transaction.ts`: apply, lock ordering, replan, journal, staging, commit, rollback y `recoverStructuralMutations(...)`.
- `workspace-axiom-yaml.ts`: parser/renderer/merger de bloque gestionado y conversión legacy conservadora.
- `workspace-derived-steps.ts`: catálogo/runner post-commit de derivados, preparado para que F lo sustituya o generalice sin duplicarlo.

Resultado mínimo:

- Estado global: `committed | rejected | failed | recovery-required`.
- Recurso: `created | updated | unchanged | skipped`.
- Warnings derivados separados de errores estructurales.
- Envelope JSON v1 coherente con los contratos de C y los wrappers existentes; stdout JSON limpio y diagnóstico humano por stderr.
- `rejected`, `failed` y `recovery-required` terminan con exit no cero.

El bloque de identidad usa exactamente:

```text
# AXIOM:MANAGED:START
...contenido propiedad de Axiom...
# AXIOM:MANAGED:END
```

El merger localiza offsets, sustituye solo ese rango y conserva exactamente BOM, newline style, prefijo, sufijo, comentarios y extensiones humanas. Cero o múltiples pares, orden inválido, YAML no interpretable o identidad conflictiva producen error de preflight. `Axiom/axiom.yaml` no se convierte automáticamente solo por ser dogfood: debe pasar las mismas condiciones o recibir una migración explícita revisada.

### Archivos y símbolos de integración previstos

Después de aplicar B/C/D al HEAD, E debe refactorizar o delegar, no mantener caminos paralelos, en:

- `apps/cli/src/commands/workspace-setup.ts`: `writeOneRepo`, `runWorkspaceSetup`.
- `apps/cli/src/commands/workspace-adopt.ts`: `runWorkspaceAdopt`.
- `apps/cli/src/commands/workspace-incremental.ts`: `resolveExistingProject`, `runRepoAdd` y la ruta de role add.
- `apps/cli/src/commands/workspace.ts`: `handleWorkspaceSetup`; setup normal mantiene apply por defecto y obtiene un `--dry-run` real que usa el planner común.
- `apps/cli/src/commands/app-onboarding.ts`: `prepareWorkspaceInput`, `sourceDoesNotOverlapDestination` y `destinationOwnershipError` dejan de ser una frontera exclusiva y delegan en preflight común.
- Wrappers/endpoints launcher que exponen preview/apply: reutilizan plan, resultado y envelope; no reinterpretan warnings estructurales como éxito.
- `packages/filesystem-truth/src/path-canonical.ts`: exporta la comparison key pública junto a la canonicalización existente.
- `packages/core/src/local-file.ts`: se reutilizan locks y atomic writer; cualquier helper multi-lock debe ser una composición pequeña de esos primitives, no otro protocolo.
- `packages/user-workspace/src/registry.ts`: añadir una operación pura o unlocked explícita para componer el cambio bajo el coordinador, conservando la API lockeada para callers independientes.
- Writers topology/bindings resultantes de C: exponer prepare/validate/write bajo lock o una API equivalente compatible con la transacción; retirar el camino de `saveLocalBindings` inseguro.

`axiom init` reutiliza el renderer de bloque gestionado, pero la ampliación de toda su operación a la transacción multiarchivo no forma parte de E. Setup sigue aplicando por defecto para no introducir una ruptura no solicitada; preview real se activa mediante `--dry-run` y launcher.

### Pasos de implementación de E

1. **Gate de entrada:** comprobar que B/C/D están verificadas en runtime, sus contratos públicos se compilan y no quedan productores topology v1 en las rutas afectadas.
2. **Filesystem truth:** publicar y probar comparison key para Windows/POSIX, ancestros no existentes, symlink, junction, 8.3, case-fold y espacios.
3. **Tipos y errores:** definir intents, plan, snapshots, outcomes, estados globales y códigos estables sin acoplarlos a Commander o HTTP.
4. **`axiom.yaml`:** implementar primero parse/merge/render y fixtures byte-exactos; no integrar writers hasta pasar casos ambiguos y markerless.
5. **Planner/preflight:** reunir inputs de setup/adopt/repo/role, resolver todos los targets y read-only sources, calcular desired documents ya validados y producir preview determinista sin efectos.
6. **Coordinador:** implementar locks, replan, journal, staging/backup adyacente, replaces, rollback y recovery con fault hooks inyectables.
7. **Registro y writers:** adaptar registry/topology/bindings/workspace state a prepare/apply sin locks anidados ni serializaciones parciales.
8. **Runners:** migrar setup, adopt, repo add y role add al coordinador; separar derivados; eliminar degradaciones de fallos estructurales a warnings.
9. **Superficies:** conectar `--dry-run`, resultados humanos, JSON y launcher al mismo modelo; no duplicar guards.
10. **Recovery de arranque:** antes de otra mutación del proyecto, inspeccionar journals incompletos y recuperar o bloquear con remediación visible.
11. **Regresión y dogfood:** actualizar únicamente runtime/config/docs operativas que correspondan al contrato implementado, revalidar el workspace Axiom y no auto-convertir contenido humano ambiguo.
12. **Cierre:** ejecutar la matriz completa, revisión independiente, integración estable y lifecycle por Axiom Core; no editar metadata/status/receipts a mano.

### Matriz de pruebas obligatoria

Crear o completar, como mínimo:

- `apps/cli/tests/workspace-structural-preflight.test.ts`
- `apps/cli/tests/workspace-structural-transaction.test.ts`
- `apps/cli/tests/workspace-structural-recovery.test.ts`
- `apps/cli/tests/workspace-structural-concurrency.test.ts`
- `apps/cli/tests/workspace-axiom-yaml.test.ts`

Casos mínimos:

1. **Preflight positivo:** setup/adopt/repo/role legítimos, reejecución e idempotencia desde axiomRepo y code repo.
2. **Preflight negativo y cero mutación:** ID/role reservado, duplicados, mismo path con dos identidades, foreign/invalid YAML, flags create incoherentes, destino↔legacy/context solapados, path ya poseído por otro proyecto y output derivado ajeno. Comparar árbol, bytes y ausencia de home/locks/tmp/journal antes/después.
3. **Path/security:** symlink, junction, 8.3, case-fold, espacios, ancestro no existente, file en lugar de directory y cambios TOCTOU entre preview/apply.
4. **YAML:** comentarios, extensiones, BOM/newline, prefijo/tail humano byte-exacto, markers múltiples/invertidos, YAML inválido, identidad conflictiva, renderer legacy conocido, markerless desconocido e idempotencia.
5. **Fault injection:** antes y después de cada write/fsync/rename/estado de journal para todos los recursos requeridos.
6. **Recovery:** all-before, all-after, mezcla conocida, hash desconocido, journal truncado, retry y bloqueo por `recovery-required`.
7. **Concurrencia:** dos setup, setup frente a repo add, dos repo/role add, registro solicitado y `--no-register`; sin lost update ni deadlock.
8. **Resultados:** outcomes por recurso, warnings derivados, JSON limpio, stderr, exit codes y paridad CLI/launcher.
9. **Regresión:** `workspace-setup.test.ts`, `workspace-adopt.test.ts`, `workspace-incremental.test.ts` y `workspace-command.test.ts`, además de suites topology/bindings/registry afectadas por B/C/D.
10. **No objetivos:** demostrar que derivados fallidos no revierten estructura comprometida y que contenido humano ajeno nunca se borra en rollback.

Comandos post-apply previstos, no ejecutados por esta auditoría:

```text
npx vitest run apps/cli/tests/workspace-structural-preflight.test.ts apps/cli/tests/workspace-axiom-yaml.test.ts
npx vitest run apps/cli/tests/workspace-structural-transaction.test.ts apps/cli/tests/workspace-structural-recovery.test.ts apps/cli/tests/workspace-structural-concurrency.test.ts
npx vitest run apps/cli/tests/workspace-setup.test.ts apps/cli/tests/workspace-adopt.test.ts apps/cli/tests/workspace-incremental.test.ts apps/cli/tests/workspace-command.test.ts
npm run typecheck
npm run build
npx vitest run
npm run doctor
npm run readiness:first-project
```

Añadir las suites focalizadas de B/C/D que resulten afectadas y una comprobación de diff/hygiene. Si la suite completa muestra interferencia concurrente, aislar y explicar la causa; una pasada aislada no sustituye una suite final reproducible.

### Evidencia exigida antes de verificar/cerrar

- B, C y D implementadas y verificadas; F permanece posterior a E.
- Todos los criterios CA-E1, CA-E2 y CA-E3 trazados a tests positivos y negativos.
- Fault injection y concurrencia verdes, no solo happy paths.
- Typecheck, build, suite completa, doctor y readiness con resultados registrados.
- Smoke del dogfood schema 2 sin fallback topology v1 y sin conversión destructiva de `axiom.yaml`.
- Revisión independiente contra intención, criterios, blast radius, lock ordering y recovery; cero blockers abiertos.
- Receipt `increment-verify` emitido por Core y transición/archivo solo mediante comandos Axiom; nunca edición manual.
- Integración final de conocimiento estable en `specs/00..08` y `context/**`; el desired state no se publica como comportamiento vigente antes de la evidencia.
- Documentación operativa cercana actualizada después del apply, sin reescribir artefactos archivados o receipts históricos.

### Blast radius y estrategia de recuperación

El blast radius incluye setup/adopt, repo/role add, init parcial por renderer, topology/bindings, registry user-level, workspace state, project resolution, launcher onboarding y todos los generadores derivados. La implementación debe conservar cambios locales no relacionados y dividir commits lógicos por dependencia; no mezclar E con seguridad del launcher o R-13.5.

Ante fallo de despliegue, no se revierte eliminando archivos desconocidos. Primero se ejecuta recovery mediante journal y hashes. Si existe un hash no reconocido, se conserva evidencia, se marca `recovery-required` y se exige intervención explícita. Los backups solo se eliminan después de `committed` durable y de validar que no se necesitan para una recuperación pendiente.

### Handoff para una sesión de implementación

Prompt reutilizable:

```text
Implementa el bloque estructural pendiente de R-13 en C:\repos\Axiom Workspace. Lee primero Axiom.SDD/AGENTS.md, Axiom/AGENTS.md, la sección "Auditoría read-only y handoff de implementación R-13 estructural — ACC-057..ACC-069" de Axiom.Spec/plans/PLAN-REVISION-INTEGRAL-AXIOM.md y los artefactos congelados de los incrementos B, C, D, E y F. Trata Axiom/ como evidencia runtime primaria. Ejecuta estrictamente B → C → D → E → F, verificando y revisando cada incremento antes del siguiente; no adaptes E a topology v1. Implementa cambios en Axiom/, gestiona lifecycle, estados, receipts, archive e índices solo mediante Axiom Core, y consolida specs/context únicamente cuando el comportamiento esté probado. Para E respeta preflight de cero escrituras, comparison key común, lock ordering, replan, journal/recovery, resultados honestos y preservación byte a byte fuera de los markers de axiom.yaml. Ejecuta las suites focalizadas, fault injection, concurrencia, typecheck, build, suite completa, doctor y readiness. No cierres ningún incremento con blockers o dependencias pendientes y solicita revisión independiente antes de verificar/archivar.
```

Este handoff no autoriza implementar ACC-070..ACC-076 ni abrir R-13.4/R-13.5: pertenecen a incrementos y auditorías separados.

## Addendum de recuperación de B y gate validator-first — ACC-057..ACC-059 (2026-09-03)

### Alcance y efecto sobre el handoff anterior

Este addendum registra el análisis posterior a dos ciclos de reparación de `INC-20260829-r13-topology-schema2-authority`. No reescribe la auditoría histórica de E ni crea un incremento nuevo. **Supersede únicamente las instrucciones operativas de B** dentro del handoff anterior; conserva el orden global B → C → D → E → F → G → H → I y mantiene C–I bloqueados hasta el gate GO definido al final.

Esta sesión es `flow=knowledge_only`, `route=sdd`: solo actualiza este plan. No autoriza cambios de runtime, specs canónicas `00..08`, `context/**`, metadata, freeze, receipts, índices, status, archive ni Git. El futuro apply reutilizará B y deberá volver a ejecutar `axiom state` y `axiom freeze --increment INC-20260829-r13-topology-schema2-authority` antes de delegar a `axiom-increment`.

### Estado observado y dictamen

B permanece `plan-approved`, sin receipt `increment-verify` y sin evidencia suficiente de cierre. La última validación dirigida obtuvo build verde y 5 archivos/39 tests verdes, pero `npm run doctor` falló TC-001 y la revisión independiente confirmó incumplimientos de ACC-058/ACC-059. Esa evidencia prueba que una suite focal verde no sustituye la conformidad con el contrato.

Blockers confirmados:

1. **Inversión de precedencia normativa.** La spec refinada exige IDs de repos y roles globalmente únicos. Una fixture MCP reutilizaba `backend` como `codeRepos[].id` y `roles[].id`; para hacerla pasar se eliminó del validator la colisión repo↔role. Se adaptó producción a una fixture inválida en vez de corregir la fixture.
2. **Validación dependiente de un anchor que se pierde.** `loadTopology` valida refs con el root autoritativo resuelto, pero devuelve solo `TopologyManifest`. Doctor vuelve a validar el mismo manifest con `resolution.rootPath`; desde `Axiom/`, `axiomRepo.ref: .` se reancla al code repo y converge falsamente con `codeRepos[].ref: ../Axiom`.
3. **Una misma entrada se valida con reglas contextuales distintas.** `TopologyValidationOptions.projectRoot` es opcional y su nombre no expresa que debe ser el root de autoridad. `topology validate` vuelve a llamar al validator sin anchor; `resolveRepoPath` redescubre autoridad y, si falla, cae al cwd. El resultado depende del consumidor, no solo del manifest y su autoridad.
4. **Pérdida de errores.** Doctor convierte cualquier `TopologyError` en `null`/`skip`; además supedita TC-001 a `profiles.yaml`, aunque la semántica schema 2 debe depender únicamente de `topology.yaml#roles`.
5. **Autoridad inferida por ausencia de evidencia.** Una raíz sin `axiom.yaml` ni topology local se acepta hoy como axiomRepo por `kind === undefined` y recibe un default. Eso permite que una carpeta vacía o `Axiom.SDD` parezca autoridad local en vez de fallar de forma tipada.
6. **Migración incompleta de fixtures y comentarios.** Quedan fixtures felices o comentarios que describen schema topológico 1. No toda aparición de `schemaVersion: 1` es topología: bindings locales, envelopes y otros dominios mantienen versionado propio y no deben eliminarse por búsqueda ciega.
7. **Dogfood no integrable todavía.** `Axiom.Spec/axiom.config/topology.yaml` existe como autoridad propuesta y `Axiom/axiom.yaml` apunta a ella, pero doctor no converge y falta demostrar explícitamente que Axiom.SDD permanece read-only y que una carga directa desde una raíz legacy no identificada falla en vez de sintetizar un default.

Dictamen: **B sigue pending; C no puede comenzar.** El próximo apply no es otra reparación ad hoc, sino una remediación contract-first del mismo incremento B, con tests de contrato rojos antes de cambiar producción y revisión independiente antes de cualquier verify.

### Precedencia normativa cerrada

Para cualquier contradicción durante el apply se usa este orden, sin excepciones implícitas:

1. Decisiones cerradas y criterios de `Axiom.Spec/specs/increments/INC-20260829-r13-topology-schema2-authority/`.
2. ACC-057..ACC-059 y este addendum del plan.
3. Tipos públicos y findings esperados, una vez alineados con 1–2.
4. Tests y fixtures.
5. Comportamiento histórico y comentarios.

Consecuencias:

- Un test no redefine el dominio. Si una fixture feliz no pasa el validator canónico, se corrige la fixture o se detiene el apply.
- Relajar una invariante exige primero una contradicción explícita en la spec y una decisión documental; no se hace para obtener una suite verde.
- Los artefactos archivados pueden conservar historia. Código, fixtures felices, comentarios operativos y documentación canónica activa no pueden presentar schema topológico 1 como vigente.
- `LocalBindings.schemaVersion: 1`, envelopes v1 y otros schemas ajenos al manifest topológico no son residuos de ACC-057.

### Contrato objetivo no negociable

#### 1. Unicidad e invariantes

- Existe un único namespace de identidad para `axiomRepo.id`, todos los `codeRepos[].id`, todos los `legacyRepos[].id` y todos los `roles[].id`.
- Cualquier segundo uso emite `duplicate-id`; las colisiones repo↔role usan `scope: global`.
- El fixture válido equivalente al caso MCP usa, por ejemplo, repo `backend`, role `builder` y assignment `backend → builder`; `backend → backend` es un negativo explícito.
- Se conservan las demás invariantes cerradas: kinds por bucket, refs no vacías, paths locales no convergentes, legacy 0..2 con funciones `sdd|spec` no repetidas y read-only, assignments solo a code repos/roles declarados, exactamente una primary por code repo, como máximo un code repo primary por role, mode coherente, shape cerrada y QA lane válida.

#### 2. Validación una vez, con procedencia

B introducirá un resultado cargado tipado —nombre recomendado y cerrado para el brief: `LoadedTopology`— con al menos:

```ts
interface LoadedTopology {
  readonly manifest: TopologyManifest;
  readonly authority: TopologyAuthority;
  readonly validation: Extract<ValidationResult, { ok: true }>;
}
```

- `loadTopology(startPath)` devolverá `Result<LoadedTopology, TopologyError>` y será la única frontera load→parse→validate para consumidores.
- El éxito significa que el manifest fue validado contra `authority.rootPath`; ningún consumidor lo vuelve a validar.
- `validateTopology` seguirá disponible para writers, builders y tests directos, pero recibirá obligatoriamente `{ authorityRoot }`. Se retiran el nombre ambiguo `projectRoot` y el anchor opcional.
- `saveTopology` resolverá primero la autoridad y validará el desired manifest exactamente con ese `authorityRoot` antes de cualquier escritura.
- `resolveRepoPath` recibirá el `authorityRoot` ya resuelto —o el `LoadedTopology`— y no redescubrirá autoridad ni caerá silenciosamente al cwd. C podrá endurecer después los bindings; B fija el anchor de las refs autoritativas.
- No se añade un overload de compatibilidad que permita omitir el anchor ni una segunda validación “best effort”.

#### 3. Prueba positiva de autoridad

`resolveTopologyAuthority` distinguirá ausencia, identidad válida e identidad inválida:

- topology local presente y repo no declarado como code/legacy: autoridad local explícita;
- `axiom.yaml` válido con `kind: axiom` y topology ausente: único caso de default single-repo en memoria;
- code/legacy: exige puntero local canonicalizable a un axiomRepo y manifest autoritativo existente;
- `axiom.yaml` inválido: error tipado, nunca equivalente a ausencia;
- raíz sin identidad y sin topology: `authority-not-found`, nunca default;
- una copia local dentro de un code/legacy repo no gana frente al puntero ni se convierte en autoridad.

Axiom.SDD no recibe `axiom.yaml`, topology ni otro marcador para hacer pasar pruebas. Está representado por el manifest autoritativo como legacy read-only. Una carga directa desde su root, al no aportar identidad/puntero, debe fallar de forma tipada y no escribir nada. La resolución desde repos sin identidad pertenece a D, no se anticipa con escaneo de padres/hermanos en B.

#### 4. Consumidores fail-closed

- Doctor consume `LoadedTopology`. TC-001 pasa ante load validado y falla preservando `TopologyError/findings` ante error; nunca revalida ni convierte topology inválida en skip.
- La lectura de profiles se limita a TC-002. Ausencia o error de profiles no omite TC-001.
- `topology show|validate` formatea el éxito ya validado o el error real del loader; no llama de nuevo a `validateTopology`. El envelope JSON estable y sus canales completos siguen perteneciendo a C/ACC-061.
- MCP proyecta `loaded.manifest` y conserva el `TopologyError`; no sintetiza un manifest de fallback tras error.
- Writers y mutaciones de roles construyen el desired manifest, lo validan con la autoridad y solo entonces persisten. Un error deja bytes y mtime del archivo autoral sin cambios.
- Los demás consumidores que necesiten resolver refs usan la procedencia transportada; no reconstruyen un root desde cwd, profiles, registry o convenciones de ID.

### Cierre de hallazgos P1/P2 de la segunda revisión

Las referencias anteriores a build, 5 archivos/39 tests y fallo de doctor son **evidencia reportada por el apply previo**. No son un receipt, no se reprodujeron en esta sesión plan-only y no satisfacen ningún gate futuro. El próximo implementador debe volver a ejecutar y fechar toda la validación; este addendum no atribuye resultados de otra sesión a la actual.

#### Tabla exhaustiva y mutuamente excluyente de autoridad

La resolución aplica las filas A1–A14 en orden. “Presente” significa que la entrada de directorio existe: un fallo al leerla nunca se degrada a “ausente”. La validez de `axiom.yaml` se limita al subcontrato de identidad/autoridad; otros campos legítimos del documento no se rechazan por no pertenecer a topología. Para cerrar resultados tipados, B conserva `io-error`, `invalid-yaml`, `authority-not-found` y `missing-authority-pointer`, y añade —si aún no existen— `invalid-axiom-config` para shape/campos de identidad, incluido un `axiomRepo` no string, e `invalid-authority-pointer` **solo** para un puntero string no vacío pero inadmisible. No se reutiliza `invalid-manifest` para ocultar un error de identidad.

| ID | Estado observado en `startPath` | Condición adicional | Resultado obligatorio |
|---|---|---|---|
| A1 | `axiom.yaml` presente pero no legible | cualquier topology local | `io-error` con path/cause; sin fallback |
| A2 | `axiom.yaml` legible pero YAML sintácticamente inválido o con clave duplicada | cualquier topology local | `invalid-yaml`; sin fallback |
| A3 | YAML parseado no es mapping, o el subcontrato de identidad tiene campos inválidos | incluye `kind` ausente, no string o fuera de `axiom|code|legacy` | `invalid-axiom-config`; sin tratarlo como ausencia |
| A4 | `axiom.yaml` ausente | topology local presente | autoridad local explícita, `source: authoritative-file`; leer y validar contra `rootPath = startPath`, propagando `io-error`, `invalid-yaml` o `invalid-manifest` según lectura, sintaxis/duplicados o shape/semántica |
| A5 | `axiom.yaml` ausente | topology local ausente | `authority-not-found`; nunca default |
| A6 | identidad válida `kind: axiom` | topology local presente | autoridad local explícita; leer y validar anclada en `startPath`, con la misma propagación tipada de A4 |
| A7 | identidad válida `kind: axiom` | topology local ausente | único default single-repo schema 2 en memoria, anclado en `startPath`; no escritura implícita |
| A8 | identidad válida `kind: code|legacy` | `axiomRepo` ausente o string vacío | `missing-authority-pointer`; una topology local no rescata el caso |
| A9 | identidad válida `kind: code|legacy` | `axiomRepo` existe pero no es string | `invalid-axiom-config`; una topology local no rescata el caso |
| A10 | identidad válida `kind: code|legacy` | puntero string no vacío pero remoto, no canonicalizable o de sintaxis local inválida | `invalid-authority-pointer`; sin cwd, parents, siblings, catálogo ni registry |
| A11 | puntero local canonicalizado | lectura o YAML de identidad del target falla | propagar `io-error`, `invalid-yaml` o `invalid-axiom-config` del target; sin reinterpretarlo |
| A12 | puntero local canonicalizado | target se identifica válidamente como `code|legacy` | `invalid-authority-pointer`; un repo miembro no puede ser autoridad |
| A13 | target sin identidad o con `kind: axiom` válido | topology autoral ausente | `authority-not-found`; el default de A7 nunca se activa a través de un puntero |
| A14 | target sin identidad o con `kind: axiom` válido | topology autoral presente | leer y validar con `authorityRoot = target`, propagando `io-error`/`invalid-yaml`/`invalid-manifest`; además exigir `manifest.axiomRepo.kind === 'axiom'` y convergencia física de su ref con el target |

Reglas transversales de la tabla:

- En A8–A14 se ignora cualquier copia local dentro del code/legacy repo, tanto válida como inválida; nunca gana ni se reescribe.
- En A4, A6 y A14, “topology presente” no presupone que pueda leerse: race/permiso es `io-error`, sintaxis o clave duplicada es `invalid-yaml`, y documento no-map, shape o semántica inválidos es `invalid-manifest` con findings cuando correspondan.
- Punteros relativos y absolutos pasan por la misma canonicalización física. Symlink/junction, case de plataforma y espacios deben converger al mismo `rootPath` sin alterar la ruta por cwd.
- Ausencia se decide sin excepciones; error de lectura, YAML no-map, `kind` ausente/desconocido y puntero de tipo incorrecto son estados distintos y observables.
- El locator resuelve procedencia; el loader valida el manifest una vez. Ningún consumidor puede reconstruir otra autoridad.

#### Frontera cerrada B/D

| Responsabilidad | B — ACC-057..ACC-059 | D — ACC-063/ACC-064/ACC-069 |
|---|---|---|
| Identidad local | parsear y validar `kind` y el campo `axiomRepo` necesario para localizar autoridad | reconciliar `repoId`, kind, mode/legacyFunction y path físico de la identidad con la entrada concreta del manifest |
| Puntero | exigir string local válido, canonicalizarlo y comprobar que llega a una autoridad admisible con manifest | decidir pertenencia del repo iniciador al proyecto y resolver identidades/catálogos cuando corresponda |
| Anchor | fijar un único `authorityRoot`, validar refs contra él y transportarlo en `LoadedTopology` | consumir esa procedencia; no cambiar el anchor |
| Manifest | parsear schema 2, aplicar invariantes y comprobar que `axiomRepo.ref` ancla la autoridad | comprobar que el `repoId` del iniciador está en `codeRepos|legacyRepos` y que su ref corresponde al root actual |
| Discovery | solo las filas A1–A14; sin búsqueda por parents, siblings, IDs, registry o home | catálogo/home, selección de proyecto y resolución de repos no identificados, según la spec de D |

B puede resolver un code/legacy repo con puntero válido sin afirmar todavía que su `repoId` pertenece al manifest. Esa reconciliación es de D. Para cumplir CA-B3 antes de D, los writers de roles de B se autorizan únicamente desde el axiomRepo o desde una identidad local válida `kind: code` con puntero A1–A14 válido; una identidad `kind: legacy` puede leer pero **nunca escribir**. Esta autorización temporal por identidad+punteo no constituye pertenencia ni ownership reconciliado y D debe endurecerla sin cambiar autoridad/anchor. Cualquier otra API sensible que necesite pertenencia espera a D. B sí rechaza autoridad, puntero o anchor inválidos; D no puede repararlos ni reinterpretarlos.

#### Decisiones cerradas por esta revisión

1. Se reutiliza B; no se crea un incremento sustituto que oculte su estado pendiente.
2. La tabla A1–A14 sustituye cualquier inferencia por `kind === undefined`: solo A7 permite default.
3. Las mutaciones de roles se prueban en copia/fixture temporal; el dogfood real es exclusivamente load/validate.
4. “B técnicamente verificado” y “B closed/archived” son gates diferentes; no se simula su separación editando metadata.
5. La evidencia cross-repo identifica dos revisiones/trees coordinados, pero no promete atomicidad entre repos Git independientes.

### Estrategia de implementación validator-first

El futuro worker ejecutará estas fases en orden. No se permite saltar a migrar consumidores para “ver qué rompe”.

#### Fase 0 — preflight, freeze y ledger del baseline sucio

1. Verificar state, dependencias y el freeze requerido con `axiom freeze --increment INC-20260829-r13-topology-schema2-authority`; si falta o no corresponde al candidato vigente, no delegar apply. Lifecycle, receipts, índices y status se gestionan solo con Axiom CLI/Core.
2. Fijar `T0` antes de editar. En Axiom, Axiom.Spec y Axiom.SDD registrar la unión de `git status --short --untracked-files=all`, `git diff --name-status`, `git diff --cached --name-status` y un inventario/hash de cada path reportado. Así entran tracked, staged si los hubiera, renames, deletes y untracked como `packages/topology/src/paths.ts` o `axiom.config/**`; `git diff` solo nunca es inventario suficiente. No usar `git add`, `commit`, `push`, `reset`, stash ni checkout destructivo.
3. Crear en la evidencia del apply un ledger exhaustivo por ruta/símbolo con ejes ortogonales:

| Ruta/símbolo | Status/hash en T0 | Origen | Owner | Traza B | Disposición |
|---|---|---|---|---|---|
| cada entrada tracked/untracked | valor observado | `preexistente-T0` o `nuevo-apply` | `B|C|D|E|F|G|H|I|fuera-lote` | `CA-B1..CA-B4` o `n/a` | `B necesario` / preservar / STOP y remitir al owner |

`preexistente` describe origen, no ownership. Solo una entrada con `owner=B` y traza CA-B1..CA-B4 puede clasificarse `B necesario`; una entrada preexistente puede pertenecer a B o a otro owner. Cualquier owner C–I permanece STOP y no se amplía. Ningún path ni símbolo puede quedar “misc”, y cada untracked se hashea individualmente. El ledger no autoriza descartar trabajo ajeno ni trasladar silenciosamente cambios entre incrementos.
4. Confirmar que solo B entra en apply y clasificar cada fallo baseline como contrato, fixture, consumidor o preexistente.

#### Fase 1 — oracles ejecutables sobre T0

Añadir primero tests/fixtures de oracle que demuestren, al menos:

- colisión global repo↔role;
- tabla A1–A14, incluidos errores de identidad, topology y puntero;
- mismo manifest desde axiomRepo y code pointer con igual `authority.rootPath`, `topologyPath` y manifest;
- doctor desde code repo sin reanclaje ni falsa colisión;
- topology inválida produce FAIL tipado en doctor, no SKIP;
- raíz sin identidad/manifest y root de Axiom.SDD no reciben default;
- `roles register` con ID colisionante no escribe;
- una fixture feliz schema 2 se autovalida antes de probar el consumidor;
- el contrato TypeScript impide invocar `validateTopology` sin `authorityRoot`.

El worktree ya contiene producción modificada. Por ello la regla correcta es: **después de fijar T0, ningún cambio adicional de producción hasta observar rojo por el motivo esperado en cada oracle que representa un gap confirmado todavía incumplido**. Tests de preservación/regresión cuyo comportamiento ya es correcto se registran verdes y no se deforman para hacerlos fallar. Si un supuesto oracle de gap ya pasa, se inspecciona y reclasifica como regresión o se demuestra que no prueba el gap; nunca se introduce un fallo artificial. Antes de cerrar esta fase solo pueden añadirse tests/fixtures, y la producción preexistente se conserva sin reset.

#### Fase 2 — validator puro

1. Restaurar el namespace global de IDs y `duplicate-id scope: global`.
2. Sustituir `TopologyValidationOptions.projectRoot?` por un contrato obligatorio `{ readonly authorityRoot: string }`; no overload, default ni valor derivado del cwd.
3. Añadir `packages/topology/type-tests/validate-topology-authority-root.ts` y `packages/topology/tsconfig.type-tests.json` (sin nueva dependencia, `noEmit`, incluyendo explícitamente `src/**/*.ts` y `type-tests/**/*.ts`). El fixture contiene una llamada válida y una llamada sin `authorityRoot` marcada `@ts-expect-error`. El gate ejecuta exactamente `npx tsc -p packages/topology/tsconfig.type-tests.json --noEmit`; si el parámetro vuelve a ser opcional, TypeScript falla por directiva no usada. No colocar T1 solo bajo `tests/`, porque el `tsconfig.json` actual de topology incluye únicamente `src/**/*`.
4. Completar una prueba negativa aislada por cada invariante de CA-B2, incluida QA lane; una fixture con varias violaciones no puede ocultar findings ausentes.
5. Mantener parser y validator separados: duplicate keys/sintaxis/shape se detectan antes de semántica; no hay coerciones.
6. Ejecutar la suite de topology. Si una fixture feliz falla, corregir la fixture; nunca relajar producción para salvarla.

#### Fase 3 — autoridad y procedencia

1. Introducir `LoadedTopology` y migrar loader/writer según el contrato de este addendum.
2. Implementar A1–A14 en el lector/locator, con tests uno-a-uno y errores tipados preservados.
3. Eliminar fallback al cwd y cualquier redescubrimiento de autoridad en `resolveRepoPath`.
4. Retirar `defaultInstalledMultiRepoManifest` y su export si el inventario demuestra que no existe consumidor coherente; no conservar compatibilidad histórica sin contrato.
5. Permitir default solo en A7; A4 carga archivo y A5 falla.

#### Fase 4 — consumidores, uno por uno

Orden obligatorio:

1. CLI topology y roles.
2. Doctor TC-001/TC-002.
3. MCP tools/server.
4. Freeze, knowledge, planes, repo-affinity, write-scope y upgrade.
5. Workspace setup/adopt/incremental, member install y launcher afectados.

Cada grupo migra a `loaded.manifest`/`loaded.authority`, ejecuta su suite focal y demuestra que no reaparecen revalidación, fallback o anclaje alternativo. Doctor preserva el `TopologyError`; TC-001 no depende de profiles y TC-002 mantiene su responsabilidad separada. No se acumula todo el fan-out antes de probar.

#### Fase 5 — higiene de fixtures y productores

- Crear `makeValidTopologyFixture` —o equivalente local compartido— con IDs globalmente únicos y autovalidación explícita con `authorityRoot`.
- Las fixtures negativas nombran una sola invariante y nunca se reutilizan como happy path.
- Convertir fixtures felices topológicas v1 a schema 2; conservar v1 solo en negativos explícitos o historia.
- Clasificar cada `schemaVersion: 1` por dominio. `LocalBindings`, envelopes, locks y otros schemas no son topology v1 y no se editan por búsqueda ciega.
- Eliminar comentarios activos sobre mirrors, copia por repo, normalización v1 o fallback desde `axiom.yaml`.

#### Fase 6 — dogfood coordinado y ausencia de mutación

1. **Fixture temporal de escritura:** crear fuera de los tres worktrees una copia/fixture coordinada con axiomRepo, code repo y legacy. Ejecutar allí las mutaciones de roles desde los roots axiom/code, comparar resultado observable y demostrar que solo cambia el topology autoral temporal. Nunca ejecutar `roles register|assign|unassign` sobre la autoridad real de Axiom.Spec durante dogfood.
2. **Dogfood real read-only:** desde Axiom.Spec y Axiom ejecutar únicamente load/show/validate y comprobar el mismo `authority.rootPath`, `topologyPath`, manifest y findings. Desde Axiom.SDD, load/validate directo debe devolver `authority-not-found`. Ninguna de estas tres comprobaciones escribe.
3. **Algoritmo de snapshot:** antes del run se fija una lista exacta de exclusiones: `.git/**`, `node_modules/**`, caché conocida del test runner y temporales del sistema ubicados fuera del repo. Para todos los demás ficheros regulares —tracked y untracked— se genera un inventario ordenado `path relativo | tamaño | SHA-256(bytes)` y después un SHA-256 del propio inventario. No se excluyen `axiom.yaml`, `axiom.config/**`, `.axiom-state/**`, `dist/**`, fuentes, docs ni paths nuevos desconocidos. Añadir una exclusión después invalida la evidencia hasta inspeccionarla.
4. **Delta de Axiom.SDD:** comparar snapshot e inventario completos junto con `git status --short --untracked-files=all` antes/después; los tres resultados deben ser idénticos.
5. **Par cross-repo reproducible:** para Axiom.Spec y Axiom registrar `HEAD`, `HEAD^{tree}`, `git status --short --untracked-files=all` y el snapshot de working tree calculado con el mismo algoritmo/exclusiones antes y después. Si cualquiera cambia durante el run, se repite. La evidencia nombra el par exacto de revisiones/trees/snapshots, pero no afirma commit atómico entre dos repos Git.
6. Comprobar que el dogfood real no cambia bytes, mtime ni deja temporales en la autoridad.

#### Fase 7 — verificación independiente y cierre documental

1. Ejecutar la matriz completa y revisar por separado los diffs de `validate.ts`, `paths.ts` y `loader.ts`.
2. Invocar revisión independiente contra CA-B1..CA-B4, A1–A14, ledger, matriz, type-test y ausencia de mutación.
3. Una relajación de contrato, reanclaje, fallback, error degradado o cambio no clasificado mantiene B pending aunque las suites pasen.
4. Emitir verify/receipt solo mediante la superficie Axiom/Core soportada y validar `axiom phase receipt` cuando exista phase log. No inventar receipts ni editar sus archivos.
5. Después del gate técnico, el orquestador realiza una única integración estable según el inventario documental inferior; el worker de B no edita `specs/00..08` ni `context/**`.

### Matriz de fixtures obligatoria

Cada subcaso separado por “;” requiere fixture/test independiente; no se acepta un único YAML inválido para cubrir varias filas.

| ID | Caso | Resultado obligatorio |
|---|---|---|
| V1 | identidad `kind: axiom` sin topology | default schema 2 validado, solo A7, sin escritura |
| V2 | multi axiom/code y legacy SDD/spec opcionales, IDs todos distintos | `ok`, sin warnings legacy |
| V3 | misma autoridad desde axiomRepo y code/legacy pointer | mismo root, topology path, manifest y validation |
| V4 | mutación de roles desde axiom/code en fixture temporal | solo cambia la autoridad temporal; mismo resultado observable |
| V5 | dogfood real Axiom.Spec/Axiom | load/validate idénticos y read-only |
| V6 | topology local válida sin `axiom.yaml` | A4: autoridad local explícita, nunca default |
| V7 | puntero local relativo/absoluto con espacios; symlink/junction; variación de case donde aplique | misma autoridad física canonicalizada; cada capacidad se ejecuta en plataforma que la soporte y una incapacidad de crear enlace es blocker explícito, nunca skip verde |
| V8 | legacy identificado con puntero válido | localiza la misma autoridad sin escribir legacy |
| N1 | schema 1; schema futuro; campos topológicos retirados | error tipado por fixture; cero rewrite |
| N2 | axiomRepo ausente/wrong kind; code wrong kind; legacy wrong mode/function/cardinalidad | finding específico por fixture |
| N3 | ID/ref vacío; duplicado repo; role; repo↔role | `duplicate-id`; `scope: global` entre dominios |
| N4 | refs relativas/absolutas físicamente convergentes | `path-collision` anclada en `authorityRoot` |
| N5 | ghost repo; unknown role; assignment duplicada | findings tipados correspondientes |
| N6 | cero primary; múltiples primary por code; role primary en varios code repos | finding específico por fixture |
| N7 | mode incoherente; unknown key; tipo incorrecto | error tipado, sin coerción |
| N8 | puntero roto; target code/legacy; copia topology local dentro de code/legacy | fail-closed; copia nunca autoral |
| N9 | ausencia total identidad+topology; Axiom.SDD directo | `authority-not-found`, nunca default |
| N10 | doctor desde code repo; topology inválida; profiles ausente/inválido | TC-001 sin falsa colisión y FAIL tipado; TC-002 independiente |
| N11 | desired manifest inválido en cada writer/roles | bytes y mtime autorales estables; sin temporales |
| N12 | `axiom.yaml` con sintaxis YAML malformada | `invalid-yaml`, sin fallback |
| N13 | `axiom.yaml` parseado como scalar/list/null | `invalid-axiom-config`, sin fallback |
| N14 | `kind` ausente; no string; desconocido | `invalid-axiom-config`, incluso si hay topology local |
| N15 | `axiomRepo` de tipo incorrecto; ausente; vacío; remoto | error diferenciado según A8–A10; una fixture por caso |
| N16 | fallo de lectura inyectado en identidad, target o topology | `io-error` preservado; no se interpreta como ausencia |
| N17 | clave YAML duplicada, incluida `axiomRepo` | `invalid-yaml` antes de construir el mapping |
| N18 | `qaLane` ausente, de tipo incorrecto o fuera de `inline|parallel` | finding tipado específico por fixture |
| N19 | target de puntero con identidad inválida; `kind: axiom` sin topology | propagar error o `authority-not-found`; nunca default indirecto |
| N20 | topology del target declara `axiomRepo.ref` que no converge al target | `invalid-manifest`/finding de anchor; cero reanclaje |
| T1 | `packages/topology/type-tests/validate-topology-authority-root.ts`: llamada sin options/`authorityRoot` y llamada válida | `npx tsc -p packages/topology/tsconfig.type-tests.json --noEmit` pasa solo mientras la inválida produzca el `@ts-expect-error` esperado |

V7 se ejecuta con directorios temporales reales. Case-fold se exige en filesystem case-insensitive; symlink/junction en una plataforma/permisos que permitan crearlos. Si el entorno de validación no ofrece una capacidad, se repite esa fila en un lane compatible o B queda bloqueada; no se acepta `skip` silencioso ni mock que eluda `@axiom/filesystem-truth`.

### Inventario de integración documental final

Este inventario se obtuvo sobre claims activos actuales; por eso `00` y `02` sí entran: ambos contienen referencias topológicas afectadas. Durante el apply delegado no se edita ningún `specs/00..08` ni `context/**`. Tras B técnicamente verificado, el orquestador relee el diff/runtime real, modifica solo claims estables y conserva prosa histórica claramente rotulada.

| Archivo | Claim actual que debe revisarse | Claim objetivo estable | Momento |
|---|---|---|---|
| `specs/00_Resumen_Ejecutivo.md` | dogfood identifica Axiom.Spec como `specRepo`; resumen de roles/topology mezcla nomenclatura previa | describir Axiom.Spec como autoridad dogfood schema 2, Axiom como code y Axiom.SDD como legacy read-only, sin borrar historia | después de review técnico; solo si el runtime dogfood real lo prueba |
| `specs/01_Requisitos_Funcionales.md` | `repoRefs`/`roleAssignments`, `sddRepo` default y retrocompatibilidad con shape legacy aparecen como vigentes | `axiomRepo`/`codeRepos`/`legacyRepos`/`roles`/`assignments`, IDs globales, validator único y autoridad sin fallback v1 | integración final antes de cierre |
| `specs/02_Requisitos_No_Funcionales.md` | aislamiento refiere genéricamente al `TopologyManifest` y a DF-001 con el modelo anterior | conservar el NFR, actualizar buckets/procedencia/fail-closed solo donde el runtime verificado cambie el claim | integración final; no reescribir requisitos ajenos |
| `specs/03_Modelo_Operativo_y_Datos.md` | documenta schema 1, `sddRepo/specRepo/roleCodeRepositories`, `axiom.yaml` autoral y topology derivada/fallback | schema 2 único; topology autoral en axiomRepo; identidad/puntero; default exclusivo A7; bindings v1 claramente separado | integración final obligatoria |
| `specs/04_Flujos_SDD_y_Ciclo_de_Vida.md` | init deriva topology y los flujos dependen de copias per-repo/materialización perezosa | consumidores cargan una autoridad por puntero/procedencia; no afirmar aún bindings/resolution de C/D | integración final, limitada a B |
| `specs/05_Interfaces_Operativas.md` | CLI/workspace/roles describen actualizaciones y lectura con superficies previas | topology/roles muestran o mutan la autoridad validada y preservan errores; detalles envelope/bindings quedan para C | integración final, limitada a comportamiento probado |
| `specs/06_Integraciones_y_Capacidades.md` | `defaultSingleRepoManifest`, topology local dogfood y fallback aparecen vigentes | default solo A7; fichero roto nunca se oculta; dogfood real converge en Axiom.Spec/Axiom | integración final tras smoke real |
| `specs/07_Gobierno_y_Seguridad.md` | DF-001 usa `roleCodeRepositories`, topología fallida puede acabar en skip y dogfood se describe sin manifest real | buckets schema 2, procedencia única y postura de error exactamente observada/revisada; Axiom.SDD continúa no escribible | integración final tras doctor/review |
| `specs/08_Glosario.md` | define `TopologyManifest` con shape v1 y “repo de control”; mezcla esa versión con `LocalBindings` v1 | definir schema 2, `TopologyAuthority`, `LoadedTopology` y legacy read-only; mantener `LocalBindings.schemaVersion: 1` como dominio distinto | integración final |
| `context/TECHNICAL_CONTEXT.md` | afirma topology local en Axiom con `specRepo.ref` | reflejar el par Axiom.Spec autoridad/Axiom puntero y citar código/tests verificados | después de specs, en la misma pasada final |
| `context/architecture/02-modelo-de-datos-y-configuracion.md` | topology opt-in derivada de `axiom.yaml`, fallback y materialización perezosa | frontera load→parse→validate, procedencia transportada y única autoridad schema 2; no anticipar C/D | reconciliación técnica final |
| `context/architecture/03-ciclo-de-vida-cli-y-orquestacion.md` | `init` deriva fallback, roles materializa perezosamente y freeze/lifecycle usa `specRepo` topológico como fallback | describir consumidores sobre `LoadedTopology`/autoridad schema 2 y retirar claims activos v1/fallback; conservar historia rotulada | reconciliación técnica final, limitada a consumidores migrados en B |
| `context/operations/02-doctor-troubleshooting-y-telemetria.md` | TC-001..003 aceptan topology “o derivación por fallback” | TC-001 consume `LoadedTopology`, error tipado es FAIL y profiles solo gobierna TC-002 | reconciliación técnica final tras doctor verde |
| `context/references/01-inventario-de-packages.md` | `@axiom/topology` figura opt-in/derivado-en-lectura con fallback desde `axiom.yaml` | describir schema 2 autoral, `LoadedTopology`, autoridad/puntero y ausencia de fallback | reconciliación técnica final tras build/tests |
| `context/references/03-riesgos-y-brechas-conocidas.md` | claims actuales usan topology local dogfood y `specRepo.ref` como evidencia | actualizar solo la resolución vigente: Axiom.Spec autoridad, Axiom puntero y Axiom.SDD legacy read-only; mantener baselines históricos como tales | reconciliación técnica final tras dogfood |

#### Documentación runtime activa propiedad del apply B

B-057.1 exige corregir documentación operativa activa del repo de código, no diferirla como conocimiento canónico. El worker la actualiza **después de tener runtime/tests focales verdes y antes del verify técnico**, sin tocar specs/context:

| Archivo | Claim actual | Claim objetivo | Momento |
|---|---|---|---|
| `Axiom/docs/overview.md` | presenta Axiom.Spec mediante el campo topológico `specRepo` | explicar la autoridad dogfood schema 2 y el puntero desde Axiom sin confundirla con `Axiom/axiom.spec/` | apply B, antes de review técnico |
| `Axiom/docs/cli/init.md` | `installed-multi-repo` deja una ruta de specification en `axiom.yaml` y otros comandos materializan topology | documentar exactamente el output/identidad implementados y A1–A7; retirar cualquier derivación v1/fallback o materialización perezosa incompatible | apply B, después de migrar productores |

Antes de verify se ejecuta además una búsqueda domain-aware en `Axiom/docs/**/*.md`; cada match de `sddRepo|specRepo|roleCodeRepositories`, topology derivada/fallback o copia per-repo se clasifica como claim activo a corregir, historia explícita o término no topológico. Un match adicional activo amplía este inventario dentro de B; no se declara limpio por haber corregido solo dos archivos conocidos.

Si una fila no coincide ya con el runtime final, no se copia el desired state del plan: se detiene el cierre y se resuelve la contradicción contra la spec B. No se crean documentos ni índices nuevos. Los índices derivados, si requieren refresh, se gestionan solo con Axiom Core después de editar sus fuentes canónicas.

### Estados y gate de lifecycle de B

**B técnicamente verificado** exige todo lo siguiente:

1. runtime y consumidores implementados contra CA-B1..CA-B4 y A1–A14;
2. matriz V/N/T, suites focales, type-test dedicado, typecheck, build, suite completa, doctor, readiness y dogfood read-only verdes;
3. ledger sin entradas sin clasificar y ausencia de mutación demostrada;
4. revisión independiente sin blockers;
5. evidencia Core: verify/phase receipt emitido y validado si la superficie existe, o evidencia explícita de que el runtime no ofrece ninguna superficie de verify/receipt y aplicación del fallback estricto inferior.

Ese estado prueba el contrato técnico, pero por sí solo **no significa `closed` ni `archived`** cuando Core permite representarlo por separado.

**B closed/archived** exige además:

1. integrar los claims estables del inventario en `specs/00..08`;
2. reconciliar todas las filas `context/**` o registrar explícitamente que una fila no requiere cambio;
3. actualizar el artifact de B con resultado, validación, review e integración usando la superficie Core permitida;
4. satisfacer todas las reglas de cierre de `AGENTS.md` y ejecutar status/archive solo mediante Axiom CLI/Core.

Protocolo de capacidad Core, decidido antes del verify:

- **Core soporta receipt/verify técnico independiente:** emitirlo y validarlo manteniendo B abierta; habilita GO técnico y la integración documental queda para la pasada final.
- **Core soporta verify pero lo acopla a close/archive o exige integración:** no emitirlo prematuramente; C permanece STOP hasta integrar documentación y completar la transición Core válida.
- **Core no ofrece ninguna superficie verify/receipt:** registrar como evidencia el comando/capability check que lo demuestra; no inventar archivos. C permanece STOP hasta integración documental y cierre/archivo mediante las demás superficies Core disponibles. Si Core tampoco puede efectuar el lifecycle requerido, B permanece pending y C no empieza.

### Comandos y gates post-apply

Ejecutar desde `Axiom/` en este orden; una fase positiva roja bloquea las siguientes y no se compensa con otra suite verde:

```text
npx vitest run packages/topology/tests
npx tsc -p packages/topology/tsconfig.type-tests.json --noEmit
npx vitest run apps/cli/tests/topology.test.ts apps/cli/tests/roles.test.ts packages/doctor/tests/topology.test.ts packages/doctor/tests/qa-lane.test.ts packages/doctor/tests/dogfooding.test.ts packages/doctor/tests/write-scope.test.ts packages/mcp-tools/tests/topology-handlers.test.ts packages/mcp-tools/tests/implementation-context-handler.test.ts packages/mcp-server/tests/spec-scope-convergence.test.ts
npx vitest run apps/cli/tests/member-install.test.ts apps/cli/tests/repo-affinity.test.ts apps/cli/tests/freeze.test.ts apps/cli/tests/knowledge.test.ts apps/cli/tests/upgrade-fanout.test.ts apps/cli/tests/workspace-setup.test.ts apps/cli/tests/workspace-incremental.test.ts
npm run typecheck
npm run build
npx vitest run
node apps/cli/dist/index.js topology validate --path . --json
node apps/cli/dist/index.js topology validate --path ../Axiom.Spec --json
npx vitest run apps/cli/tests/topology-dogfood.test.ts
npm run doctor
npm run readiness:first-project
git diff --check
```

`apps/cli/tests/topology-dogfood.test.ts` es el harness de aserción del negativo real: invoca el CLI contra `../Axiom.SDD`, exige exit no cero **y** error JSON `kind: authority-not-found`, y solo entonces devuelve verde. También falla si aparece cualquier escritura según los snapshots de Fase 6. Así el negativo no se intercala como “fase roja” en una secuencia positiva ni se acepta cualquier error distinto.

Añadir una búsqueda domain-aware de `normalizeLegacyShape`, `sddRepo`, `specRepo`, `roleCodeRepositories`, branches topology schema 1, revalidaciones de manifests cargados y fallbacks al cwd en runtime, tests y `Axiom/docs/**`. Cada match se clasifica como runtime activo, doc activa, test negativo, otro schema o historia; un grep vacío no es evidencia suficiente. Ejecutar `git diff --check` en cada repo modificado. La suite completa sigue siendo obligatoria para el fan-out de B y para ACC-076 al final del lote.

### STOP/GO para C

**STOP** si se cumple cualquiera:

- una fila A1–A14 es ambigua, no está probada o degrada error a ausencia;
- el validator acepta colisión repo↔role o puede llamarse sin `authorityRoot`;
- T1 no está incluido en `tsconfig.type-tests.json` o su comando compile-only no se ejecutó;
- un caller revalida `LoadedTopology`, cambia el anchor o cae a cwd/fallback/SKIP;
- falta el ledger T0, omite staged/untracked, mezcla origen con owner o tiene entradas sin clasificar;
- se añadió producción antes de observar los oracles rojos de gaps confirmados, o se forzó rojo un test de preservación ya verde;
- una fixture feliz topology v1 permanece o una schema 2 feliz no se autovalida;
- Axiom y Axiom.Spec no convergen en el mismo snapshot de autoridad;
- se intentó una mutación sobre el dogfood real, el harness acepta un error distinto o Axiom.SDD cambió;
- el par revision/tree/snapshot no está registrado o cambió durante la evidencia;
- doctor/readiness, suite completa, type-test, matriz o review tienen blockers;
- Core soporta verify/receipt y falta o es inválido;
- Core no permite representar verify separado —incluida ausencia total de receipt— y la integración/cierre fallback aún no terminó.

**GO técnico para C** solo cuando B cumple íntegramente la definición “técnicamente verificado”, Core permite conservarla abierta para integración y no queda ningún STOP. **GO lifecycle/archivo de B** solo después de integración documental y reglas de cierre. Si Core no representa ambos momentos, prevalece el gate más estricto y C espera al cierre completo de B.

### No objetivos de esta remediación

- No implementar bindings strict, envelopes JSON finales, locks/CAS o writer concurrente de C.
- No implementar pertenencia/reconciliación de `repoId`, catálogo/home, repo/role add o workspace state de D.
- No implementar transacciones estructurales de E ni seguridad launcher de G–I.
- No añadir heurísticas de búsqueda por parents, siblings, registry o nombres convencionales.
- No escribir identidad, topology ni proyecciones dentro de Axiom.SDD.
- No crear otro incremento para esconder el bloqueo de B.
- No prometer atomicidad Git cross-repo ni introducir arquitectura enterprise, MCP obligatorio, Workbench o índices nuevos.

### Brief tipado y determinista para el próximo implementador de B

```text
Incremento: INC-20260829-r13-topology-schema2-authority.
Flow/route: increment/sdd. Scope de apply: solo B (ACC-057..ACC-059) en Axiom/; C–I STOP.
Precondición: leer AGENTS.md y este addendum; ejecutar state y axiom freeze --increment INC-20260829-r13-topology-schema2-authority. Si el freeze no es vigente, no aplicar.
Baseline: fijar T0 con status --untracked-files=all, diffs tracked/cached e inventario/hash. Ledger por path con ejes origen(preexistente|nuevo), owner(B..I|fuera) y traza CA-B1..B4; solo owner B trazado es B necesario. Preservar trabajo ajeno y no ejecutar mutaciones Git.
Oracle-first: desde T0 añadir solo tests/fixtures hasta observar rojo por causa esperada en cada gap confirmado. Registrar verdes de preservación sin forzarlos a rojo. Cubrir A1–A14 y T1.
Contrato: restaurar IDs globales repo+role; validateTopology exige { authorityRoot }; loadTopology devuelve LoadedTopology(manifest, authority, validation); una sola validación anclada; errores de identidad/topology/puntero tipados; cero cwd, revalidación, fallback o SKIP.
Frontera: B valida identidad de autoridad, puntero y anchor. Roles escribe desde axiomRepo o kind:code con puntero válido; legacy nunca escribe. Pertenencia/reconciliación de repoId, catálogo/home y demás resolution quedan en D; bindings/envelopes/locks en C, transacciones en E y launcher G–I.
Migración: validator → locator/loader/writer → CLI roles/topology → doctor → MCP → resto de consumidores, con suite focal tras cada grupo. Corregir fixtures inválidas; nunca relajar el contrato para ponerlas verdes.
Type gate: crear packages/topology/type-tests/validate-topology-authority-root.ts + tsconfig.type-tests.json y ejecutar npx tsc -p packages/topology/tsconfig.type-tests.json --noEmit; no confiar en Vitest para T1.
Dogfood: mutaciones de roles solo en fixture temporal coordinada. Axiom.Spec/Axiom reales solo load/show/validate; Axiom.SDD se prueba mediante harness que exige authority-not-found. Snapshots incluyen tracked+untracked con exclusiones previas exactas; registrar HEAD/HEAD^{tree}/snapshot sin prometer atomicidad Git.
Validación: matriz V1–V8/N1–N20/T1, lane real para symlink/case sin skip silencioso, suites focales, typecheck, build, suite completa, validates, harness dogfood, doctor, readiness, diff-check y axiom-review sin blockers.
Docs/lifecycle: durante apply actualizar docs runtime activas inventariadas; no editar specs/00..08 ni context/**. El orquestador integra el inventario canónico final. Metadata/status/receipts/archive solo por Core. Si verify no es separable o no existe receipt, C sigue STOP hasta integración y cierre Core; si tampoco hay lifecycle soportado, B queda pending.
Resultado: ante cualquier STOP, dejar B pending y no iniciar C.
```
## Síntesis vigente de cierre — bloque estructural R-13 B→F (2026-09-07)

Los incrementos B→F quedaron implementados, verificados, integrados y archivados en orden mediante Axiom Core:

1. `INC-20260829-r13-topology-schema2-authority` — archivado; freeze `1fa46f0330296b675a3f07f8123b84f74a89dc2dbb4c510c5daa7917194b29b5`.
2. `INC-20260829-r13-topology-bindings-persistence` — archivado; freeze `135a2e1ad9969766dc2ca7c5175af3374383383787a684c1cea22bb4da953acb`.
3. `INC-20260829-r13-workspace-resolution-surface` — archivado; freeze `c9b70a36cc4623ba50b4a72a44504d978b06f4d07d75bc7c6385355ba1861b0e`.
4. `INC-20260829-r13-structural-mutation-safety` — archivado; freeze `fda546c137fe1a87852450a740716614cf06567650d5a6a03de15550262fef5b`.
5. `INC-20260829-r13-workspace-step-reconciliation` — archivado; freeze `d61a030fd88dfd6727e484d0e1863234c7517469a4108dce81cde4d39b0dad4f`.

La evidencia final suma `292` pruebas dirigidas (`42 + 121 + 25 + 32 + 19 + 53`), build, doctor (`48/61 OK`, `0` fallos), readiness, index validate y diff-check en PASS. Dos intentos de revisión delegada agotaron timeout; la revisión inline de fallback concluyó GO sin blockers. Cada incremento conserva receipts finales `verify`, `knowledge` y `increment-archive`; E conserva además su `increment-verify` vigente. El primer archive de B fue bloqueado correctamente por QA inline pendiente; se ejecutó el lifecycle soportado `qa-e2e start → verify → pass`, quedó evidencia `passed` y el archive posterior fue exitoso. La decisión ENOENT-only/commit estructural unitario/warnings post-commit quedó registrada como memoria `mtrk76gt-8qg5b9sv`.

El conocimiento estable está reconciliado en `specs/00..08` y `context/**`: topology schema 2 autoral, bindings y workspace state estrictos, resolución común, preflight read-only, journals/rollback/recovery, bloque `AXIOM:MANAGED`, resultados honestos y `WORKSPACE_STEP_CATALOG`. La sección de auditoría anterior permanece solo como baseline histórica fechada.

R-13.4 continúa deliberadamente fuera de este lote: `INC-20260829-r13-launcher-control-plane`, `INC-20260829-r13-launcher-catalog-ado` e `INC-20260829-r13-launcher-telemetry-regression` permanecen pendientes para ACC-070..ACC-076. No se implementó ni archivó ninguno de ellos durante este cierre.

## Cierre consolidado R-15 (2026-09-13)

R-15 queda cerrada técnicamente y documentalmente. Las acciones `ACC-088`,
`ACC-089`, `ACC-090`, `ACC-091`, `ACC-092`, `ACC-093`, `ACC-094` y `ACC-095`
quedan en estado vigente `validado`; sus incrementos correspondientes están
archivados por Core.

- `Axiom/docs/**` es el manual único del runtime: 50 familias Commander tienen
   cobertura verificable desde la ayuda compilada y el test de cobertura.
- La fuente única de templates permanece en `Axiom/axiom.spec/templates/`.
- Las decisiones y raíces canónicas de `Axiom.Spec` quedaron reconciliadas por
   ACC-094; las referencias activas obsoletas fueron eliminadas o reencuadradas.
- El cierre usa el gate documental común con `documents`/`affectedPaths`,
   estados `updated`/`unchanged`/`unreviewed` y modos `warning`/`block`.
- El manual runtime se distribuye en `docs/axiom/` durante setup, adopción y
   upgrade explícito, con bundle TypeScript generado desde `Axiom/docs/**`,
   `manifest.json`, `sourceHash`, hashes SHA-256, preservación stale y preview
   sin escritura. `sync` y `configure` quedan fuera.

Evidencia final de ACC-095: generador `1/1`, distribución `6/6`, integración de
setup/adopt/upgrade y cobertura documental `41/41`, `npm run typecheck`, `npm
run build`, `npm run doctor` (0 fallos), `npm run readiness:first-project`,
`axiom index validate` (39 artefactos, 0 fallos), reemplazo atómico sobre archivo
existente en Windows y `git diff --check` en PASS. Receipt verify final:
`c82d0e94350fe082077823cf65043ad30dccf9512a07c1d001d1aa4d77f9147c`; freeze
final: `ff3b777ecd125e4390f3e6ce14e085728bc8e0e39cb054c9c52d7c425230edee`.
La suite global `npm test` clasificó `3742/3764` tests verdes en `346/358`
archivos; sus 22 fallos quedaron fuera de R-15 por ser contratos/timeouts
preexistentes de R-14/R-13, incluida la repetición de
`apps/cli/tests/workspace-step-reconciliation.test.ts`; no se observó una
regresión nueva del lote.

La integración estable se consolidó una sola vez en `specs/00..08` y
`context/**`, incluyendo la resolución de la brecha de cobertura documental y
la frontera entre el manual runtime y el material específico de
`Axiom.Spec/specs/manuales/`.